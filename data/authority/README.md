# Private authority bundle

The selected local revision is `bundle:official-2026-08-20`, bound to the exact bundle root recorded in `receipts/official-2026-08-20.json`. Runtime consumers select that ID and hash locally; they do not fetch, poll, scrape, serve, or resolve a mutable latest revision.

The private evidence lock is `.local/authority/locks/official-2026-08-20/source-set-lock.json`. Git alone is insufficient to rebuild the revision. Rebuild only from either complete independently verified private source set, preserving the original card API bytes and their source hash; the strict in-memory adapter produces derived project card records and never relabels them as publisher bytes.

Official source bytes, normalized corpus, rulebook pages, artwork, private absolute locators, build inputs, and revisions remain outside Git and packages. This repository contains only project code, synthetic tests, and the non-content receipt.

Written publisher permission and a separate approved plan are required before recurring acquisition, broader agent-run collection, sharing, release, hosting, third-party upload, redistribution, a public or network card API, artwork acquisition, or commercial use.
