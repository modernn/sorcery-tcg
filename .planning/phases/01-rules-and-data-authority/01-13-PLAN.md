---
phase: 01-rules-and-data-authority
plan: "13"
type: execute
wave: 10
depends_on: ["01-09", "01-10", "01-12", "01-15"]
files_modified:
  - data/authority/README.md
  - data/authority/receipts/official-2026-08-27-v3.json
  - tests/private-authority/private-revision.test.ts
  - tests/private-authority/repository-boundary.test.ts
autonomous: true
requirements: [DATA-01, DATA-02, DATA-03]
gap_closure: true
must_haves:
  truths:
    - "Only the fresh Plan 15 v3 lock and its verified primary/backup roots can supply final build inputs."
    - "Independent fresh-primary and fresh-backup builds under bundle:official-2026-08-27-v3 are byte-identical and have one independently recomputed bundle root."
    - "The selected ignored v3 revision, safe receipt, and README bind one exact immutable ID/root only after both candidates validate."
    - "Every official-2026-08-20 root, lock, authorization record, receipt, and revision remains byte-for-byte unchanged historical evidence."
    - "Default, full private, and narrow repository-boundary gates pass without network access, source mutation, private leakage, or a mutable/latest fallback."
  artifacts:
    - path: ".local/authority/revisions/official-2026-08-27-v3"
      provides: "Write-once selected private revision rebuilt from verified fresh Plan 15 evidence"
    - path: "data/authority/receipts/official-2026-08-27-v3.json"
      provides: "Safe non-content receipt for the exact new ID and independently verified root"
    - path: "data/authority/README.md"
      provides: "Exact local selection instructions for bundle:official-2026-08-27-v3"
    - path: "tests/private-authority/private-revision.test.ts"
      provides: "Fresh dual-root rebuild, equality, selection, historical immutability, and tamper proof"
    - path: "tests/private-authority/repository-boundary.test.ts"
      provides: "Final reachable-history/worktree/index/package leakage and no-network gate"
  key_links:
    - from: ".local/authority/locks/official-2026-08-27-v3/source-set-lock.json"
      to: "tests/private-authority/private-source-completeness.test.ts"
      via: "completed Plan 15 proof binds the exact fresh roots, seven entries, authorization, content, hashes, and immutable maps"
      pattern: "fresh v3 primary and backup completeness"
    - from: ".local/authority/locks/official-2026-08-27-v3/source-set-lock.json"
      to: ".local/authority/revisions/official-2026-08-27-v3"
      via: "independent fresh-primary and fresh-backup input materialization plus the exact importer contract"
      pattern: "bundle:official-2026-08-27-v3"
    - from: ".local/authority/revisions/official-2026-08-27-v3"
      to: "data/authority/receipts/official-2026-08-27-v3.json"
      via: "independently recomputed selected bundle root and safe seven-source evidence"
      pattern: "official-2026-08-27-v3"
    - from: "data/authority/receipts/official-2026-08-27-v3.json"
      to: "data/authority/README.md"
      via: "later consumers select only the exact v3 bundle ID and receipt root"
      pattern: "bundle:official-2026-08-27-v3"
---

<objective>
Build, validate, publish, and select the final immutable authority revision using only the fresh Plan 15 evidence.

Purpose: Close Phase 1 against the final card, provenance, content, and leakage contracts without reusing nonconforming historical roots or changing historical evidence.
Output: Two byte-identical v3 candidates, one write-once selected private revision, a safe receipt/README selection, and complete release gates.
</objective>

<execution_context>
@C:/Users/Dad/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Dad/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@package.json
@.planning/REQUIREMENTS.md
@.planning/phases/01-rules-and-data-authority/01-CONTEXT.md
@.planning/phases/01-rules-and-data-authority/01-RESEARCH.md
@.planning/phases/01-rules-and-data-authority/01-VALIDATION.md
@.planning/phases/01-rules-and-data-authority/01-08-SUMMARY.md
@.planning/phases/01-rules-and-data-authority/01-09-SUMMARY.md
@.planning/phases/01-rules-and-data-authority/01-10-SUMMARY.md
@.planning/phases/01-rules-and-data-authority/01-11-SUMMARY.md
@.planning/phases/01-rules-and-data-authority/01-12-SUMMARY.md
@.planning/phases/01-rules-and-data-authority/01-14-SUMMARY.md
@.planning/phases/01-rules-and-data-authority/01-15-SUMMARY.md
@data/authority/README.md
@data/authority/receipts/official-2026-08-20.json
@scripts/collect-private-authority.ps1
@src/authority/private-source-set.ts
@src/commands/import-authority.ts
@src/commands/validate-authority.ts
@tests/private-authority/private-source-completeness.test.ts
@tests/private-authority/private-revision.test.ts
@tests/private-authority/repository-boundary.test.ts

<interfaces>
Completed Plan 15 evidence:
- Lock: `.local/authority/locks/official-2026-08-27-v3/source-set-lock.json`.
- Exact acquisition identity: `user-authorized-user-run-one-shot-powershell` / `phase-01-20260827-private-reacquisition-1`.
- `fresh v3 primary and backup completeness` proves both lock-resolved roots pass `verifyPrivateSourceSet` and the shared Plan 14 content verifier with unchanged before/after maps.
- The authorization is consumed and grants no retry, recurring acquisition, broader use, publisher permission, or legal clearance.

Completed importer/validator contract:
- `pnpm authority:import -- --input <input-root> --input-lock input-lock.json --expected-input-root-hash <independently-recomputed-input-hash> --output-root <candidate-parent> --revision-id official-2026-08-27-v3`
- `pnpm authority:validate -- --root <candidate-revision-root> --bundle bundle.json --id bundle:official-2026-08-27-v3 --hash <independently-recomputed-bundle-root>`
- Candidate output is accepted only by fixed stable ID plus an external independently recomputed root hash.

Historical evidence:
- `data/authority/receipts/official-2026-08-20.json` and the ignored `official-2026-08-20` roots/lock/revision remain immutable audit history.
- No stale source root, old lock, live endpoint, latest alias, or date fallback may supply the new revision.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build two final candidates from only the verified fresh roots</name>
  <read_first>
    - .planning/phases/01-rules-and-data-authority/01-15-SUMMARY.md
    - scripts/collect-private-authority.ps1
    - src/authority/private-source-set.ts
    - src/commands/import-authority.ts
    - src/commands/validate-authority.ts
    - tests/private-authority/private-source-completeness.test.ts
    - tests/private-authority/private-revision.test.ts
    - data/authority/receipts/official-2026-08-20.json
  </read_first>
  <files>tests/private-authority/private-revision.test.ts</files>
  <behavior>
    - Only the fixed v3 lock is accepted; an old lock/root, wrong reference/method, missing Plan 15 proof, or mixed identity fails before input derivation.
    - Fresh primary and backup root maps remain identical and unchanged through re-verification and input derivation.
    - Independent builds use official-2026-08-27-v3, produce identical canonical file maps, and validate to one independently recomputed bundle root.
    - All historical official-2026-08-20 root/lock/revision/receipt maps remain unchanged.
    - Fetch/http/https/net sentinels throw if any build or validation path attempts network access.
  </behavior>
  <action>Add RED assertions to the opt-in private revision gate, then require the completed Plan 15 summary and fixed v3 lock before opening source bytes. Reject the historical lock/roots and any mixed method/reference/revision identity. Capture bounded sorted relative-path/length/SHA-256 maps for both fresh roots, every historical root and lock, the old selected revision, and the old receipt. Re-run the fresh structural/content proof, install throwing fetch/http/https/net sentinels, and derive complete importer inputs independently from the fresh primary and fresh backup under separate ignored temporary parents. For each tree run exactly `pnpm authority:import -- --input <input-root> --input-lock input-lock.json --expected-input-root-hash <independently-recomputed-input-hash> --output-root <candidate-parent> --revision-id official-2026-08-27-v3`. Independently canonicalize and hash every candidate file, require identical path-length-hash maps and one equal bundle root, then run exactly `pnpm authority:validate -- --root <candidate-revision-root> --bundle bundle.json --id bundle:official-2026-08-27-v3 --hash <independently-recomputed-bundle-root>` for each candidate. Recompute every fresh and historical map afterward and require exact equality. Fail closed on a missing/extra source, content defect, candidate difference, alias/link, stale ID, unexpected network call, mutation, or private diagnostic. Do not fetch, reacquire, retry, repair, mutate source evidence, select/install a revision, or reuse an old source/lock as a fallback.</action>
  <verify>
    <automated>node --test --test-name-pattern="fresh v3 roots|final v3 candidates|historical evidence remains immutable" tests/private-authority/private-source-completeness.test.ts tests/private-authority/private-revision.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The fixed v3 lock and both fresh roots pass the completed Plan 15 contract before build.
    - Fresh-primary and fresh-backup candidates for `bundle:official-2026-08-27-v3` have identical maps and bundle roots.
    - Fresh roots and every historical root/lock/revision/receipt retain exact before/after maps.
    - No network, retry, source mutation, private output, stale fallback, or renewed authorization behavior exists.
  </acceptance_criteria>
  <done>Either fresh Plan 15 root deterministically produces the same valid final candidate while every source and historical artifact remains immutable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Install the write-once v3 revision and select its exact ID and root</name>
  <read_first>
    - data/authority/README.md
    - data/authority/receipts/official-2026-08-20.json
    - tests/private-authority/private-revision.test.ts
    - src/commands/validate-authority.ts
  </read_first>
  <files>data/authority/README.md, data/authority/receipts/official-2026-08-27-v3.json, tests/private-authority/private-revision.test.ts</files>
  <behavior>
    - Exactly one verified candidate is atomically installed at the ignored write-once path `.local/authority/revisions/official-2026-08-27-v3`.
    - Validation and later selection use only `bundle:official-2026-08-27-v3` plus the independently recomputed selected root.
    - The new receipt contains only approved source labels, relative paths, retrieval/effective dates, media types, hashes, fixed authorization evidence, revision ID/root, and private-boundary statement.
    - README selects only the v3 ID/root; all old receipts/revisions remain available and unchanged as historical evidence.
  </behavior>
  <action>After Task 1 proves both candidates equal, atomically install one candidate at the exact ignored v3 revision path. Fail rather than overwrite when a different path already exists; accept an existing directory only when its complete map equals the verified candidate. Independently rehash and validate the installed revision, then create `data/authority/receipts/official-2026-08-27-v3.json` by reusing only the approved non-content receipt shape and substituting values computed from the fixed v3 lock, input evidence, installed file-map hash, ID, and selected bundle root. Update README to select exactly `bundle:official-2026-08-27-v3` and its receipt root while retaining Git-insufficient, private/local/noncommercial, no-redistribution/no-release/no-hosting/no-upload/no-artwork/no-public-API/no-recurring-acquisition, and no-legal-permission statements. Extend tests so a stale old ID/root, mutable latest/date alias, wrong source lock, receipt mismatch, local overwrite, or missing final field fails. Never commit or print source bytes, normalized corpus, absolute roots, lock fields, locators, PDFs/pages, artwork, ignored revision contents, or acquisition diagnostics.</action>
  <verify>
    <automated>node --test --test-name-pattern="official-2026-08-27-v3|safe receipt|write-once selection|receipt root validates" tests/private-authority/private-revision.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The installed ignored revision validates by exact v3 ID and the receipt's independently recomputed root.
    - README selects `bundle:official-2026-08-27-v3` with no mutable/latest/date/network alternative.
    - The receipt matches the installed revision and contains no absolute private path/value, lock field, locator, protected content, or legal-clearance claim.
    - Every historical receipt, lock, root, authorization record, and revision remains unchanged.
  </acceptance_criteria>
  <done>Later phases select the final immutable authority by exact v3 ID/root while historical evidence remains auditable and immutable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Run the complete final private release and leakage gates</name>
  <read_first>
    - package.json
    - scripts/verify-private-authority-boundary.ts
    - tests/private-authority/private-source-completeness.test.ts
    - tests/private-authority/private-revision.test.ts
    - tests/private-authority/repository-boundary.test.ts
    - data/authority/README.md
    - data/authority/receipts/official-2026-08-27-v3.json
  </read_first>
  <files>tests/private-authority/repository-boundary.test.ts</files>
  <behavior>
    - The production boundary scanner covers the new safe receipt/selection and all ignored revisions without allowing protected content or private values into Git/package candidates.
    - Default clean-clone verification, fresh-root completeness, the complete opt-in private suite, and the focused repository-boundary test pass over the same staged tracked set.
    - Network sentinels remain active during fresh-root verification, dual build, validation, installation, selection, and boundary scanning.
    - DATA-01, DATA-02, and DATA-03 are marked complete only after every phase gate passes.
  </behavior>
  <action>Extend the repository-boundary release assertion only as needed to recognize the new safe receipt and fixed v3 selection while retaining Plan 12's single production scanner for reachable history, worktree, index, decoded content, and actual `pnpm pack --dry-run --json` files. Treat any ignored revision file, private locator/value, raw or normalized source derivative, publisher page/PDF, artwork, or community corpus as prohibited. Stage the complete intended tracked set, run `pnpm verify`, the focused fresh v3 completeness test, `pnpm authority:verify-private`, and the narrow repository-boundary test. If tracked content changes after scanning, restage and rerun the complete gate. Mark DATA-01/02/03 complete only after all commands pass and the selected ID/root independently match. Do not force-add private evidence, weaken scanner bounds/exemptions, disclose a match, broaden the private-use policy, or perform another acquisition.</action>
  <verify>
    <automated>pnpm verify &amp;&amp; node --test --test-name-pattern="fresh v3 primary and backup completeness" tests/private-authority/private-source-completeness.test.ts &amp;&amp; pnpm authority:verify-private &amp;&amp; node --test tests/private-authority/repository-boundary.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All four release commands exit 0 after the v3 revision and safe selection are finalized.
    - The scanner covers reachable history, worktree, index, package contents, decoded strings, arbitrary excerpts, semantic derivatives, and locator variants.
    - No private byte, root, locator, lock value, ignored revision file, normalized corpus, artwork, or publisher document enters Git/package output, summary, receipt, or diagnostics.
    - The staged tracked set remains unchanged between its passing boundary scan and atomic task commit.
    - DATA-01/02/03 completion occurs only after the selected v3 ID/root and every release gate pass.
  </acceptance_criteria>
  <done>The final v3 authority is selected and Phase 1 closes only after deterministic, offline, immutability, no-network, and zero-leakage proof.</done>
</task>

</tasks>

<gap_coverage>
- Final-contract key link: the final plan depends on completed Plans 09, 10, 12, and 15 and rebuilds/selects one new immutable revision.
- Fresh evidence requirement: only Plan 15's conforming v3 lock/roots can enter the importer; old nonconforming roots remain historical.
- Release truth: complete default/private/narrow gates bind the selected v3 ID/root before requirements close.
</gap_coverage>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Plan 15 lock -> fresh source roots | The lock could resolve incomplete, substituted, aliased, mutable, or wrong-authorization evidence. |
| fresh roots -> final candidates | Two roots or final schemas could produce divergent results. |
| verified candidate -> selected revision/receipt | A stale ID, wrong root, overwrite, or unsafe receipt could misbind later consumers or leak evidence. |
| selected revision -> Git/package/runtime | Release could expose protected content or reintroduce network/latest selection. |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan |
|-----------|----------|----------|-----------|-------------|-----------------|
| T-01-57 | Spoofing / Tampering | high | fresh lock/roots | mitigate (blocking) | Exact Plan 15 method/reference/revision plus shared content/hash proof and immutable maps. |
| T-01-58 | Tampering / Repudiation | high | dual candidate build | mitigate (blocking) | Fixed v3 ID, independent builds, canonical map equality, external root recomputation, and historical immutability. |
| T-01-59 | Spoofing | high | selection/receipt | mitigate (blocking) | Write-once install and exact ID/root binding; stale/latest/mismatched selections fail. |
| T-01-60 | Information disclosure | high | release surfaces | mitigate (blocking) | Full Plan 12 scanner over history/worktree/index/package with content-free diagnostics before commit. |
| T-01-61 | Elevation / legal boundary | high | consumed authorization | mitigate (blocking) | No acquisition, retry, distribution, hosting, API, artwork, permission inference, or legal-clearance claim. |
| T-01-SC | Tampering | low | package installs | accept | No dependency is added; use existing Node, PowerShell, Zod, and project helpers. |
</threat_model>

<verification>
Run the focused fresh-root proof, dual-candidate/immutability tests, exact importer/validator commands, default verification, complete private gate, and narrow repository-boundary gate. Confirm the v3 ID/root matches both candidates and the safe receipt, all fresh/historical evidence maps are unchanged, no network path runs, and no private value appears in tracked/package output or diagnostics.
</verification>

<success_criteria>
- Only the fresh Plan 15 evidence supplies final build inputs.
- Either fresh root produces the same immutable `bundle:official-2026-08-27-v3` bytes and root.
- The v3 revision is selected by exact ID/root with a safe receipt; all historical evidence remains unchanged.
- Default, private, and narrow release gates pass with zero network access or private leakage.
</success_criteria>

<output>
Create `.planning/phases/01-rules-and-data-authority/01-13-SUMMARY.md` when done. Commit only tracked implementation/test/receipt/README changes after the final staged-set boundary gate passes; never add `.local/authority/`.
</output>
