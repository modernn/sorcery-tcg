---
phase: 01-rules-and-data-authority
plan: "10"
type: execute
wave: 8
depends_on: ["01-09"]
files_modified:
  - src/authority/schemas.ts
  - src/authority/validate-bundle.ts
  - src/commands/validate-authority.ts
  - tests/authority/bundle.test.ts
  - tests/authority/provenance.test.ts
autonomous: true
requirements: [DATA-01, DATA-03]
gap_closure: true
must_haves:
  truths:
    - "Precedence resolves only a uniquely justified official winner; partial, competing, cyclic, broken, or otherwise unresolved supersession fails closed."
    - "The exported resolver rejects impossible calendar dates, including invalid leap days, before comparing effective dates."
    - "A SHA-256 durable locator is accepted only when its digest equals the source byte hash, while HTTPS/procedure records are reported as declarations rather than reverified bytes."
    - "Source derivation parent hashes form a deterministic acyclic graph with no duplicate edge."
  artifacts:
    - path: "src/authority/validate-bundle.ts"
      provides: "Graph-based supersession and derivation validation plus honest evidence states"
      exports: ["resolveAuthorityPrecedence", "validateAuthorityBundle"]
    - path: "src/authority/schemas.ts"
      provides: "SHA locator-to-byteHash semantic binding"
    - path: "src/commands/validate-authority.ts"
      provides: "Machine-readable evidence that distinguishes local rehash, SHA binding, and declaration binding"
    - path: "tests/authority/bundle.test.ts"
      provides: "Supersession/date/command evidence regressions"
    - path: "tests/authority/provenance.test.ts"
      provides: "Locator, duplicate derivation edge, and source-cycle regressions"
  key_links:
    - from: "src/authority/schemas.ts"
      to: "SourceRecord.byteHash"
      via: "urn:sha256 locator super-refinement"
      pattern: "durableLocator"
    - from: "src/authority/validate-bundle.ts"
      to: "src/commands/validate-authority.ts"
      via: "separate validated evidence arrays become explicit command evidence labels"
      pattern: "manifest.*Bound"
    - from: "src/authority/validate-bundle.ts"
      to: "SourceRecord.derivation.parentByteHashes"
      via: "bounded DFS over byte-hash nodes"
      pattern: "parentByteHashes"
---

<objective>
Close CR-04, the confirmed part of CR-05, CR-09, and WR-02 at the single reusable offline authority boundary.

Purpose: DATA-01/03 cannot be trusted when a partial superseder becomes a global winner, impossible dates resolve, SHA locators contradict byte hashes, declarations are overstated, or circular derivation passes.
Output: Fail-closed precedence/date logic, semantic manifest evidence, acyclic source derivation, command-output updates, and adversarial regressions.
</objective>

<execution_context>
@C:/Users/Dad/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Dad/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-rules-and-data-authority/01-CONTEXT.md
@.planning/phases/01-rules-and-data-authority/01-RESEARCH.md
@.planning/phases/01-rules-and-data-authority/01-PATTERNS.md
@.planning/phases/01-rules-and-data-authority/01-VERIFICATION.md
@.planning/phases/01-rules-and-data-authority/01-REVIEW.md
@.planning/phases/01-rules-and-data-authority/01-04-SUMMARY.md
@.planning/phases/01-rules-and-data-authority/01-05-SUMMARY.md
@.planning/phases/01-rules-and-data-authority/01-09-SUMMARY.md

<interfaces>
From src/authority/validate-bundle.ts:
- `resolveAuthorityPrecedence(records: readonly AuthorityPrecedenceRecord[], scope: string | null, effectiveAt: string): ResolutionResult`
- Unsupported reasons already include `unclear-date`, `broken-supersession`, `ambiguous-supersession`, and `equal-rank`.
- `validateAuthorityBundle(root, bundlePath, expected): Promise&lt;ValidatedAuthorityBundle&gt;`
- Existing result fields are `storedBytesRehashed` and the overbroad `manifestBindingsVerified`.

From src/authority/schemas.ts:
- A manifest-only source has `durableLocator: https URL | urn:sha256:* | null`, `acquisitionProcedureHash: Hash | null`, and `byteHash: Hash`.

From src/commands/validate-authority.ts:
- `runValidateAuthorityCommand(argv, io): Promise&lt;number&gt;`
- Current evidence label `manifest-binding-verified` does not distinguish SHA byte binding from an offline declaration.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Resolve supersession as a graph and validate real dates</name>
  <read_first>
    - .planning/phases/01-rules-and-data-authority/01-CONTEXT.md
    - .planning/phases/01-rules-and-data-authority/01-VERIFICATION.md
    - docs/authority-precedence.md
    - src/authority/validate-bundle.ts
    - tests/authority/bundle.test.ts
  </read_first>
  <files>src/authority/validate-bundle.ts, tests/authority/bundle.test.ts</files>
  <behavior>
    - A supersedes B plus unrelated viable C never gives A a global supersession rank; unresolved A versus C is unsupported.
    - Transitive A->B->C leaves A viable, competing superseders remain contenders, and a supersession cycle is unsupported.
    - Broken edges remain unsupported and all winning/contending/superseded/provenance refs remain sorted and complete.
    - Invalid month/day and non-leap February 29 return `unclear-date`; a valid leap day participates normally.
  </behavior>
  <action>Add RED probes for the exact verification failures plus competing, transitive, cyclic, and leap-date cases. Replace the `supersedes.length` rank boost with a bounded directed graph over applicable official source IDs: validate every edge, detect cycles, eliminate only explicitly superseded targets, and apply the documented scope/kind/effective-date ranking only among remaining viable peers. Resolve only when that process yields one unique contender; otherwise preserve evidence and return the existing deterministic unsupported reason. Add a small strict UTC round-trip date check before lexical comparisons in the exported resolver so direct callers receive the same semantic date contract as schema-validated bundles. Keep D-01/D-02 official-only behavior and never use array order or community provenance as a tiebreaker.</action>
  <verify>
    <automated>node --test --test-name-pattern="partial supersession|competing superseders|transitive supersession|supersession cycle|impossible|leap" tests/authority/bundle.test.ts &amp;&amp; pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Partial supersession does not outrank an unrelated viable official record.
    - Transitive, competing, broken, and cyclic graphs return the expected deterministic resolved/unsupported evidence.
    - Impossible dates cannot select a winner through the exported resolver; valid leap days remain accepted.
  </acceptance_criteria>
  <done>D-01/D-02 precedence is graph-correct, semantically dated, evidence-retaining, and fail-closed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bind SHA locators and report honest manifest evidence</name>
  <read_first>
    - .planning/phases/01-rules-and-data-authority/01-VERIFICATION.md
    - src/authority/schemas.ts
    - src/authority/validate-bundle.ts
    - src/commands/validate-authority.ts
    - tests/authority/provenance.test.ts
    - tests/authority/bundle.test.ts
  </read_first>
  <files>src/authority/schemas.ts, src/authority/validate-bundle.ts, src/commands/validate-authority.ts, tests/authority/provenance.test.ts, tests/authority/bundle.test.ts</files>
  <behavior>
    - `urn:sha256:<digest>` must equal the source `byteHash` digest or fail at `/durableLocator` even after all outer hashes are recomputed.
    - A matching SHA locator reports `manifest-byte-binding-verified` without claiming local bytes were rehashed.
    - HTTPS locators and acquisition-procedure hashes report `manifest-declaration-bound`, never byte verification.
    - Stored bytes retain the separate `stored-bytes-rehashed` evidence state.
  </behavior>
  <action>Write recomputed-artifact negative tests first. Add a `sourceRecordSchema` super-refinement that compares a SHA-256 URN locator digest to `byteHash` and emits a stable `locator_byte_hash_mismatch` diagnostic at `/durableLocator`. Replace the single `manifestBindingsVerified` result with two explicit readonly arrays named `manifestByteBindingsVerified` and `manifestDeclarationsBound`; classify only a matching SHA URN in the former and HTTPS/procedure-only records in the latter. Update `runValidateAuthorityCommand` to emit the exact labels `manifest-byte-binding-verified` and `manifest-declaration-bound`, preserving deterministic order and `stored-bytes-rehashed`. Do not dereference HTTPS, claim a procedure was executed, fetch remote bytes, or reject an honest declaration merely because it cannot be revalidated offline per D-03/D-04/D-06.</action>
  <verify>
    <automated>node --test --test-name-pattern="locator|manifest.*declaration|manifest.*byte|validation command" tests/authority/provenance.test.ts tests/authority/bundle.test.ts &amp;&amp; pnpm typecheck &amp;&amp; pnpm lint</automated>
  </verify>
  <acceptance_criteria>
    - A mismatched SHA locator fails after recomputing enclosing canonical hashes.
    - Matching SHA, HTTPS/procedure declaration, and stored-byte evidence appear under three truthful, mutually exclusive command labels.
    - No validation or command output says remote bytes or procedures were verified when only their declaration is bound.
  </acceptance_criteria>
  <done>Manifest evidence is semantically bound and reported no more strongly than the bytes actually available offline.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Reject duplicate and cyclic source derivation edges</name>
  <read_first>
    - .planning/phases/01-rules-and-data-authority/01-CONTEXT.md
    - src/authority/validate-bundle.ts
    - tests/authority/provenance.test.ts
    - tests/authority/fixtures/provenance-cycle.json
  </read_first>
  <files>src/authority/validate-bundle.ts, tests/authority/provenance.test.ts</files>
  <behavior>
    - Repeating one parent byte hash in a source derivation fails at the duplicate edge.
    - Two-node and longer derivation cycles fail with deterministic `derivation_cycle` paths.
    - A rooted acyclic multi-source derivation remains valid; missing, self, and verbatim-parent failures retain existing diagnostics.
  </behavior>
  <action>Add RED in-memory bundles whose canonical hashes are fully recomputed for duplicate-parent, two-node cycle, longer cycle, and valid rooted DAG cases. Extend `validateGraph` with one bounded DFS over source byte-hash derivation edges, reusing the artifact graph's state/depth discipline and diagnostic cap. Detect duplicate hashes within each `parentByteHashes` array before traversal, preserve the existing missing/self/verbatim checks, and sort every diagnostic. Do not conflate artifact parentRefs with source byte derivation, accept a cycle because each individual parent exists, or add a second public validator.</action>
  <verify>
    <automated>node --test --test-name-pattern="derivation" tests/authority/provenance.test.ts &amp;&amp; pnpm verify</automated>
  </verify>
  <acceptance_criteria>
    - Duplicate derivation edges and every tested cycle length fail before a bundle is returned.
    - Diagnostics identify deterministic parentByteHashes paths and remain capped/sorted.
    - Valid acyclic provenance and all existing artifact-reference tests continue to pass.
  </acceptance_criteria>
  <done>D-04/D-06 provenance is rooted, acyclic, duplicate-free, and tamper-evident.</done>
</task>

</tasks>

<gap_coverage>
- CR-04: closed by Task 1; explicit supersession is an edge, not a global rank boost.
- CR-05 (adjudicated confirmed portion): closed by Task 2; SHA locators bind to byteHash and non-dereferenceable records remain honest declarations.
- CR-09: closed by Task 3; source byte derivation receives duplicate-edge and cycle validation.
- WR-02: closed by Task 1 at the exported resolver boundary, not only through bundle schemas.
</gap_coverage>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| precedence records -> normative result | Malformed or partial relationships can silently choose the wrong official authority. |
| manifest declaration -> evidence output | A locator/procedure declaration can be overstated as verified bytes. |
| source derivation graph -> validated bundle | Existing but cyclic parent hashes can counterfeit rooted provenance. |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan |
|-----------|----------|----------|-----------|-------------|-----------------|
| T-01-45 | Spoofing / Tampering | high | precedence resolver | mitigate (blocking) | Graph-valid supersession, semantic dates, unique-winner rule, and adversarial tests block authority selection. |
| T-01-46 | Spoofing / Repudiation | high | manifest evidence | mitigate (blocking) | SHA digest equality plus explicit byte-binding/declaration labels; remote bytes are never claimed as rehashed. |
| T-01-47 | Tampering | high | derivation graph | mitigate (blocking) | Duplicate-edge rejection and bounded DFS cycle detection over recomputed bundles. |
| T-01-48 | Denial of service | medium | graph traversal | mitigate | Reuse fixed reference/depth/diagnostic limits and deterministic traversal. |
| T-01-SC | Tampering | low | package installs | accept | No package is added; Node, existing Zod, and current graph helpers are sufficient. |
</threat_model>

<verification>
Run `pnpm verify`. Exercise direct resolver probes for partial supersession and impossible dates; schema/command probes for matching and mismatched SHA URNs plus declaration-only evidence; and recomputed two-node/long derivation cycles. Confirm all fail or resolve exactly as documented without network access.
</verification>

<success_criteria>
- Official precedence selects only one uniquely justified winner and otherwise returns unsupported with complete evidence.
- Impossible resolution dates fail at the public function boundary.
- SHA locators cannot contradict byte hashes, and evidence output distinguishes rehashed bytes, byte bindings, and declarations.
- Source derivation is duplicate-free, bounded, acyclic, and covered by recomputed-hash regressions.
</success_criteria>

<output>
Create `.planning/phases/01-rules-and-data-authority/01-10-SUMMARY.md` when done.
</output>
