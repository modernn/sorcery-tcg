---
phase: 01-rules-and-data-authority
plan: "07"
type: execute
wave: 5
depends_on: [01-06]
files_modified: []
autonomous: false
requirements: [DATA-01, DATA-02, DATA-03]
user_setup:
  - service: official Sorcery card API via the user's browser
    why: "Project code is prohibited from fetching or automating acquisition; only the user can manually save the one private official response."
    dashboard_config:
      - task: "Save https://api.sorcerytcg.com/api/cards as .local/authority/inputs/official-2026-08-20/cards.raw.json without editing it."
        location: "User-controlled browser and local filesystem"
      - task: "Copy those exact bytes to a durable private backup outside Git and provide its absolute locator as AUTHORITY_BACKUP_FILE."
        location: "User-selected private backup storage"
must_haves:
  truths:
    - "The user manually supplies one complete official API response; project code performs no network acquisition per amended D-08."
    - "The fixed primary file and a user-selected durable private backup are ordinary files with identical byte length and SHA-256."
    - "Both copies remain private, noncommercial, unredistributed, outside Git, and contain no requested card art or publisher PDF."
    - "The checkpoint records an operating decision and provenance evidence, not legal certainty or publisher permission."
    - "Plan 08 remains unreachable and no 01-07 summary exists when any file, hash, backup locator, JSON parse, ignore check, or attestation is missing."
  artifacts:
    - path: ".local/authority/inputs/official-2026-08-20/cards.raw.json"
      provides: "User-supplied exact official API response; private and Git-ignored, never a committed plan artifact"
    - path: ".planning/phases/01-rules-and-data-authority/01-07-SUMMARY.md"
      provides: "Non-content provenance receipt and operating attestation created only after every blocking check succeeds"
  key_links:
    - from: "https://api.sorcerytcg.com/api/cards"
      to: ".local/authority/inputs/official-2026-08-20/cards.raw.json"
      via: "one manual browser save performed by the user"
      pattern: "cards.raw.json"
    - from: ".local/authority/inputs/official-2026-08-20/cards.raw.json"
      to: "AUTHORITY_BACKUP_FILE"
      via: "independent byte-length and SHA-256 equality check"
      pattern: "sha256"
    - from: ".planning/phases/01-rules-and-data-authority/01-07-SUMMARY.md"
      to: ".planning/phases/01-rules-and-data-authority/01-08-PLAN.md"
      via: "exact source URL/date/path/backup locator/byte length/hash/attestation dependency"
      pattern: "private.*noncommercial.*no redistribution"
---

<objective>
Obtain the unavoidable human-supplied official card response and bind it to a durable private backup without adding any acquisition code or corpus bytes to Git.

Purpose: Release the final local build only when the user's exact manual input, backup, provenance, and private-use operating attestation are complete.
Output: A non-content Plan 07 summary containing exact paths, byte length, SHA-256, source date/URL, and attestation; otherwise a hard stop with no summary and no Plan 08 execution.
</objective>

<execution_context>
@C:/Users/Dad/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Dad/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-rules-and-data-authority/01-CONTEXT.md
@.planning/phases/01-rules-and-data-authority/01-DISCUSSION-LOG.md
@.planning/phases/01-rules-and-data-authority/01-RESEARCH.md
@.planning/phases/01-rules-and-data-authority/01-VALIDATION.md
@.planning/phases/01-rules-and-data-authority/01-06-SUMMARY.md
@.gitignore
@docs/external-reuse-policy.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Supply and attest the exact private official response</name>
  <read_first>
    - .planning/phases/01-rules-and-data-authority/01-CONTEXT.md
    - .planning/phases/01-rules-and-data-authority/01-RESEARCH.md
    - .planning/phases/01-rules-and-data-authority/01-06-SUMMARY.md
    - .gitignore
    - docs/external-reuse-policy.md
  </read_first>
  <files>.local/authority/inputs/official-2026-08-20/cards.raw.json (human-created, Git-ignored), user-selected AUTHORITY_BACKUP_FILE (human-created, outside Git), .planning/phases/01-rules-and-data-authority/01-07-SUMMARY.md (executor-created only after success)</files>
  <action>Before pausing, run `git check-ignore -q .local/authority/inputs/official-2026-08-20/cards.raw.json`, prove `git ls-files --error-unmatch` fails for that path, and prove the project has no card-data network acquisition command/module. Then ask the user to open `https://api.sorcerytcg.com/api/cards` in their browser and save the complete response without editing or reformatting it to exactly `.local/authority/inputs/official-2026-08-20/cards.raw.json`. Ask the user to make one byte-identical durable private backup outside the repository and provide its absolute ordinary-file locator as `AUTHORITY_BACKUP_FILE`; a mutable URL, a hash without retrievable bytes, or another path beneath the repository is not sufficient. Require the user to attest exactly that this tool and both copies are private and noncommercial, will not be redistributed or released, the response was saved manually through the browser, project automation did not acquire it, and no card art or publisher PDF was included. Explain that this operating choice does not resolve the conflict between the official API page and Terms, establish legal permission, or authorize later sharing, release with content, automation, or commercialization. After the user responds, run the automated verification below using the supplied backup locator. Independently `lstat` both paths, reject symlinks/non-files, reject a backup inside the repository, bound each file to the importer limits, compare byte lengths and SHA-256 values, parse the primary as JSON, prove the primary remains ignored/untracked/unstaged, and prove the backup is outside Git. Only after every check and the complete attestation succeeds, create `01-07-SUMMARY.md` with source URL `https://api.sorcerytcg.com/api/cards`, UTC retrieval timestamp supplied by the user, fixed primary path, absolute durable backup locator, byte length, lowercase SHA-256, exact attestation text, and the statement that this is provenance/operating evidence rather than a legal conclusion. If any input, backup, hash equality, JSON parse, ignore/tracking check, timestamp, or attestation is absent, contradictory, or fails, stop: do not create `01-07-SUMMARY.md`, do not execute Plan 08, and do not mark Phase 1 or DATA-01/02/03 complete.</action>
  <verify>
    <automated>node -e "const fs=require('node:fs'),p=require('node:path'),c=require('node:crypto'),cp=require('node:child_process');const root=fs.realpathSync('.'),a=p.resolve('.local/authority/inputs/official-2026-08-20/cards.raw.json'),b=p.resolve(process.env.AUTHORITY_BACKUP_FILE||'');if(!process.env.AUTHORITY_BACKUP_FILE||b.startsWith(root+p.sep))process.exit(1);for(const x of [a,b]){const s=fs.lstatSync(x);if(!s.isFile()||s.isSymbolicLink()||s.size===0||s.size&gt;268435456)process.exit(1)}const A=fs.readFileSync(a),B=fs.readFileSync(b),h=x=&gt;c.createHash('sha256').update(x).digest('hex');if(A.length!==B.length||h(A)!==h(B))process.exit(1);JSON.parse(A.toString('utf8'));cp.execFileSync('git',['check-ignore','-q',a],{cwd:root});const tracked=cp.spawnSync('git',['ls-files','--error-unmatch','--',a],{cwd:root});if(tracked.status===0)process.exit(1);const staged=cp.execFileSync('git',['diff','--cached','--name-only'],{cwd:root,encoding:'utf8'});if(staged.split(/\r?\n/).some(x=&gt;x&amp;&amp;p.resolve(root,x)===a))process.exit(1);console.log(JSON.stringify({byteLength:A.length,sha256:h(A),primary:a,backup:b}))"</automated>
  </verify>
  <acceptance_criteria>
    - The fixed primary path contains a non-empty, valid JSON ordinary file of at most 256 MiB and is neither a symlink nor tracked/staged by Git.
    - `AUTHORITY_BACKUP_FILE` resolves to a non-symlink ordinary file outside the repository with byte length and lowercase SHA-256 exactly equal to the primary.
    - The user supplies a UTC retrieval timestamp and explicitly confirms: private use, noncommercial use, no redistribution/release, manual browser acquisition, no project automation, and no card art or publisher PDF.
    - `01-07-SUMMARY.md` contains only source URL/date, paths/locator, byte length, SHA-256, attestation, and the no-legal-conclusion caveat; it contains no card record or corpus excerpt.
    - If any criterion fails, `01-07-SUMMARY.md` and `01-08-SUMMARY.md` do not exist and Plan 08 is not started.
  </acceptance_criteria>
  <resume-signal>Reply with `AUTHORITY_BACKUP_FILE=&lt;absolute path&gt;`, the UTC retrieval timestamp, and: `I attest this tool and both copies are private and noncommercial; I will not redistribute or release them; I saved the complete response manually through my browser without project automation; it contains no card art or publisher PDF. I understand this does not establish legal permission.`</resume-signal>
  <done>The exact manual response and durable private backup have matching independently computed hashes, remain outside Git, and the complete scoped attestation is recorded without a legal claim.</done>
</task>

</tasks>

<verification>
Do not rely on the user's claimed hash: run the exact independent hash/JSON/ordinary-file/containment/Git checks after the response. Re-read the created summary and require every receipt/attestation field before releasing dependency 01-08.
</verification>

<success_criteria>
- One exact full official API response exists at the fixed Git-ignored path and one byte-identical durable private backup exists outside the repository.
- Source URL, retrieval timestamp, primary/backup locators, byte length, SHA-256, and complete private-use attestation are recorded without corpus content.
- No project code fetched, scraped, polled, or automated acquisition; no official content entered Git or a package.
- Missing or contradictory evidence produces a hard stop with no summary, no Phase 1 completion, and no Plan 08 execution.
</success_criteria>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser-saved JSON -> private local input | The response is untrusted, mutable, and may be malformed or oversized. |
| primary private file -> durable private backup | A wrong locator, symlink, or changed backup could defeat reproducibility. |
| private files -> Git boundary | Corpus bytes could be staged or committed accidentally. |
| user attestation -> execution dependency | Ambiguous scope could be mistaken for legal permission or broader authorization. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-31 | Tampering / DoS | malicious JSON | mitigate | Bound both inputs to 256 MiB, require non-empty valid JSON, and defer schema/record/depth bounds to the already-tested importer before any revision build. |
| T-01-32 | Tampering / Elevation | path traversal/symlink escape | mitigate | Fix the primary path, resolve both locators, require ordinary non-symlink files, and require the backup outside the repository. |
| T-01-33 | Spoofing | source substitution | mitigate | Record the exact official URL/date and independently compare byte length plus SHA-256 across primary and backup; state that hashes do not prove authenticity. |
| T-01-34 | Tampering / Repudiation | receipt and artifact/reference seed | mitigate | Executor computes the receipt values, records both locators and exact attestation, and refuses a summary on any missing/mismatched evidence. |
| T-01-35 | Information disclosure / legal | restricted asset publication | mitigate | Precheck and postcheck ignore/tracked/staged state, keep both files private, exclude art/PDF, and treat later distribution/automation/commercial use as a separate permission-gated plan. |
| T-01-SC | Tampering | package installs | accept | The checkpoint installs no package and adds no acquisition dependency. |
</threat_model>

<must_haves>
- DATA-01 receives exact official bytes only through the amended D-08 manual browser path and no network-capable project code.
- DATA-02 remains reproducible from the fixed primary or the selected durable private backup with the recorded exact hash; Git alone is explicitly insufficient.
- DATA-03 receives source URL/date, locators, byte length, SHA-256, and the D-11 private/noncommercial/no-redistribution attestation.
- D-08/D-11 stop execution without a complete file, backup, independent hash equality, and attestation; no checkpoint output claims legal certainty.
</must_haves>

<output>
Create `.planning/phases/01-rules-and-data-authority/01-07-SUMMARY.md` only after every automated and human acceptance criterion passes. Otherwise create neither `01-07-SUMMARY.md` nor `01-08-SUMMARY.md`, do not execute Plan 08, and do not mark Phase 1 or DATA-01/02/03 complete.
</output>
