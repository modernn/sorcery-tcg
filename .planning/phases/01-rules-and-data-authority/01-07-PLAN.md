---
phase: 01-rules-and-data-authority
plan: "07"
type: execute
wave: 5
depends_on: [01-06]
files_modified: []
autonomous: false
requirements: [DATA-01, DATA-02, DATA-03]
must_haves:
  truths:
    - "No official API retrieval, normalized derivative commit, PDF/image/raw-corpus publication, or restricted external reuse proceeds without recorded written scope."
    - "A human reviewer attests that Phase 1 code, tests, fixtures, and data were independently implemented and contain no copied GPL or unlicensed source material."
    - "The reviewer approves exactly one durable exact-byte rebuild path: permitted fixed repository bytes, an immutable archive/custodian, or a mismatch-refusing approved acquisition procedure."
    - "If permission, clean-room attestation, or a durable exact-byte path is absent, execution stops without weakening DATA-01/02/03 or publishing a partial official revision."
  artifacts:
    - path: "docs/external-reuse-policy.md"
      provides: "Exact permission and clean-room fields the human must approve before Plan 08"
  key_links:
    - from: "human permission response"
      to: "01-08-PLAN.md"
      via: "blocking dependency; Plan 08 records permission plus the approved durable input lock before publication"
      pattern: "reviewer|allowed sources|redistribution|images|attribution|revocation|durable input|input root hash"
---

<objective>
Obtain the non-automatable publisher-permission and clean-room approvals required before an official authority revision is published.

Purpose: Preserve the D-08/D-09 legal and provenance boundary without blocking synthetic pipeline development or silently treating public URLs as permission.
Output: A human approval response containing exact non-confidential permission, clean-room, and durable input-lock metadata for Plan 08; otherwise an intentional hard stop with no repository mutation or phase-completion summary.
</objective>

<execution_context>
@C:/Users/Dad/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Dad/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-rules-and-data-authority/01-CONTEXT.md
@.planning/phases/01-rules-and-data-authority/01-RESEARCH.md
@.planning/phases/01-rules-and-data-authority/01-VALIDATION.md
@.planning/phases/01-rules-and-data-authority/01-06-SUMMARY.md
@docs/external-reuse-policy.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 1: Confirm publisher scope and clean-room review</name>
  <read_first>
    - .planning/phases/01-rules-and-data-authority/01-CONTEXT.md
    - .planning/phases/01-rules-and-data-authority/01-RESEARCH.md
    - .planning/phases/01-rules-and-data-authority/01-VALIDATION.md
    - docs/external-reuse-policy.md
    - docs/authority-precedence.md
  </read_first>
  <files>None — this checkpoint must not publish or edit authority artifacts.</files>
  <action>First run the automated pre-check to prove the repository still contains no official revision or retrieval bypass. Then pause. Ask the user to supply a non-confidential summary of written publisher permission covering: reviewer/permission issuer, date, allowed official sources/endpoints, whether automated retrieval is allowed, whether raw caching is allowed, whether normalized-data redistribution is allowed, whether images/PDFs are allowed, required attribution, revocation/update expectations, and any scope limits. Separately ask the user/reviewer to attest that the implementation, tests, fixtures, and policies were reviewed against the recorded Contested Realms and spells.bar revisions and contain no copied code, tests, assets, card implementations, or data. Finally require the reviewer to approve exactly one durable DATA-02 rebuild path and its canonical input lock: (A) `repository-stored` only when permission explicitly allows the fixed `data/authority/official-2026-08-20/raw/cards.raw.json` bytes; (B) `immutable-custodian` with a non-secret immutable locator, named archive/custodian and revision, exact retrieval method, per-file relative paths/byte counts/SHA-256 values, and canonical input-root SHA-256; or (C) `approved-acquisition-procedure` with exact authorized endpoint/manual steps, required credentials kept outside git, per-file relative paths/byte counts/SHA-256 values, canonical input-root SHA-256, and an instruction to refuse any mismatched response. A mutable local directory alone is not durable evidence. Do not request or commit confidential correspondence. Do not accept silence, repository publicity, a URL, or a hash as permission. If permission, clean-room attestation, or the durable path/input-lock fields are unavailable, contradictory, narrower than Plan 08, or cannot reproduce the exact bytes, halt execution: keep `permission-required` and Phase 1/DATA-01/02/03 pending, do not run Plan 08, do not publish a reduced official bundle, and create neither `01-07-SUMMARY.md` nor `01-08-SUMMARY.md`.</action>
  <verify>
    <automated>node -e "const fs=require('node:fs');const s=fs.readFileSync('docs/external-reuse-policy.md','utf8').toLowerCase();if(!s.includes('permission-required'))process.exit(1);if(fs.existsSync('data/authority/official-2026-08-20'))process.exit(1)" &amp;&amp; pnpm verify</automated>
    <human-check>The response names the reviewer/issuer/date; explicitly answers automated retrieval, raw caching, normalized redistribution, images/PDFs, attribution, revocation/update, scope limits, and clean-room attestation; selects exactly one durable input kind; and supplies its exact relative-file hashes plus canonical input-root hash and locator/procedure.</human-check>
  </verify>
  <acceptance_criteria>
    - The automated pre-check proves no official revision was published before approval and the complete offline suite remains green.
    - Approval is explicit and written for every artifact Plan 08 will commit; absent/narrow approval blocks rather than silently narrowing a locked decision.
    - Clean-room review covers code, tests, fixtures, assets, card implementations, and data against both audited external revisions.
    - One durable input kind is named with enough non-confidential locator/procedure, exact file-list, and SHA-256 metadata for Plan 08 to rebuild from clean local materialization and refuse any byte mismatch.
  </acceptance_criteria>
  <resume-signal>Reply `approved` with the permission fields, clean-room attestation, and exactly one complete durable input-lock option; otherwise reply `not approved` to halt Phase 1 without summaries or publication.</resume-signal>
  <done>Plan 08 may execute only after complete written scope, clean-room attestation, and a durable exact-byte input lock are supplied; otherwise Phase 1 and DATA-01/02/03 remain pending with no Plan 07/08 summary or prohibited artifact.</done>
</task>

</tasks>

<must_haves>
- DATA-01/02 publication authority and the durable exact-byte rebuild path are resolved by a human because public terms do not grant automated retrieval or normalized redistribution.
- DATA-03 provenance includes independent-implementation review rather than copied GPL/unlicensed code, tests, assets, or data.
- D-08/D-09 remain non-negotiable; failure to approve stops execution instead of reducing the promised authority bundle.
</must_haves>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| publisher/legal permission -> repository publication | Only the rights holder/reviewer can authorize retrieval, storage, and redistribution scope. |
| human clean-room review -> provenance claim | Manual comparison/attestation is required to detect copied external material. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-31 | Tampering / DoS | malicious JSON | accept | No JSON is ingested in this checkpoint; all prior bounded parser tests must remain green. |
| T-01-32 | Tampering | path traversal/symlink escape | accept | No filesystem input is accepted or published; the automated pre-check only reads fixed repository paths. |
| T-01-33 | Spoofing | permission/source authority | transfer | Require written publisher scope, named reviewer/issuer/date, and one approved durable input source; neither the agent nor hashes can manufacture authenticity or rights. |
| T-01-34 | Tampering / Repudiation | approval/input-lock record | mitigate | Require explicit answers for every permission category, a named clean-room attestation, exact per-file hashes, canonical input-root hash, and immutable locator/procedure before dependency release. |
| T-01-35 | Information disclosure / legal | restricted artifact publication | mitigate | Verify the official revision is absent, keep confidential correspondence outside git, and hard-stop on absent/narrow permission. |
| T-01-SC | Tampering | package installs | accept | No install occurs; locked full verification is rerun before the checkpoint. |
</threat_model>

<verification>
Run the automated pre-check and full suite, then inspect the human response against every permission, clean-room, and durable-input field in `docs/external-reuse-policy.md`. Do not mark this plan complete unless the response authorizes every exact Plan 08 artifact, confirms clean-room review, and supplies one exact-byte rebuild lock. A failed gate produces no summary.
</verification>

<success_criteria>
- No official data or restricted asset exists before the checkpoint.
- A named reviewer supplies complete written scope, independent-implementation attestation, and one durable exact-byte input lock, or the phase stops safely with DATA-01/02/03 pending.
- The checkpoint itself edits nothing and cannot be auto-approved by workflow settings.
</success_criteria>

<output>
Create `.planning/phases/01-rules-and-data-authority/01-07-SUMMARY.md` only after explicit permission, clean-room, and durable-input approval. Otherwise report the blocking gap, create neither `01-07-SUMMARY.md` nor `01-08-SUMMARY.md`, do not execute Plan 08, and do not mark Phase 1 or DATA-01/02/03 complete.
</output>
