---
phase: 01-rules-and-data-authority
plan: "07"
type: execute
wave: 5
depends_on: [01-06]
files_modified:
  - src/authority/private-source-set.ts
  - tests/authority/private-source-set.test.ts
autonomous: false
requirements: [DATA-01, DATA-02, DATA-03]
user_setup:
  - service: official Sorcery authority sources via the user's browser
    why: "Project code is prohibited from fetching or automating acquisition; only the user can manually save the complete private official source set."
    dashboard_config:
      - task: "Save all seven required official source files beneath .local/authority/inputs/official-2026-08-20/primary/ at their exact locked relative paths without editing or reformatting them."
        location: "User-controlled browser and private local filesystem"
      - task: "Copy those exact files to an independent durable private backup root outside the repository and provide that absolute root locator."
        location: "User-selected private backup storage"
must_haves:
  truths:
    - "The manual D-08 input is a complete seven-file official source set: current rulebook, base Constructed format, Codex, FAQs, Codex changelog, official card-update notice, and full card API JSON."
    - "Every source entry records exact relative path, official HTTPS URL, retrieval timestamp, effective date when available, media type, byte length, and SHA-256."
    - "A reusable TypeScript verifier proves primary and backup roots are independent and every corresponding file is an ordinary non-symlink/non-junction, non-hardlinked, byte-identical file."
    - "Project code performs no network acquisition; all primary/backup bytes remain private, Git-ignored, untracked, unstaged, and unpackaged."
    - "The human checkpoint records private/noncommercial/no-redistribution scope and provenance evidence without claiming publisher permission or legal certainty."
    - "Plan 08 remains unreachable and no 01-07 summary exists when any source, metadata field, independent backup, hash/equality check, or attestation is missing."
  artifacts:
    - path: "src/authority/private-source-set.ts"
      provides: "Reusable bounded source-set metadata, path containment, independent-root, file-identity, and byte/hash equality verifier"
      exports: ["PRIVATE_AUTHORITY_SOURCE_PATHS", "verifyPrivateSourceSet"]
    - path: "tests/authority/private-source-set.test.ts"
      provides: "Cross-platform source-set independence, traversal, symlink/junction, hardlink, metadata, and equality tests"
    - path: ".local/authority/locks/official-2026-08-20/source-set-lock.json"
      provides: "Private exact source-set metadata and independently computed root hash; Git-ignored and never committed"
    - path: ".planning/phases/01-rules-and-data-authority/01-07-SUMMARY.md"
      provides: "Safe non-content provenance receipt and operating attestation created only after every blocking check succeeds"
  key_links:
    - from: "src/authority/private-source-set.ts"
      to: ".local/authority/inputs/official-2026-08-20/primary/"
      via: "realpath/path.relative containment plus lstat/stat identity and streaming SHA-256 verification"
      pattern: "verifyPrivateSourceSet"
    - from: ".local/authority/inputs/official-2026-08-20/primary/"
      to: "the user-selected backup root"
      via: "exact relative-path set, distinct dev/ino identities, byte length, and SHA-256 equality"
      pattern: "sourceSetRootHash"
    - from: ".local/authority/locks/official-2026-08-20/source-set-lock.json"
      to: ".planning/phases/01-rules-and-data-authority/01-08-PLAN.md"
      via: "complete seven-source evidence and independently recomputed source-set root hash"
      pattern: "rulebook/rulebook-current.pdf"
---

<objective>
Create the reusable private source-set safety verifier, then obtain and bind the complete authoritative input set to an independent durable backup without adding acquisition code or official bytes to Git.

Purpose: Release the final local build only when every DATA-01 authority source and the full DATA-02 card input have exact primary/backup bytes, complete provenance metadata, and a private-use operating attestation.
Output: A tested generic TypeScript verifier plus a private source-set lock and safe Plan 07 summary; otherwise a hard stop with no summary and no Plan 08 execution.
</objective>

<execution_context>
@C:/Users/Dad/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Dad/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-rules-and-data-authority/01-CONTEXT.md
@.planning/phases/01-rules-and-data-authority/01-DISCUSSION-LOG.md
@.planning/phases/01-rules-and-data-authority/01-RESEARCH.md
@.planning/phases/01-rules-and-data-authority/01-VALIDATION.md
@.planning/phases/01-rules-and-data-authority/01-06-SUMMARY.md
@.gitignore
@docs/external-reuse-policy.md
@src/authority/canonical-json.ts
@src/authority/hash.ts
@src/authority/schemas.ts
@tests/authority/provenance.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement the cross-platform private source-set verifier</name>
  <read_first>
    - AGENTS.md
    - .gitignore
    - .planning/REQUIREMENTS.md
    - .planning/phases/01-rules-and-data-authority/01-CONTEXT.md
    - .planning/phases/01-rules-and-data-authority/01-RESEARCH.md
    - src/authority/canonical-json.ts
    - src/authority/hash.ts
    - src/authority/schemas.ts
    - tests/authority/provenance.test.ts
    - src/authority/private-source-set.ts (if it exists)
    - tests/authority/private-source-set.test.ts (if it exists)
  </read_first>
  <files>src/authority/private-source-set.ts, tests/authority/private-source-set.test.ts</files>
  <behavior>
    - A primary and independent backup containing the exact seven allowed relative paths with complete valid metadata and matching bytes returns a deterministic entry list and source-set root hash.
    - Missing/extra files, missing metadata, non-HTTPS/unapproved official hosts, invalid dates/media/hash forms, oversized individual/aggregate bytes, invalid card JSON, or byte/hash mismatch fail with exact deterministic paths.
    - Absolute, backslash, dot, empty, `..`, reserved, or non-canonical relative paths fail before filesystem access.
    - Same/nested roots, backup beneath the repository, symlinked roots/files, Windows junctions/reparse aliases, same realpath, and same dev/ino hardlinks fail closed.
    - Windows path comparisons are case-insensitive after realpath normalization; POSIX comparisons preserve case.
  </behavior>
  <action>Using TypeScript and Node standard-library APIs only per D-10, create `src/authority/private-source-set.ts`. Export `PRIVATE_AUTHORITY_SOURCE_PATHS` as exactly: `rulebook/rulebook-current.pdf`, `formats/constructed-current.html`, `codex/codex-current.html`, `codex/faqs-current.html`, `codex/changelog-current.html`, `updates/card-updates-2025.html`, and `cards/cards.raw.json`. Define strict readonly entry/options/result types requiring for every path: official HTTPS URL, ISO retrieval timestamp, nullable ISO effective date, exact media type (`application/pdf`, `text/html`, or `application/json` as appropriate), expected byte length, and lowercase `sha256:` digest. Export async `verifyPrivateSourceSet(options)` taking primary root, backup root, repository root, and metadata entries. Resolve then `realpath` all three roots; normalize comparisons case-insensitively on Windows and case-sensitively elsewhere; use `path.relative` plus `path.isAbsolute` to require each file stays beneath its root, the backup root stays outside the repository, and neither source root equals or contains the other. Reject root/file symlinks and junction/reparse aliases by comparing normalized resolved paths with realpaths and using `lstat`; use bigint `stat`/file-handle identity (`dev` and `ino`, failing closed when unavailable/zero) to reject the same directory, same file, or hardlinks on Node/libuv including Windows. Recursively enumerate both roots without following links and require exactly the seven canonical files and expected directories, no extras. Stream-hash rather than buffering arbitrary bytes; bound `cards/cards.raw.json` to the importer-compatible 10,000,000-byte maximum, each of the other six source files to 256 MiB, and the complete set to 768 MiB, validate byte lengths/hashes against metadata and across primary/backup, and strictly parse the bounded card JSON. Allow normative URLs only on the already-audited official hosts (`sorcerytcg.com`, `www.sorcerytcg.com`, `api.sorcerytcg.com`, `curiosa.io`, `www.curiosa.io`) with no credentials. Return canonical sorted entry evidence and a `sourceSetRootHash` computed over metadata plus byte hashes but excluding machine-specific root paths. Never fetch, write, or accept a hash as publisher authentication. Build tests first for every behavior above using temporary ordinary files, hardlinks, nested/same roots, traversal paths, and a Windows junction or equivalent reparse alias; preserve originals and clean only verified test roots. Commit the helper/test atomically after its focused test, typecheck, and full `pnpm verify` pass.</action>
  <verify>
    <automated>node --test tests/authority/private-source-set.test.ts &amp;&amp; pnpm typecheck &amp;&amp; pnpm verify</automated>
  </verify>
  <acceptance_criteria>
    - `PRIVATE_AUTHORITY_SOURCE_PATHS` contains exactly the seven locked relative paths and no generic glob or optional source branch.
    - `verifyPrivateSourceSet` requires every metadata field and produces a deterministic path-sorted result/source-set root hash only after primary and backup bytes match.
    - Containment uses realpath plus platform-aware `path.relative`; no case-sensitive textual prefix check remains.
    - Same/nested/repository-contained roots, symlink/junction aliases, non-ordinary files, and matching dev/ino hardlinks fail closed on Windows and POSIX.
    - Focused tests exercise valid sets, metadata/size/JSON failures, missing/extra files, traversal, casing, symlink/junction, hardlink, same/nested roots, and changed backup bytes.
  </acceptance_criteria>
  <done>A dependency-free reusable verifier safely binds the complete official primary/backup source set across Windows and POSIX.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Supply, back up, and attest the complete private official source set</name>
  <read_first>
    - .planning/REQUIREMENTS.md
    - .planning/phases/01-rules-and-data-authority/01-CONTEXT.md
    - .planning/phases/01-rules-and-data-authority/01-RESEARCH.md
    - .planning/phases/01-rules-and-data-authority/01-06-SUMMARY.md
    - .gitignore
    - docs/authority-precedence.md
    - docs/external-reuse-policy.md
    - src/authority/private-source-set.ts
    - tests/authority/private-source-set.test.ts
  </read_first>
  <files>.local/authority/inputs/official-2026-08-20/primary/** (human-created, Git-ignored), user-selected independent backup root (human-created, outside Git), .local/authority/locks/official-2026-08-20/source-set-lock.json (executor-created, Git-ignored), .planning/phases/01-rules-and-data-authority/01-07-SUMMARY.md (executor-created only after success)</files>
  <action>Before pausing, run `node --test tests/authority/private-source-set.test.ts`, `pnpm verify`, `git check-ignore -q .local/authority/inputs/official-2026-08-20/primary/probe`, and prove `git ls-files .local/authority` is empty. Then ask the user to save through their browser, without editing/reformatting, the exact seven files beneath `.local/authority/inputs/official-2026-08-20/primary/`: (1) the current standard rulebook PDF linked from `https://sorcerytcg.com/news/sorcery-contested-realm-december-2025-rulebook-update` as `rulebook/rulebook-current.pdf`; (2) `https://sorcerytcg.com/constructed` as `formats/constructed-current.html`; (3) `https://curiosa.io/codex` as `codex/codex-current.html`; (4) `https://curiosa.io/faqs` as `codex/faqs-current.html`; (5) `https://curiosa.io/codex/changelog` as `codex/changelog-current.html`; (6) `https://sorcerytcg.com/news/sorcery-contested-realm-card-updates-2025` as `updates/card-updates-2025.html`; and (7) `https://api.sorcerytcg.com/api/cards` as `cards/cards.raw.json`. For the rulebook entry, keep `https://sorcerytcg.com/news/sorcery-contested-realm-december-2025-rulebook-update` as the normative source URL in both the lock entry `url` and `rulebookAcquisitionEvidence.sourceUrl`; bind `rulebook/rulebook-current.pdf` by its exact byte length and SHA-256, and record the observed PDF filename, UTC retrieval timestamp, and optional private locator evidence explicitly marked non-normative. Never use a Google Drive, redirect, or download URL as normative authority. Require the user to provide each file's UTC retrieval timestamp, effective date when published/available, and media type. Ask the user to copy the exact seven-file tree to an absolute durable private backup root outside the repository without symlinks, junctions, hardlinks, or nested/shared roots, and provide that locator. Require the user to attest that the tool and both complete sets are private and noncommercial, will not be redistributed or released, every file was saved manually through the browser without project automation, and no card art was included. Explain that private rulebook/page bytes remain allowed only inside these ignored source roots for this operating model; they are never copied into the built revision, Git, or packages. Explain that this does not resolve the API-page/Terms conflict, establish publisher permission, or authorize later sharing, release with content, automated updating, or commercialization. After the user responds, construct metadata entries, call `verifyPrivateSourceSet`, and write canonical `.local/authority/locks/official-2026-08-20/source-set-lock.json` containing schema version, both absolute root locators, all seven metadata/byte fields, the returned `sourceSetRootHash`, and `rulebookAcquisitionEvidence` with exact `sourceUrl`, relative path, PDF byte hash, observed filename, retrieval timestamp, optional `privateLocatorEvidence`, and `privateLocatorIsNormative: false`; no source bytes or excerpts. Re-read that lock and call the verifier again rather than trusting user-supplied hashes. Prove all primary/backup paths remain untracked/unstaged/ignored. Only then create `01-07-SUMMARY.md` with a seven-row safe metadata/hash table, the rulebook observed filename and manual-release-page derivation note, source-set root hash, the fixed repository-relative primary label, a user-chosen non-sensitive backup label, the private lock SHA-256, exact attestation, and explicit no-legal-conclusion caveat. If any source, metadata field, independent-root/identity check, byte/hash equality, JSON parse, ignore/tracking check, or attestation is missing/contradictory/failing, stop: create no `01-07-SUMMARY.md`, do not execute Plan 08, and do not mark Phase 1 or DATA-01/02/03 complete.</action>
  <verify>
    <automated>node --input-type=module -e "import fs from 'node:fs';import cp from 'node:child_process';import {verifyPrivateSourceSet} from './src/authority/private-source-set.ts';const p='.local/authority/locks/official-2026-08-20/source-set-lock.json',lock=JSON.parse(fs.readFileSync(p,'utf8'));const u='https://sorcerytcg.com/news/sorcery-contested-realm-december-2025-rulebook-update',rule=lock.entries.find(function(e){return e.relativePath==='rulebook/rulebook-current.pdf'}),a=lock.rulebookAcquisitionEvidence;if(!rule||rule.url!==u||!a||a.sourceUrl!==u||a.relativePath!==rule.relativePath||a.byteHash!==rule.byteHash||a.retrievedAt!==rule.retrievedAt||typeof a.observedFilename!=='string'||a.observedFilename.trim()===''||a.privateLocatorIsNormative!==false)process.exit(1);const v=await verifyPrivateSourceSet({primaryRoot:lock.primaryRoot,backupRoot:lock.backupRoot,repositoryRoot:'.',entries:lock.entries});if(v.sourceSetRootHash!==lock.sourceSetRootHash||v.entries.length!==7)process.exit(1);if(cp.execFileSync('git',['ls-files','.local/authority'],{encoding:'utf8'}).trim())process.exit(1);if(cp.execFileSync('git',['diff','--cached','--name-only'],{encoding:'utf8'}).split(/\r?\n/).some(x=&gt;x.startsWith('.local/authority/')))process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - Primary and backup roots each contain exactly the seven required ordinary files at the locked relative paths and no extras.
    - Every entry has its exact official URL, UTC retrieval timestamp, effective date when available, media type, byte length, and lowercase SHA-256; the rulebook entry URL is exactly the official December 2025 release page while the locked PDF bytes are identified by relative path/hash.
    - The reusable verifier proves roots are independent/outside Git and corresponding files are non-linked, non-symlink/non-junction, byte-identical copies with distinct identities.
    - The user supplies the complete private/noncommercial/no-redistribution/manual-browser/no-automation/no-card-art attestation and acknowledges it is not legal permission.
    - The private `source-set-lock.json` contains exact absolute root locators and optional non-normative rulebook locator evidence; the committed `01-07-SUMMARY.md` contains only non-sensitive labels, relative paths, metadata/hashes, observed filename/derivation note, and the private-lock fingerprint—never the private locator. Every official source byte remains ignored, untracked, unstaged, and unpackaged.
    - If any criterion fails, `01-07-SUMMARY.md` and `01-08-SUMMARY.md` do not exist and Plan 08 is not started.
  </acceptance_criteria>
  <resume-signal>Reply with the absolute independent backup root, the seven retrieval timestamps/effective dates/media types, the observed rulebook PDF filename, optional private locator evidence, and: `I attest this tool and both complete source sets are private and noncommercial; I will not redistribute or release them; I saved every required file manually through my browser without project automation; no card art is included. I understand this does not establish legal permission.`</resume-signal>
  <done>The complete seven-source primary set and independent backup have matching verified bytes/metadata/root hash, remain outside Git, and the scoped attestation is recorded without a legal claim.</done>
</task>

</tasks>

<verification>
Run the helper's focused tests and `pnpm verify` before the pause. After the user action, independently re-run `verifyPrivateSourceSet` from the private lock, verify all seven metadata/hash rows and Git-ignore/tracked/staged state, and re-read the summary. Never release dependency 01-08 from a user-claimed hash or an incomplete source set.
</verification>

<success_criteria>
- A reusable TypeScript helper rejects textual-prefix, case, traversal, symlink/junction, hardlink, shared-root, and non-independent backup attacks.
- The current rulebook, base Constructed page, Codex, FAQs, changelog, card-update notice, and full API JSON exist as exact primary plus independent backup bytes with complete locked metadata.
- No project code fetched, scraped, polled, or automated acquisition; no official content entered Git or a package.
- Missing or contradictory evidence produces a hard stop with no summary, no Phase 1 completion, and no Plan 08 execution.
</success_criteria>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser-saved seven-file set -> private primary root | Files are untrusted, mutable, potentially oversized, malformed, mislabeled, or incomplete. |
| primary root -> independent backup root | Aliases, nesting, junctions, symlinks, or hardlinks could create the illusion of an independent backup. |
| private source roots -> Git/package boundary | Rulebook/page/API bytes could be staged, committed, or packaged accidentally. |
| user metadata/attestation -> Plan 08 dependency | Missing provenance or ambiguous scope could be mistaken for complete authority or legal permission. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-31 | Tampering / DoS | malicious source-set bytes/metadata | mitigate | Exact seven-file allowlist, strict metadata, per-file/aggregate bounds, streaming hashes, card JSON parse, no extras, and deterministic diagnostics/tests. |
| T-01-32 | Tampering / Elevation | traversal, symlink, junction, hardlink, shared-root escape | mitigate | Reusable realpath plus platform-aware path.relative containment, lstat alias rejection, bigint dev/ino identity checks, outside-repository backup, and focused Windows/POSIX tests. |
| T-01-33 | Spoofing | official source substitution | mitigate | Exact official HTTPS host/URL metadata, retrieval/effective dates, media types, primary/backup equality, and canonical source-set root hash; hashes are explicitly not authenticity proof. |
| T-01-34 | Tampering / Repudiation | source-set lock/receipt | mitigate | Executor recomputes every field with the helper, re-verifies the canonical private lock, and refuses the summary on missing/mismatched evidence. |
| T-01-35 | Information disclosure / legal | restricted source publication | mitigate | `.local/authority/` ignore plus tracked/staged checks; official PDF/page/API bytes remain private and never enter the built revision, Git, or packages; broader use requires a separate permission-gated plan. |
| T-01-SC | Tampering | package installs | accept | Helper/tests use Node standard-library APIs and existing pinned dependencies; no package is added. |
</threat_model>

<must_haves>
- DATA-01 receives exact independently backed bytes and complete metadata for every required official rule/format/Codex-FAQ/changelog/card-update source.
- DATA-02 receives the full API JSON inside the same locked seven-file set and remains reproducible from either independent root without network access.
- DATA-03 receives stable source IDs, URLs/dates/media types/byte hashes, a canonical source-set root hash, and exact reference evidence.
- D-08/D-11 keep the entire manual source set private/noncommercial/no-redistribution and stop execution without full evidence; no checkpoint output claims legal certainty.
</must_haves>

<output>
Create `.planning/phases/01-rules-and-data-authority/01-07-SUMMARY.md` only after both tasks and every automated/human acceptance criterion pass. Otherwise create neither `01-07-SUMMARY.md` nor `01-08-SUMMARY.md`, do not execute Plan 08, and leave Phase 1 plus DATA-01/02/03 pending.
</output>
