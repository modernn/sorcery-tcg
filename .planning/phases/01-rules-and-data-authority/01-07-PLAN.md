---
phase: 01-rules-and-data-authority
plan: "07"
type: execute
wave: 5
depends_on: [01-06]
files_modified:
  - src/authority/private-source-set.ts
  - tests/authority/private-source-set.test.ts
autonomous: false
requirements: [DATA-01, DATA-02, DATA-03]
user_setup:
  - service: fixed user-run private authority collector
    why: "Only the user may invoke the accepted one-shot collector and acknowledge the unresolved private-use risk; executors and tests must never run its production entry point."
    dashboard_config:
      - task: "Choose an absent absolute backup root outside the repository, then personally run: pwsh -NoProfile -NonInteractive -File scripts/collect-private-authority.ps1 -BackupRoot <absolute-path> -AcknowledgePrivateUseRisk"
        location: "User-controlled PowerShell 7 session and private local filesystem"
      - task: "Report only that the command completed and the private lock exists; do not paste private locators or source content into chat or Git."
        location: "User confirmation"
must_haves:
  truths:
    - "The D-08 user-run one-shot collector produces a complete seven-file official source set: current standard rulebook, base Constructed format, Codex, FAQs, Codex changelog, official card-update notice, and full card API JSON."
    - "Every source entry records exact relative path, official HTTPS URL, retrieval timestamp, effective date when available, media type, byte length, and SHA-256."
    - "A reusable TypeScript verifier proves primary and backup roots are independent and every corresponding file is an ordinary non-symlink/non-junction, non-hardlinked, byte-identical file."
    - "The collector production wrapper accepts only an absolute backup root and AcknowledgePrivateUseRisk; tests use synthetic loopback bytes and the executor never invokes the production path."
    - "The private lock records acquisitionMethod: user-run-one-shot-powershell plus the exact private/noncommercial/no-redistribution/no-upload/no-artwork, API/Terms/robots-risk, no-legal-permission, and stop-without-retry/evasion acknowledgment."
    - "All primary/backup bytes remain private, Git-ignored, untracked, unstaged, and unpackaged; the collector acquires no card art and exposes no artwork, arbitrary production URL, scheduler, or recurring update; it performs no retry and no evasion."
    - "Plan 08 remains unreachable and no 01-07 summary exists when the acquisition method, acknowledgment, source, metadata field, independent backup, hash/equality check, or rulebook evidence is missing or conflicting."
  artifacts:
    - path: "src/authority/private-source-set.ts"
      provides: "Reusable bounded source-set metadata, path containment, independent-root, file-identity, and byte/hash equality verifier"
      exports: ["PRIVATE_AUTHORITY_SOURCE_PATHS", "verifyPrivateSourceSet"]
    - path: "tests/authority/private-source-set.test.ts"
      provides: "Cross-platform source-set independence, traversal, symlink/junction, hardlink, metadata, and equality tests"
    - path: ".local/authority/locks/official-2026-08-20/source-set-lock.json"
      provides: "Collector-produced private exact source-set metadata, fixed acquisition method/acknowledgment, and independently computed root hash; Git-ignored and never committed"
    - path: ".planning/phases/01-rules-and-data-authority/01-07-SUMMARY.md"
      provides: "Safe non-content provenance receipt and operating attestation created only after every blocking check succeeds"
  key_links:
    - from: "scripts/collect-private-authority.ps1"
      to: ".local/authority/locks/official-2026-08-20/source-set-lock.json"
      via: "user-run one-shot collection, independent backup, three verifier gates, and receipt-last publication"
      pattern: "acquisitionMethod: user-run-one-shot-powershell"
    - from: "src/authority/private-source-set.ts"
      to: ".local/authority/inputs/official-2026-08-20/primary/"
      via: "realpath/path.relative containment plus lstat/stat identity and streaming SHA-256 verification"
      pattern: "verifyPrivateSourceSet"
    - from: ".local/authority/inputs/official-2026-08-20/primary/"
      to: "the user-selected backup root"
      via: "exact relative-path set, distinct dev/ino identities, byte length, and SHA-256 equality"
      pattern: "sourceSetRootHash"
    - from: ".local/authority/locks/official-2026-08-20/source-set-lock.json"
      to: ".planning/phases/01-rules-and-data-authority/01-08-PLAN.md"
      via: "complete seven-source evidence and independently recomputed source-set root hash"
      pattern: "rulebook/rulebook-current.pdf"
---

<objective>
Create the reusable private source-set safety verifier, then independently reverify the complete collector-produced authoritative input set and its durable backup without adding official bytes to Git.

Purpose: Release the final local build only when every DATA-01 authority source and the full DATA-02 card input have exact primary/backup bytes, complete provenance metadata, and a private-use operating attestation.
Output: A tested generic TypeScript verifier plus a private source-set lock and safe Plan 07 summary; otherwise a hard stop with no summary and no Plan 08 execution.
</objective>

<execution_context>
@C:/Users/Dad/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Dad/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-rules-and-data-authority/01-CONTEXT.md
@.planning/phases/01-rules-and-data-authority/01-DISCUSSION-LOG.md
@.planning/phases/01-rules-and-data-authority/01-RESEARCH.md
@.planning/phases/01-rules-and-data-authority/01-VALIDATION.md
@.planning/phases/01-rules-and-data-authority/01-06-SUMMARY.md
@.gitignore
@docs/external-reuse-policy.md
@src/authority/canonical-json.ts
@src/authority/hash.ts
@src/authority/schemas.ts
@tests/authority/provenance.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement the cross-platform private source-set verifier</name>
  <read_first>
    - AGENTS.md
    - .gitignore
    - .planning/REQUIREMENTS.md
    - .planning/phases/01-rules-and-data-authority/01-CONTEXT.md
    - .planning/phases/01-rules-and-data-authority/01-RESEARCH.md
    - src/authority/canonical-json.ts
    - src/authority/hash.ts
    - src/authority/schemas.ts
    - tests/authority/provenance.test.ts
    - src/authority/private-source-set.ts (if it exists)
    - tests/authority/private-source-set.test.ts (if it exists)
  </read_first>
  <files>src/authority/private-source-set.ts, tests/authority/private-source-set.test.ts</files>
  <behavior>
    - A primary and independent backup containing the exact seven allowed relative paths with complete valid metadata and matching bytes returns a deterministic entry list and source-set root hash.
    - Missing/extra files, missing metadata, non-HTTPS/unapproved official hosts, invalid dates/media/hash forms, oversized individual/aggregate bytes, invalid card JSON, or byte/hash mismatch fail with exact deterministic paths.
    - Absolute, backslash, dot, empty, `..`, reserved, or non-canonical relative paths fail before filesystem access.
    - Same/nested roots, backup beneath the repository, symlinked roots/files, Windows junctions/reparse aliases, same realpath, and same dev/ino hardlinks fail closed.
    - Windows path comparisons are case-insensitive after realpath normalization; POSIX comparisons preserve case.
  </behavior>
  <action>Using TypeScript and Node standard-library APIs only per D-10, create `src/authority/private-source-set.ts`. Export `PRIVATE_AUTHORITY_SOURCE_PATHS` as exactly: `rulebook/rulebook-current.pdf`, `formats/constructed-current.html`, `codex/codex-current.html`, `codex/faqs-current.html`, `codex/changelog-current.html`, `updates/card-updates-2025.html`, and `cards/cards.raw.json`. Define strict readonly entry/options/result types requiring for every path: official HTTPS URL, ISO retrieval timestamp, nullable ISO effective date, exact media type (`application/pdf`, `text/html`, or `application/json` as appropriate), expected byte length, and lowercase `sha256:` digest. Export async `verifyPrivateSourceSet(options)` taking primary root, backup root, repository root, and metadata entries. Resolve then `realpath` all three roots; normalize comparisons case-insensitively on Windows and case-sensitively elsewhere; use `path.relative` plus `path.isAbsolute` to require each file stays beneath its root, the backup root stays outside the repository, and neither source root equals or contains the other. Reject root/file symlinks and junction/reparse aliases by comparing normalized resolved paths with realpaths and using `lstat`; use bigint `stat`/file-handle identity (`dev` and `ino`, failing closed when unavailable/zero) to reject the same directory, same file, or hardlinks on Node/libuv including Windows. Recursively enumerate both roots without following links and require exactly the seven canonical files and expected directories, no extras. Stream-hash rather than buffering arbitrary bytes; bound `cards/cards.raw.json` to the importer-compatible 10,000,000-byte maximum, each of the other six source files to 256 MiB, and the complete set to 768 MiB, validate byte lengths/hashes against metadata and across primary/backup, and strictly parse the bounded card JSON. Allow normative URLs only on the already-audited official hosts (`sorcerytcg.com`, `www.sorcerytcg.com`, `api.sorcerytcg.com`, `curiosa.io`, `www.curiosa.io`) with no credentials. Return canonical sorted entry evidence and a `sourceSetRootHash` computed over metadata plus byte hashes but excluding machine-specific root paths. Never fetch, write, or accept a hash as publisher authentication. Build tests first for every behavior above using temporary ordinary files, hardlinks, nested/same roots, traversal paths, and a Windows junction or equivalent reparse alias; preserve originals and clean only verified test roots. Commit the helper/test atomically after its focused test, typecheck, and full `pnpm verify` pass.</action>
  <verify>
    <automated>node --test tests/authority/private-source-set.test.ts &amp;&amp; pnpm typecheck &amp;&amp; pnpm verify</automated>
  </verify>
  <acceptance_criteria>
    - `PRIVATE_AUTHORITY_SOURCE_PATHS` contains exactly the seven locked relative paths and no generic glob or optional source branch.
    - `verifyPrivateSourceSet` requires every metadata field and produces a deterministic path-sorted result/source-set root hash only after primary and backup bytes match.
    - Containment uses realpath plus platform-aware `path.relative`; no case-sensitive textual prefix check remains.
    - Same/nested/repository-contained roots, symlink/junction aliases, non-ordinary files, and matching dev/ino hardlinks fail closed on Windows and POSIX.
    - Focused tests exercise valid sets, metadata/size/JSON failures, missing/extra files, traversal, casing, symlink/junction, hardlink, same/nested roots, and changed backup bytes.
  </acceptance_criteria>
  <done>A dependency-free reusable verifier safely binds the complete official primary/backup source set across Windows and POSIX.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Run and independently verify the one-shot private collector</name>
  <read_first>
    - .planning/REQUIREMENTS.md
    - .planning/phases/01-rules-and-data-authority/01-CONTEXT.md
    - .planning/phases/01-rules-and-data-authority/01-RESEARCH.md
    - .planning/phases/01-rules-and-data-authority/01-06-SUMMARY.md
    - .gitignore
    - docs/authority-precedence.md
    - docs/external-reuse-policy.md
    - scripts/collect-private-authority.ps1
    - tests/authority/private-authority-collector.test.ts
    - src/authority/private-source-set.ts
    - tests/authority/private-source-set.test.ts
  </read_first>
  <files>.local/authority/inputs/official-2026-08-20/primary/** (collector-created, Git-ignored), user-selected independent backup root (collector-created, outside Git), .local/authority/locks/official-2026-08-20/source-set-lock.json (collector-created, Git-ignored), .planning/phases/01-rules-and-data-authority/01-07-SUMMARY.md (executor-created only after independent verification)</files>
  <action>Before pausing, run `node --test tests/authority/private-authority-collector.test.ts`, `node --test tests/authority/private-source-set.test.ts`, `pnpm verify`, `git check-ignore -q .local/authority/inputs/official-2026-08-20/primary/probe`, and prove `git ls-files .local/authority` is empty. The executor and test suite must not run the production collector or contact publisher endpoints.

Ask the user to choose an absent absolute durable backup root outside the repository, then personally run exactly `pwsh -NoProfile -NonInteractive -File scripts/collect-private-authority.ps1 -BackupRoot <absolute-path> -AcknowledgePrivateUseRisk`. Explain that this is the sole fixed user-run one-shot D-08 exception. Its recorded acknowledgment is: private/local/noncommercial use; no redistribution, release, hosting, third-party upload, or artwork; the unresolved public API guidance/publisher Terms/API `robots.txt` conflict and private-use risk are accepted; the collector does not establish legal permission; and HTTP 401/403/429, CAPTCHA/block evidence, or publisher objection requires stopping without retry or evasion. The command has no arbitrary production URL, scheduler, recurring update, retry/evasion, or art surface. The user should report only completion, not private locators or source content.

After the user reports completion, independently load `.local/authority/locks/official-2026-08-20/source-set-lock.json`. Before opening source bytes, require `schemaVersion === 1`, `acquisitionMethod === 'user-run-one-shot-powershell'`, the exact structured operating acknowledgment above, exactly seven entries at the locked paths, the fixed official URLs/media/effective dates, lowercase hashes and bounded lengths, and `rulebookAcquisitionEvidence` whose `sourceUrl` is the official December 2025 release page, relative path/hash/retrievedAt match the rulebook entry, observed filename is `SorceryRulebook.pdf`, and `privateLocatorIsNormative === false`. Reject missing, stale, manual, recurring, or conflicting acquisition method/evidence.

Call `verifyPrivateSourceSet` using the lock's final primary/backup roots, repository root, and entries. Require its sorted seven entries and `sourceSetRootHash` to match the lock exactly; hashes prove identity, not authenticity or permission. Prove `.local/authority/` remains ignored, untracked, and unstaged and that the independent backup remains outside Git. Only then create `01-07-SUMMARY.md` with `acquisitionMethod: user-run-one-shot-powershell`, a seven-row safe metadata/hash table, standard-rulebook observed filename and collector release-page derivation note, source-set root hash, fixed repository-relative primary label, a non-sensitive backup label, private-lock SHA-256, the exact operating acknowledgment, and explicit no-legal-conclusion caveat—never private locators or source excerpts. If any method, acknowledgment, source, metadata, independent-root/identity, byte/hash equality, JSON, rulebook evidence, ignore/tracking, or attestation check fails, stop: create no `01-07-SUMMARY.md`, do not execute Plan 08, and do not mark Phase 1 or DATA-01/02/03 complete.</action>
  <verify>
    <automated>node --input-type=module -e "import fs from 'node:fs';import cp from 'node:child_process';import {PRIVATE_AUTHORITY_SOURCE_PATHS,verifyPrivateSourceSet} from './src/authority/private-source-set.ts';const p='.local/authority/locks/official-2026-08-20/source-set-lock.json',lock=JSON.parse(fs.readFileSync(p,'utf8')),expected={scope:'private-local-noncommercial',noRedistributionReleaseHostingUploadOrArtwork:true,apiTermsRobotsConflictAndPrivateUseRiskAccepted:true,establishesLegalPermission:false,stopOnBlockedStatusCaptchaOrPublisherObjection:true,retryOrEvasion:false};if(lock.schemaVersion!==1||lock.acquisitionMethod!=='user-run-one-shot-powershell'||JSON.stringify(lock.operatingAcknowledgment)!==JSON.stringify(expected)||lock.entries.length!==7||JSON.stringify(lock.entries.map(e=&gt;e.relativePath).sort())!==JSON.stringify([...PRIVATE_AUTHORITY_SOURCE_PATHS].sort()))process.exit(1);const u='https://sorcerytcg.com/news/sorcery-contested-realm-december-2025-rulebook-update',rule=lock.entries.find(e=&gt;e.relativePath==='rulebook/rulebook-current.pdf'),a=lock.rulebookAcquisitionEvidence;if(!rule||rule.url!==u||!a||a.sourceUrl!==u||a.relativePath!==rule.relativePath||a.byteHash!==rule.byteHash||a.retrievedAt!==rule.retrievedAt||a.observedFilename!=='SorceryRulebook.pdf'||a.privateLocatorIsNormative!==false)process.exit(1);const v=await verifyPrivateSourceSet({primaryRoot:lock.primaryRoot,backupRoot:lock.backupRoot,repositoryRoot:'.',entries:lock.entries});if(v.sourceSetRootHash!==lock.sourceSetRootHash||JSON.stringify(v.entries)!==JSON.stringify(lock.entries))process.exit(1);if(cp.execFileSync('git',['ls-files','.local/authority'],{encoding:'utf8'}).trim())process.exit(1);if(cp.execFileSync('git',['diff','--cached','--name-only'],{encoding:'utf8'}).split(/\r?\n/).some(x=&gt;x.startsWith('.local/authority/')))process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - Primary and backup roots each contain exactly the seven required ordinary files at the locked relative paths and no extras.
    - Every entry has its exact official URL, UTC retrieval timestamp, effective date when available, media type, byte length, and lowercase SHA-256; the rulebook entry URL is exactly the official December 2025 release page while the locked PDF bytes are identified by relative path/hash.
    - The reusable verifier proves roots are independent/outside Git and corresponding files are non-linked, non-symlink/non-junction, byte-identical copies with distinct identities.
    - The private lock and summary require `acquisitionMethod: user-run-one-shot-powershell`; missing, manual, recurring, or conflicting method evidence fails closed before Plan 08.
    - The collector records the complete private/local/noncommercial, no-redistribution/release/hosting/upload/artwork, API/Terms/robots-risk, no-legal-permission, and stop-without-retry/evasion acknowledgment.
    - The private `source-set-lock.json` contains exact absolute root locators and non-normative rulebook locator evidence; the committed `01-07-SUMMARY.md` contains only non-sensitive labels, relative paths, metadata/hashes, observed filename/collector derivation note, and the private-lock fingerprint—never the private locator. Every official source byte remains ignored, untracked, unstaged, and unpackaged.
    - If any criterion fails, `01-07-SUMMARY.md` and `01-08-SUMMARY.md` do not exist and Plan 08 is not started.
  </acceptance_criteria>
  <resume-signal>Choose an absent absolute backup root outside the repository, personally run `pwsh -NoProfile -NonInteractive -File scripts/collect-private-authority.ps1 -BackupRoot &lt;absolute-path&gt; -AcknowledgePrivateUseRisk`, then reply `collector complete`. By using the switch, you record: private/local/noncommercial use; no redistribution, release, hosting, third-party upload, or artwork; acceptance of the unresolved API/Terms/robots private-use risk; this does not establish legal permission; and stop without retry or evasion on 401/403/429/CAPTCHA/block or publisher objection.</resume-signal>
  <done>The complete seven-source primary set and independent backup have matching verified bytes/metadata/root hash, remain outside Git, and the scoped attestation is recorded without a legal claim.</done>
</task>

</tasks>

<verification>
Run both collector/verifier focused suites and `pnpm verify` before the pause; never run the production collector as the executor. After the user's one-shot action, require the fixed acquisition method and acknowledgment, independently re-run `verifyPrivateSourceSet` from the private lock, verify all seven metadata/hash rows plus rulebook evidence and Git-ignore/tracked/staged state, and re-read the summary. Never release dependency 01-08 from a user-claimed hash, conflicting acquisition method, or incomplete source set.
</verification>

<success_criteria>
- A reusable TypeScript helper rejects textual-prefix, case, traversal, symlink/junction, hardlink, shared-root, and non-independent backup attacks.
- The current rulebook, base Constructed page, Codex, FAQs, changelog, card-update notice, and full API JSON exist as exact primary plus independent backup bytes with complete locked metadata.
- Only the user's fixed one-shot collector acquired the sources; no executor/test live request, recurring/agent-run acquisition, retry/evasion, artwork, or arbitrary production URL exists, and no official content entered Git or a package.
- Missing or contradictory evidence produces a hard stop with no summary, no Phase 1 completion, and no Plan 08 execution.
</success_criteria>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user-run collector -> private primary/backup roots | Remote responses and filesystem destinations are untrusted, potentially oversized, malformed, blocked, aliased, partial, or pre-existing. |
| primary root -> independent backup root | Aliases, nesting, junctions, symlinks, or hardlinks could create the illusion of an independent backup. |
| private source roots -> Git/package boundary | Rulebook/page/API bytes could be staged, committed, or packaged accidentally. |
| collector lock/acknowledgment -> Plan 08 dependency | Missing or conflicting acquisition method, provenance, rulebook evidence, or scope could be mistaken for complete authority or legal permission. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-31 | Tampering / DoS | malicious source-set bytes/metadata | mitigate | Exact seven-file allowlist, strict metadata, per-file/aggregate bounds, streaming hashes, card JSON parse, no extras, and deterministic diagnostics/tests. |
| T-01-32 | Tampering / Elevation | traversal, symlink, junction, hardlink, shared-root escape | mitigate | Reusable realpath plus platform-aware path.relative containment, lstat alias rejection, bigint dev/ino identity checks, outside-repository backup, and focused Windows/POSIX tests. |
| T-01-33 | Spoofing | official source substitution | mitigate | Fixed seven-source collector manifest, bounded redirect/content validation, exact official provenance, dates/media, primary/backup equality, and canonical source-set root hash; hashes are explicitly not authenticity proof. |
| T-01-34 | Tampering / Repudiation | source-set lock/receipt | mitigate | Collector publishes the lock last only after staged/final verification; executor independently requires the acquisition method/acknowledgment and re-verifies every field before summary. |
| T-01-35 | Information disclosure / legal | restricted source publication | mitigate | `.local/authority/` ignore plus tracked/staged checks; official PDF/page/API bytes remain private and never enter the built revision, Git, or packages; broader use requires a separate permission-gated plan. |
| T-01-SC | Tampering | package installs | accept | Helper/tests use Node standard-library APIs and existing pinned dependencies; no package is added. |
</threat_model>

<must_haves>
- DATA-01 receives exact independently backed bytes and complete metadata for every required official rule/format/Codex-FAQ/changelog/card-update source.
- DATA-02 receives the full API JSON inside the same locked seven-file set and remains reproducible from either independent root without network access.
- DATA-03 receives stable source IDs, URLs/dates/media types/byte hashes, a canonical source-set root hash, and exact reference evidence.
- D-08/D-11 permit only the fixed user-run one-shot collector with `AcknowledgePrivateUseRisk`; all content remains private/noncommercial/no-redistribution/no-upload/no-artwork, broader use remains permission-gated, and no checkpoint output claims legal certainty.
</must_haves>

<output>
Create `.planning/phases/01-rules-and-data-authority/01-07-SUMMARY.md` only after both tasks and every automated/human acceptance criterion pass. Otherwise create neither `01-07-SUMMARY.md` nor `01-08-SUMMARY.md`, do not execute Plan 08, and leave Phase 1 plus DATA-01/02/03 pending.
</output>
