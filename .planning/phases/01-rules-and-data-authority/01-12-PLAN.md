---
phase: 01-rules-and-data-authority
plan: "12"
type: execute
wave: 7
depends_on: ["01-08"]
files_modified:
  - scripts/verify-private-authority-boundary.ts
  - tests/authority/private-authority-boundary.test.ts
  - tests/private-authority/repository-boundary.test.ts
  - docs/external-reuse-policy.md
autonomous: true
requirements: [DATA-01, DATA-02, DATA-03]
gap_closure: true
must_haves:
  truths:
    - "Arbitrary-offset private source excerpts and reordered/normalized card records fail the same boundary gate as exact copies."
    - "Reachable history, worktree, index, and every dry-run package file are all scanned by one production implementation."
    - "Native, slash-normalized, JSON-escaped, TypeScript-escaped, and Windows case-varied private locator forms fail without the diagnostic disclosing the locator."
    - "Policy permits normalized official derivatives only inside the ignored private revision and forbids them from Git, packages, sharing, hosting, and redistribution."
  artifacts:
    - path: "scripts/verify-private-authority-boundary.ts"
      provides: "Single comprehensive restricted-content and locator scanner"
    - path: "tests/authority/private-authority-boundary.test.ts"
      provides: "Synthetic all-offset, normalized, encoding, and surface regressions"
    - path: "tests/private-authority/repository-boundary.test.ts"
      provides: "Opt-in real boundary gate that delegates to the production scanner without printing private values"
    - path: "docs/external-reuse-policy.md"
      provides: "Non-contradictory ignored-private-revision policy"
  key_links:
    - from: "tests/private-authority/repository-boundary.test.ts"
      to: "scripts/verify-private-authority-boundary.ts"
      via: "invokes the same production scanner used by the release gate"
      pattern: "verify-private-authority-boundary"
    - from: "scripts/verify-private-authority-boundary.ts"
      to: "git and pnpm candidate surfaces"
      via: "reachable-history, worktree, index, and package enumerators"
      pattern: "reachable-history|worktree|index|package"
---

<objective>
Close CR-06 and WR-03 by replacing sampled leakage checks with one comprehensive boundary and correcting the private-revision policy.

Purpose: D-08/D-11 isolation is not proved when unsampled excerpts, semantic derivatives, or escaped/case-varied locators can enter history, the worktree, index, or package; documentation must match the actual ignored private revision.
Output: Comprehensive standard-library scanner, public synthetic regressions, one delegated private gate, and precise policy wording.
</objective>

<execution_context>
@C:/Users/Dad/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Dad/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-rules-and-data-authority/01-CONTEXT.md
@.planning/phases/01-rules-and-data-authority/01-RESEARCH.md
@.planning/phases/01-rules-and-data-authority/01-PATTERNS.md
@.planning/phases/01-rules-and-data-authority/01-VERIFICATION.md
@.planning/phases/01-rules-and-data-authority/01-REVIEW.md
@.planning/phases/01-rules-and-data-authority/01-06-SUMMARY.md
@.planning/phases/01-rules-and-data-authority/01-08-SUMMARY.md
@docs/external-reuse-policy.md

<interfaces>
Production CLI:
- `node scripts/verify-private-authority-boundary.ts --repository-root &lt;root&gt; --lock &lt;private-lock&gt;`
- Success prints one fixed line; violations print category plus candidate surface/path, never matched content or locator.

Current scanner gaps:
- `sourceMarkers` samples only start/middle/end windows.
- `gitCandidates` scans HEAD and index, while the private test separately implements reachable history/worktree/index/package scanning.
- Locator forms include only native and slash-normalized text.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Scan every candidate surface for raw and normalized source derivatives</name>
  <read_first>
    - .planning/phases/01-rules-and-data-authority/01-VERIFICATION.md
    - .planning/phases/01-rules-and-data-authority/01-REVIEW.md
    - scripts/verify-private-authority-boundary.ts
    - tests/authority/private-authority-boundary.test.ts
    - tests/private-authority/repository-boundary.test.ts
    - src/authority/canonical-json.ts
  </read_first>
  <files>scripts/verify-private-authority-boundary.ts, tests/authority/private-authority-boundary.test.ts, tests/private-authority/repository-boundary.test.ts</files>
  <behavior>
    - A 32-byte source excerpt from any source offset fails; detection is not restricted to periodic or three sampled offsets.
    - Reordered JSON objects, a single copied card record, whitespace/case-normalized visible HTML text, and safely decoded JSON/TypeScript string representations fail.
    - Every safely decoded JSON or TypeScript string-literal candidate is re-fed through the same raw-offset and semantic fingerprint pipeline used for ordinary candidate bytes; decoding is never locator-only.
    - A blob reachable only from older Git history, a tracked/untracked worktree file, an index blob, and an actual package file are each inspected.
    - Exact hashes, artwork signatures/extensions, publisher-document/full-corpus/community checks, fixed bounds, and content-free diagnostics remain active.
  </behavior>
  <action>Add RED synthetic cases at unsampled offsets and for reordered JSON, one card record, normalized visible text, JSON-escaped source excerpts, TypeScript-escaped source excerpts, escaped/reordered protected card records, removed-from-HEAD reachable history, modified/untracked worktree, staged index, and dry-run package surfaces. Exercise the escaped source and escaped card negatives on every history/worktree/index/package surface. Replace `sourceMarkers` with a deterministic bounded comprehensive fingerprint index that covers every raw source offset at the minimum protected excerpt length and confirms matches before failing; periodic/three-position sampling is forbidden. Add normalized fingerprints for bounded UTF-8 visible text and canonical JSON top-level records/subtrees using the existing project canonicalizer, so property order/insignificant whitespace cannot evade the gate. Build one production `inspectCandidate` pipeline that accepts a candidate byte buffer plus a surface label and applies the raw-offset and semantic fingerprints. Add a bounded, non-evaluating decoder for JSON string values and TypeScript string literals, then feed every successfully decoded textual candidate back through `inspectCandidate` so encoded protected content receives identical checks. Move reachable-history, worktree, index, and package enumeration into the production script and route every ordinary and decoded candidate through that pipeline; preserve path confinement, byte/work limits, exact-source hash checks, and output that names only category/surface/candidate path. Use Node/project code only, no third-party package, and fail closed if fingerprint or decode work exceeds fixed limits rather than silently sampling per D-07/D-08/D-09/D-10.</action>
  <verify>
    <automated>node --test --test-name-pattern="arbitrary|offset|reordered|partial card|normalized|encoded|escaped source|escaped card|history|worktree|index|package" tests/authority/private-authority-boundary.test.ts &amp;&amp; pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - No fixed-position or periodic source-marker sampler remains.
    - Public tests prove arbitrary-offset raw, normalized semantic derivatives, JSON/TypeScript-escaped source excerpts, and escaped/reordered protected card records fail on every declared surface.
    - Safely decoded textual candidates re-enter the same raw-offset and semantic inspection path on history, worktree, index, and package surfaces.
    - Production scanner owns all four surface enumerators; the private test does not maintain a divergent leakage algorithm.
    - Diagnostics never include source bytes, excerpts, roots, or locator values.
  </acceptance_criteria>
  <done>D-08/D-09 restricted source content and derivatives cannot bypass the release gate by offset, normalization, surface, or file name.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Detect escaped and case-varied locator forms through the same gate</name>
  <read_first>
    - .planning/phases/01-rules-and-data-authority/01-CONTEXT.md
    - scripts/verify-private-authority-boundary.ts
    - tests/authority/private-authority-boundary.test.ts
    - tests/private-authority/repository-boundary.test.ts
  </read_first>
  <files>scripts/verify-private-authority-boundary.ts, tests/authority/private-authority-boundary.test.ts, tests/private-authority/repository-boundary.test.ts</files>
  <behavior>
    - Native and slash-normalized roots/locators fail as before.
    - JSON-escaped and TypeScript single/double-quoted escape forms fail after safe decoding.
    - Windows drive/path locator matching is case-insensitive even when the gate runs on another platform; POSIX locator matching remains case-sensitive.
    - The real private repository-boundary test invokes the production scanner and asserts only status/fixed output, never reads or prints matched private values.
  </behavior>
  <action>Write cross-platform synthetic canary cases first. Build a deduplicated locator representation set for native separators, slash/backslash forms, `JSON.stringify` escaping, TypeScript-safe single/double-quoted escaping, and Windows case-folding. Reuse the bounded, non-evaluating decoder from Task 1 for those supported escape forms and extend it only for explicit locator encodings under the same size/depth limits. Match Windows-shaped locators case-insensitively and POSIX-shaped locators exactly. Delete the duplicated weak locator/source-marker inspection from `repository-boundary.test.ts`; retain its no-network assertions, but make its leakage assertion execute the production scanner with the existing private CLI inputs and validate only fixed success/nonzero output. Never include a matched locator in an exception, snapshot, or summary.</action>
  <verify>
    <automated>node --test --test-name-pattern="locator|escaped|case-varied" tests/authority/private-authority-boundary.test.ts &amp;&amp; node --test --test-name-pattern="reachable history worktree index and package" tests/private-authority/repository-boundary.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - JSON/TypeScript escaped Windows locator canaries and mixed-case drive/path canaries fail on all platforms.
    - POSIX case semantics remain exact and no broad case folding creates a silent allow path.
    - The opt-in private leakage gate has one scanner implementation and emits no private content.
  </acceptance_criteria>
  <done>D-08/D-11 locator secrecy is enforced across common source-code/string encodings by the same release gate.</done>
</task>

<task type="auto">
  <name>Task 3: Align policy with the ignored private normalized revision</name>
  <read_first>
    - .planning/phases/01-rules-and-data-authority/01-CONTEXT.md
    - .planning/phases/01-rules-and-data-authority/01-VERIFICATION.md
    - docs/external-reuse-policy.md
    - data/authority/README.md
    - src/commands/import-authority.ts
    - tests/authority/bundle.test.ts
  </read_first>
  <files>docs/external-reuse-policy.md</files>
  <action>Correct the WR-03 contradiction per D-08/D-11: normalized official derivatives may exist only inside the selected ignored private local authority revision needed by the offline importer/runtime, while raw publisher PDF/HTML/API corpus bytes remain excluded from that built revision. State that both raw content and normalized derivatives remain forbidden from Git, package contents, sharing, hosting, upload, redistribution, public/network APIs, and commercialization without written permission and a separate plan. Preserve every no-artwork, no-recurring-acquisition, private/noncommercial, no-legal-conclusion, and community clean-room restriction; change no importer/revision format because the implemented private revision is the locked current operating decision.</action>
  <verify>
    <automated>node -e "const s=require('node:fs').readFileSync('docs/external-reuse-policy.md','utf8').toLowerCase();for(const x of ['normalized official derivatives','ignored private','built revision','forbidden from git','packages','sharing','hosting','redistribution','written publisher permission','separate plan'])if(!s.includes(x))process.exit(1);if(s.includes('normalized official derivatives')&amp;&amp;s.includes('forbidden from git, packages, and the built revision'))process.exit(1)" &amp;&amp; pnpm verify</automated>
  </verify>
  <acceptance_criteria>
    - Policy explicitly permits normalized derivatives only in the ignored private revision used offline.
    - Policy continues to exclude raw source corpus bytes from the built revision and all protected content/derivatives from Git/packages/external use.
    - No acquisition, redistribution, artwork, public API, or commercial permission is broadened.
  </acceptance_criteria>
  <done>WR-03 is closed with one internally consistent private-local storage and distribution boundary.</done>
</task>

</tasks>

<gap_coverage>
- CR-06: closed by Tasks 1-2 with comprehensive raw/semantic fingerprints, all four candidate surfaces, encoding variants, case-aware locator matching, and one scanner implementation.
- WR-03: closed by Task 3; normalized derivatives are private-revision-only while raw sources and every external distribution remain prohibited.
</gap_coverage>

<source_coverage_audit>

| Source | ID | Feature / constraint | Plan | Status | Notes |
|--------|----|----------------------|------|--------|-------|
| GOAL | — | Exact reproducible official rules, format, card data, and provenance | 01-09..01-12 | COVERED | Card completeness/identity, authority resolution, acquisition, and isolation all close. |
| REQ | DATA-01 | Immutable exact offline authority bundle | 01-10, 01-11, 01-12 | COVERED | Complete sources, fail-closed resolution/evidence, and private boundary. |
| REQ | DATA-02 | Complete deterministic offline normalized snapshot | 01-09, 01-12 | COVERED | Gameplay fields preserved; private derivative policy aligned. |
| REQ | DATA-03 | Stable IDs, schemas, provenance, and hashes | 01-09, 01-10, 01-12 | COVERED | Stable card identity, honest acyclic provenance, zero leakage. |
| RESEARCH | — | Strict bounded canonical offline pipeline; no service or repair | 01-09..01-12 | COVERED | Existing modules/stdlib only; every trust boundary fails closed. |
| RESEARCH | — | Official-only precedence and unsupported ambiguity | 01-10 | COVERED | Supersession graph and semantic date checks. |
| RESEARCH | — | Private/local clean-room and no-distribution boundary | 01-11, 01-12 | COVERED | Collector output and comprehensive repository/package gate. |
| CONTEXT | D-01 | Official sources alone are normative | 01-10, 01-11 | COVERED | Complete official acquisition plus official-only resolution. |
| CONTEXT | D-02 | Later official supersession; unresolved conflicts unsupported | 01-10 | COVERED | Graph resolution, cycles/partial conflicts fail closed. |
| CONTEXT | D-03 | Runtime selects validated local ID/hash, never live authority | 01-10, 01-12 | COVERED | Honest offline validator evidence and private local revision. |
| CONTEXT | D-04 | Immutable revisions with complete source/derivation evidence | 01-10, 01-11 | COVERED | Source completeness, SHA binding, acyclic derivation. |
| CONTEXT | D-05 | Deterministic cards retain publisher ID plus stable project ID | 01-09 | COVERED | Lossless fields and publisher-scoped logical ID. |
| CONTEXT | D-06 | Common provenance envelope | 01-09, 01-10 | COVERED | SourceRefs remain revision-bound; graph/evidence corrected. |
| CONTEXT | D-07 | Canonical strict validation; no repair/drop | 01-09, 01-10, 01-11 | COVERED | Required fields, graph/date/source completeness checks. |
| CONTEXT | D-08 | Narrow private one-shot evidence; no leakage/art/retry/evasion | 01-11, 01-12 | COVERED | Sanitized fixed collector plus comprehensive boundary. |
| CONTEXT | D-09 | External projects remain behavior-only | 01-12 | COVERED | Scanner/policy preserve no-copy clean-room boundary. |
| CONTEXT | D-10 | TypeScript/Node standard library first | 01-09..01-12 | COVERED | No package install; reuse existing Zod/project helpers. |
| CONTEXT | D-11 | Broader acquisition/sharing/API/art/commercial use remains gated | 01-12 | COVERED | Corrected policy broadens no external use. |
| CONTEXT | Deferred | Deck/collection, game rules, models, browser UI | NONE | EXCLUDED | Explicitly deferred to later roadmap phases. |
</source_coverage_audit>

<finding_adjudication_audit>

| Finding | Verdict from verification | Closure plan |
|---------|---------------------------|--------------|
| CR-01 | REAL | 01-11 Task 1 |
| CR-02 | REAL | 01-11 Task 2 |
| CR-03 | REAL | 01-11 Task 1 |
| CR-04 | REAL | 01-10 Task 1 |
| CR-05 | PARTIALLY REAL; SHA locator binding and evidence honesty confirmed | 01-10 Task 2 |
| CR-06 | REAL | 01-12 Tasks 1-2 |
| CR-07 | REAL | 01-09 Tasks 1-2 |
| CR-08 | REAL | 01-09 Task 3 |
| CR-09 | REAL | 01-10 Task 3 |
| WR-01 | REAL | 01-11 Task 2 |
| WR-02 | REAL | 01-10 Task 1 |
| WR-03 | REAL | 01-12 Task 3 |
</finding_adjudication_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| private authority roots -> fingerprint index | Restricted bytes must be analyzed without disclosure or unbounded work. |
| Git/worktree/index/package -> candidate scanner | Any publication surface can carry renamed, partial, or transformed content. |
| private locator -> source-code encoding | Escaping and Windows case variation can hide a locator from literal matching. |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan |
|-----------|----------|----------|-----------|-------------|-----------------|
| T-01-53 | Information disclosure | high | raw/normalized fingerprints | mitigate (blocking) | Every-offset and semantic fingerprints across all surfaces; fixed content-free diagnostics; fail closed on work bounds. |
| T-01-54 | Information disclosure | high | locator matcher | mitigate (blocking) | Native/slash/JSON/TS/case-aware forms plus safe decoding and cross-platform tests. |
| T-01-55 | Tampering | high | release-gate divergence | mitigate (blocking) | Private test delegates to the production scanner; duplicate weak algorithm is removed. |
| T-01-56 | Denial of service | medium | comprehensive scan | mitigate | Fixed byte/work/fingerprint limits and bounded child-command buffers; exceedance fails closed. |
| T-01-SC | Tampering | low | package installs | accept | No package is added; Node standard library and existing canonicalizer suffice. |
</threat_model>

<verification>
Run `pnpm verify` and the focused opt-in repository-boundary test. Confirm arbitrary-offset, canonical/reordered, encoded, partial-card, escaped-locator, and case-varied canaries fail across reachable history, worktree, index, and package; diagnostics expose only category/surface/candidate path; policy matches the ignored private revision.
</verification>

<success_criteria>
- One bounded production scanner covers raw excerpts, semantic derivatives, locator encodings, and every publication surface.
- The private test delegates to that scanner and never logs protected content or locators.
- High-severity leakage and release-gate divergence threats are blocking.
- Policy and importer agree: normalized data is private-revision-only; raw sources and all external publication remain prohibited.
</success_criteria>

<output>
Create `.planning/phases/01-rules-and-data-authority/01-12-SUMMARY.md` when done.
</output>
