---
phase: 01-rules-and-data-authority
plan: "05"
type: execute
wave: 3
depends_on: [01-03, 01-04]
files_modified:
  - package.json
  - src/commands/import-authority.ts
  - src/commands/validate-authority.ts
  - tests/authority/bundle.test.ts
  - tests/authority/fixtures/bundle-input/sources.json
  - tests/authority/fixtures/bundle-input/formats.json
  - tests/authority/fixtures/bundle-input/cards.raw.json
  - tests/authority/fixtures/bundle-input/input-lock.json
autonomous: true
requirements: [DATA-01, DATA-02]
must_haves:
  truths:
    - "A developer can build a new immutable authority revision from approved local inputs and validate it offline."
    - "The importer refuses any input tree whose exact file list, per-file hashes, or canonical input-root hash differs from its reviewed durable input lock."
    - "The builder validates completely before an atomic publish, refuses overwrite, and leaves no published partial revision after failure per D-04."
    - "The validator reports stable diagnostics and exits non-zero for invalid, tampered, ambiguous, unsupported, or storage-prohibited bundles."
    - "Neither command contains an official API retrieval or any other network path per D-03 and the license checkpoint."
  artifacts:
    - path: "src/commands/import-authority.ts"
      provides: "Local-only immutable authority revision builder"
    - path: "src/commands/validate-authority.ts"
      provides: "Offline authority bundle validation command"
    - path: "tests/authority/bundle.test.ts"
      provides: "Write-once, atomic, deterministic, and offline command integration proof"
  key_links:
    - from: "src/commands/import-authority.ts"
      to: "src/authority/normalize-cards.ts"
      via: "normalizes approved pinned local card bytes"
      pattern: "normalizeCards"
    - from: "src/commands/import-authority.ts"
      to: "src/authority/validate-bundle.ts"
      via: "validates the temporary revision before atomic rename"
      pattern: "validateAuthorityBundle"
    - from: "src/commands/validate-authority.ts"
      to: "src/authority/validate-bundle.ts"
      via: "thin offline CLI adapter"
      pattern: "validateAuthorityBundle"
---

<objective>
Expose the authority library through local-only, atomic developer commands.

Purpose: Let developers reproducibly build and validate immutable authority bundles without granting runtime or automation a mutable network acquisition path.
Output: Import and validate commands, package scripts, and complete bundle integration tests.
</objective>

<execution_context>
@C:/Users/Dad/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Dad/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-rules-and-data-authority/01-CONTEXT.md
@.planning/phases/01-rules-and-data-authority/01-RESEARCH.md
@.planning/phases/01-rules-and-data-authority/01-VALIDATION.md
@.planning/phases/01-rules-and-data-authority/01-PATTERNS.md
@.planning/phases/01-rules-and-data-authority/01-03-SUMMARY.md
@.planning/phases/01-rules-and-data-authority/01-04-SUMMARY.md

<interfaces>
Consume `normalizeCards`, `createCanonicalArtifact`, and `validateAuthorityBundle` exactly as exported by Plans 02-04.

Command contracts:
- `pnpm authority:import -- --input <local-dir> --input-lock <relative-lock-path> --expected-input-root-hash <sha256:...> --output-root <authority-root> --revision-id <stable-id>`
- `pnpm authority:validate -- --root <authority-root> --bundle <relative-bundle-path> --id <stable-id> --hash <sha256:...>`
- import stdout is deterministic machine-readable JSON containing the verified input-root hash and newly calculated bundle root hash; diagnostics go to stderr in path/code/message order; invalid input exits non-zero.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build write-once revisions atomically from local inputs</name>
  <read_first>
    - .planning/phases/01-rules-and-data-authority/01-CONTEXT.md
    - .planning/phases/01-rules-and-data-authority/01-RESEARCH.md
    - package.json
    - src/authority/schemas.ts
    - src/authority/normalize-cards.ts
    - src/authority/validate-bundle.ts
    - src/commands/import-authority.ts (if it exists)
    - tests/authority/bundle.test.ts
    - tests/authority/fixtures/README.md
  </read_first>
  <files>package.json, src/commands/import-authority.ts, tests/authority/bundle.test.ts, tests/authority/fixtures/bundle-input/sources.json, tests/authority/fixtures/bundle-input/formats.json, tests/authority/fixtures/bundle-input/cards.raw.json, tests/authority/fixtures/bundle-input/input-lock.json</files>
  <behavior>
    - Approved local source metadata, format data, and card bytes build `bundle.json`, `sources.json`, `formats.json`, and `cards.normalized.json` in a new revision.
    - The importer canonicalizes and verifies the reviewed input lock, exact relative file set, every raw byte hash, and independently supplied input-root hash before parsing or normalization.
    - Two clean builds from identical bytes/metadata but different temporary roots produce byte-identical final files and bundle hashes.
    - Existing target revision, invalid input, validation failure, or simulated write failure returns non-zero and leaves no published partial revision or modified existing revision.
    - Restricted storage policy blocks prohibited raw/PDF/image bytes before publication.
  </behavior>
  <action>Replace the input-lock/build/rebuild/write-once/atomic-failure integration todos, run them red, then implement `import-authority.ts` as the imperative filesystem shell around the pure modules. Parse only the documented arguments, resolve all paths through Plan 04 confinement, validate the canonical input lock against the independently supplied input-root hash, require its exact relative file set, and hash every supplied raw file before parsing; any missing, extra, or mismatched byte aborts. Then strict-parse, normalize, canonicalize, and write canonical JSON into a same-parent temporary revision directory. Emit the verified input-root hash and newly calculated bundle root hash to stdout before recursive validation so callers never need to trust the candidate bundle's own hash field. For stored-source records, copy only explicitly approved bytes to their fixed confined paths so offline validation can rehash them. For manifest-only records, bind the import-time raw hash, durable locator/acquisition-procedure fingerprint, derivation, and source refs but publish no raw bytes; later offline validation verifies that canonical binding rather than claiming absent bytes were rehashed. Recursively validate the temporary bundle using the calculated root hash before one atomic directory rename. Refuse any existing target and clean only the exact verified temporary directory after failure. Add `authority:import` to `package.json`. Do not call `fetch`, accept URLs as input paths, retrieve official data, overwrite a revision, publish unapproved restricted bytes, include wall-clock data unless supplied in source metadata, or use Docker/Podman. Commit only after the targeted test and typecheck pass.</action>
  <verify>
    <automated>node --test --test-name-pattern="input|root|build|byte-identical|write-once|atomic|failure|storage" tests/authority/bundle.test.ts &amp;&amp; pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Successful manifest-only builds publish exactly four canonical JSON files; stored-source builds additionally publish only the fixed byte paths explicitly allowed by permission, and both validate before rename.
    - Import rejects a changed input-lock hash, missing/extra input, or one-byte raw mismatch before generating normalized output, and stdout reports the verified input-root hash plus calculated bundle root hash.
    - Two clean builds are byte-identical; an overwrite or injected failure preserves the original target and leaves no published partial revision.
    - Tests prove no prohibited media/raw corpus is published and global network access can throw without affecting the build.
  </acceptance_criteria>
  <done>DATA-01/02 authority revisions build deterministically and atomically from local approved inputs only.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Provide deterministic offline validation command</name>
  <read_first>
    - .planning/REQUIREMENTS.md
    - .planning/phases/01-rules-and-data-authority/01-VALIDATION.md
    - package.json
    - src/authority/validate-bundle.ts
    - src/commands/import-authority.ts
    - src/commands/validate-authority.ts (if it exists)
    - tests/authority/bundle.test.ts
  </read_first>
  <files>package.json, src/commands/validate-authority.ts, tests/authority/bundle.test.ts</files>
  <behavior>
    - A valid selected local bundle ID/hash prints a deterministic success record and exits zero; stored bytes are rehashed while manifest-only records report canonical binding validation without raw-byte revalidation.
    - Invalid arguments, missing files, tampering, ambiguity/unsupported authority, restricted storage, and ID/hash mismatch print sorted diagnostics and exit non-zero.
    - The command does not fetch, repair, rewrite, drop data, or mutate the validated revision.
    - Full tests pass with network calls disabled and every Wave 0 bundle todo replaced.
  </behavior>
  <action>Replace the remaining validate-command/offline/stored-tamper/manifest-binding-tamper/ambiguity todos, run them red, then implement `validate-authority.ts` as a thin adapter: parse the exact command contract, call `validateAuthorityBundle`, print canonical success JSON that distinguishes `stored-bytes-rehashed` from `manifest-binding-verified`, or deterministic stderr diagnostics, and set the correct exit status. Add `authority:validate` to `package.json`. Keep validation read-only and local; do not accept an external raw input directory, claim absent bytes were reverified, add acquisition, automatically repair, use best-effort behavior, or add a general human-play CLI. Finish `bundle.test.ts` with no todo/skip and commit only after the targeted test, typecheck, lint, and full package tests pass.</action>
  <verify>
    <automated>node --test tests/authority/bundle.test.ts &amp;&amp; pnpm typecheck &amp;&amp; pnpm lint &amp;&amp; pnpm test</automated>
  </verify>
  <acceptance_criteria>
    - Valid command invocation exits 0 with stable machine-readable bundle ID/hash output.
    - Every invalid/tampered/ambiguous/unsupported/prohibited case exits non-zero, prints sorted exact diagnostics, and leaves files byte-identical to their pre-validation state.
    - `tests/authority/bundle.test.ts` contains no todo/skip and the full suite passes with network access unavailable.
  </acceptance_criteria>
  <done>A developer can build and validate immutable authority revisions through documented local automation commands, and invalid bundles cannot enter runtime consumers.</done>
</task>

</tasks>

<must_haves>
- DATA-01 is implemented by an atomic write-once builder and recursive offline validator.
- DATA-02 is implemented by rebuilding normalized cards from an independently expected, exact durable input lock with no game/experiment network access.
- D-03/D-04/D-07 make bundle selection ID/hash-bound, immutable, deterministic, and fail-closed.
- D-08 keeps restricted upstream bytes out of published revisions until the explicit permission gate passes.
</must_haves>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| command arguments/local input tree -> authority library | Malicious arguments/files can trigger traversal, DoS, or substitution. |
| validated temporary revision -> published immutable revision | Partial or tampered output must not become visible. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-21 | Tampering / DoS | command input JSON | mitigate | Reuse strict bounded schemas and graph caps; read only explicitly named files; return bounded sorted diagnostics. |
| T-01-22 | Tampering / Elevation | input/output paths | mitigate | Reuse realpath confinement, reject absolute/traversal/symlink escape, and delete only the verified temp target on failure. |
| T-01-23 | Spoofing | local source bytes | mitigate | Bind reviewed source metadata to exact raw hashes and require Plan 06 authenticity/permission checkpoint before official publication. |
| T-01-24 | Tampering / Repudiation | temporary/published artifacts | mitigate | Revalidate expected bundle ID/hash before atomic rename; refuse overwrite; emit deterministic machine-readable result. |
| T-01-25 | Information disclosure / legal | bundle publisher | mitigate | Storage policy blocks PDFs/images/unapproved raw corpora; commands contain no acquisition or bypass flag. |
| T-01-SC | Tampering | package installs | mitigate | Use Plan 01's locked approved packages; add no CLI/filesystem library. |
</threat_model>

<verification>
Run `pnpm typecheck && pnpm lint && pnpm test`. In tests, deny network access, compare two clean build directories byte-for-byte, inject a mid-write failure, retry an existing revision, tamper each artifact/reference class, and assert deterministic exit status/output.
</verification>

<success_criteria>
- Local approved inputs build a fully validated immutable revision through one documented command.
- Failed/invalid/duplicate builds never modify or partially publish a revision.
- The validation command is read-only, offline, ID/hash-bound, deterministically diagnostic, and non-zero for every invalid state.
</success_criteria>

<output>
Create `.planning/phases/01-rules-and-data-authority/01-05-SUMMARY.md` when done.
</output>
