---
phase: 01-rules-and-data-authority
plan: "01"
type: execute
wave: 0
depends_on: []
files_modified:
  - package.json
  - pnpm-lock.yaml
  - tsconfig.json
  - eslint.config.js
  - tests/authority/canonical-json.test.ts
  - tests/authority/provenance.test.ts
  - tests/authority/card-snapshot.test.ts
  - tests/authority/bundle.test.ts
  - tests/authority/fixtures/README.md
autonomous: true
user_setup:
  - service: "Local Node toolchain"
    why: "The research probe found Node 22.22.2 and pnpm 9.15.9, but native TypeScript verification is pinned to Node 24.19.0 and pnpm 11.22.0."
    dashboard_config:
      - task: "Install/activate Node.js 24.19.0 and pnpm 11.22.0 before Wave 0; stop before repository edits if the exact versions are unavailable."
        location: "Local development environment"
requirements: [DATA-01, DATA-02, DATA-03]
must_haves:
  truths:
    - "The repository is one pinned Node 24.19.0, TypeScript 6.0.3, pnpm 11.22.0 package per D-10."
    - "Every Phase 1 requirement has a discoverable node:test contract before dependent implementation begins."
    - "Only synthetic or minimal factual fixtures are permitted before the D-08/D-09 permission review."
  artifacts:
    - path: "package.json"
      provides: "Pinned package manager, runtime constraints, and verification scripts"
      contains: "pnpm@11.22.0"
    - path: "pnpm-lock.yaml"
      provides: "Reproducible audited dependency graph"
    - path: "tests/authority/bundle.test.ts"
      provides: "DATA-01 executable test contract"
    - path: "tests/authority/card-snapshot.test.ts"
      provides: "DATA-02 executable test contract"
    - path: "tests/authority/canonical-json.test.ts"
      provides: "DATA-03 canonical identity test contract"
  key_links:
    - from: "package.json"
      to: "tests/authority/*.test.ts"
      via: "pnpm test script"
      pattern: "node --test tests/authority/\\*.test.ts"
---

<objective>
Establish the pinned one-package TypeScript toolchain and the complete Phase 1 test contract.

Purpose: Give every later authority task a fast, deterministic verification command and an explicit Nyquist test destination without adding runtime, UI, database, or container scaffolding.
Output: Package/tooling configuration, audited lockfile, four node:test contract files, and the license-safe fixture policy.
</objective>

<execution_context>
@C:/Users/Dad/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Dad/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-rules-and-data-authority/01-CONTEXT.md
@.planning/phases/01-rules-and-data-authority/01-RESEARCH.md
@.planning/phases/01-rules-and-data-authority/01-VALIDATION.md
@.planning/phases/01-rules-and-data-authority/01-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pin the native TypeScript toolchain</name>
  <read_first>
    - AGENTS.md
    - .planning/phases/01-rules-and-data-authority/01-RESEARCH.md
    - .planning/phases/01-rules-and-data-authority/01-VALIDATION.md
    - package.json (if created by another process before execution)
    - tsconfig.json (if created by another process before execution)
  </read_first>
  <files>package.json, pnpm-lock.yaml, tsconfig.json, eslint.config.js</files>
  <action>Create one private ESM package with `packageManager: pnpm@11.22.0`, Node `24.19.0` engine enforcement, scripts `typecheck`, `lint`, `test`, and `verify`, production dependency `zod@4.4.3`, and dev dependencies `typescript@6.0.3`, `@types/node@24.1.0`, `eslint@10.8.1`, and `typescript-eslint@8.67.0`. Configure NodeNext, strict/noEmit, `erasableSyntaxOnly`, `verbatimModuleSyntax`, `rewriteRelativeImportExtensions`, `noUncheckedIndexedAccess`, and `exactOptionalPropertyTypes`. Use a small flat ESLint configuration over `src/**/*.ts` and `tests/**/*.ts`; do not add a test runner, transpiler, workspace, Python, container, GUI, engine, or unused dependency per D-10. Generate `pnpm-lock.yaml` with pnpm rather than hand-editing it. The package legitimacy audit in `01-RESEARCH.md` approves every install; no [ASSUMED], [SUS], or [SLOP] package is present. Commit this task only after its automated verification succeeds.</action>
  <verify>
    <automated>node -e "if(process.versions.node!=='24.19.0')process.exit(1)" &amp;&amp; pnpm --version | node -e "let s='';process.stdin.on('data',d=&gt;s+=d).on('end',()=&gt;{if(s.trim()!=='11.22.0')process.exit(1)})" &amp;&amp; pnpm install --frozen-lockfile &amp;&amp; pnpm exec tsc --version &amp;&amp; pnpm exec eslint --version</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` contains exactly the researched Phase 1 dependency set and no `pure-rand`, browser, database, test-runner, or container dependency.
    - `pnpm-lock.yaml` resolves `zod` 4.4.3, TypeScript 6.0.3, Node types 24.1.0, ESLint 10.8.1, and typescript-eslint 8.67.0.
    - Node 24.19.0 and pnpm 11.22.0 are enforced before any source task runs.
  </acceptance_criteria>
  <done>The pinned package installs from its lockfile and exposes the exact typecheck, lint, test, and aggregate verification commands required by `01-VALIDATION.md`.</done>
</task>

<task type="auto">
  <name>Task 2: Establish Phase 1 test contracts and fixture policy</name>
  <read_first>
    - .planning/REQUIREMENTS.md
    - .planning/phases/01-rules-and-data-authority/01-CONTEXT.md
    - .planning/phases/01-rules-and-data-authority/01-VALIDATION.md
    - tests/authority/canonical-json.test.ts (if it exists)
    - tests/authority/provenance.test.ts (if it exists)
    - tests/authority/card-snapshot.test.ts (if it exists)
    - tests/authority/bundle.test.ts (if it exists)
  </read_first>
  <files>tests/authority/canonical-json.test.ts, tests/authority/provenance.test.ts, tests/authority/card-snapshot.test.ts, tests/authority/bundle.test.ts, tests/authority/fixtures/README.md</files>
  <action>Create node:test files containing named `test.todo` contracts for every case in `01-VALIDATION.md`: RFC-derived canonicalization/rejection vectors; strict provenance, tamper, cycle, and path confinement; deterministic card normalization with malformed/duplicate/reordered inputs; and offline current/superseded/ambiguous/prohibited-media/write-once/atomic-failure bundle behavior. Create a fixture policy stating that fixtures must be compact, synthetic or minimal facts, independently authored, deterministic, and must not contain publisher PDFs/images/raw corpora or copied GPL/unlicensed project material per D-08 and D-09. These are Wave 0 executable contracts, not reduced acceptance criteria: each dependent plan must replace its relevant todos with passing assertions. Commit only these files after the discovery command passes.</action>
  <verify>
    <automated>node --test tests/authority/*.test.ts &amp;&amp; node -e "const fs=require('node:fs');for(const f of ['canonical-json','provenance','card-snapshot','bundle'])if(!fs.readFileSync('tests/authority/'+f+'.test.ts','utf8').includes('test.todo'))process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - The four exact test files listed in `01-VALIDATION.md` are discovered by Node and exit zero.
    - Their todo names cover canonicalization vectors, strict paths, deterministic normalization, offline bundle validation, immutability/atomic failure, reference cycles/tampering, storage policy, and clean-room guardrails.
    - `tests/authority/fixtures/README.md` forbids restricted publisher bytes and copied reference-project code/tests/assets/data.
  </acceptance_criteria>
  <done>All Wave 0 test paths exist, are executable, and define the full DATA-01/02/03 contract that later plans must turn green.</done>
</task>

</tasks>

<must_haves>
- DATA-01 has an executable bundle contract covering immutability, precedence, offline validation, and storage policy.
- DATA-02 has an executable deterministic normalization contract with no network dependency.
- DATA-03 has executable canonical identity and provenance contracts.
- D-08, D-09, and D-10 constrain fixtures, dependencies, and repository shape from the first implementation commit.
</must_haves>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry/lockfile -> repository | Third-party package metadata and bytes enter the build. |
| future untrusted JSON -> test contracts | Malicious authority inputs are specified before parsers exist. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering / DoS | authority JSON inputs | mitigate | Wave 0 contract requires byte, record, nesting, and diagnostic-count bounds; Plans 02-04 implement them. |
| T-01-02 | Tampering | filesystem paths/symlinks | mitigate | Provenance contract requires traversal, absolute-path, and symlink-escape rejection; Plan 04 implements it. |
| T-01-03 | Spoofing | official source identity | mitigate | Bundle contract requires reviewed HTTPS URLs plus exact byte hashes; hashes are never described as publisher authentication. |
| T-01-04 | Tampering | artifact/reference graph | mitigate | Provenance contract requires recomputed hashes, missing-reference rejection, and cycle detection. |
| T-01-05 | Information disclosure / legal | publisher and reference assets | mitigate | Fixture policy rejects PDFs, images, raw corpora, and copied GPL/unlicensed material before any builder exists. |
| T-01-SC | Tampering | npm installs | mitigate | Use only packages marked Approved by the research Package Legitimacy Audit and commit the exact pnpm lockfile. |
</threat_model>

<verification>
Run `node --test tests/authority/*.test.ts`. Confirm four files are discovered, every required behavior is named, the dependency lock is exact, and no production/runtime subsystem beyond Phase 1 tooling was scaffolded.
</verification>

<success_criteria>
- Node 24.19.0, pnpm 11.22.0, TypeScript 6.0.3, and the audited dependencies are pinned reproducibly.
- All four Nyquist test files and the license-safe fixture policy exist before dependent plans execute.
- The plan completes in two atomic, verified commits and leaves no network, engine, simulator, model, optimizer, GUI, database, or container code.
</success_criteria>

<output>
Create `.planning/phases/01-rules-and-data-authority/01-01-SUMMARY.md` when done.
</output>
