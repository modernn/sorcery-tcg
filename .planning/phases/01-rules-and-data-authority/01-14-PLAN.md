---
phase: 01-rules-and-data-authority
plan: "14"
type: execute
wave: 8
depends_on: ["01-11"]
files_modified:
  - scripts/collect-private-authority.ps1
  - tests/authority/private-authority-collector.test.ts
  - tests/private-authority/private-source-completeness.test.ts
autonomous: true
requirements: [DATA-01]
gap_closure: true
must_haves:
  truths:
    - "The executed Plan 11 source-specific completeness checks are exposed through one reusable offline verifier without changing collector acquisition behavior."
    - "The reusable verifier checks both already-acquired primary and backup seven-file roots from the consumed lock before Plan 13 can build a replacement revision."
    - "Offline existing-root verification invokes no transport or publication path and leaves both roots byte-for-byte unchanged."
    - "Success and failure output remains fixed and content-free; private roots, locators, bytes, and lock fields never cross stdout or stderr."
  artifacts:
    - path: "scripts/collect-private-authority.ps1"
      provides: "Reusable local content-set verifier plus a mutually exclusive read-only existing-roots mode"
    - path: "tests/authority/private-authority-collector.test.ts"
      provides: "Synthetic routing, no-transport, failure, and sanitized-output regressions for offline mode"
    - path: "tests/private-authority/private-source-completeness.test.ts"
      provides: "Opt-in proof that both real existing roots pass the final completeness contract without mutation"
  key_links:
    - from: "scripts/collect-private-authority.ps1"
      to: "src/authority/official-card-api-adapter.ts"
      via: "shared bounded Node bridge validates the exact 1,100-card official shape for staged or existing local bytes"
      pattern: "adaptOfficialCardApiSnapshot"
    - from: ".local/authority/locks/official-2026-08-20/source-set-lock.json"
      to: "scripts/collect-private-authority.ps1 -VerifyExistingRoots"
      via: "consumed lock resolves the primary and backup roots for read-only verification only"
      pattern: "VerifyExistingRoots"
    - from: "tests/private-authority/private-source-completeness.test.ts"
      to: "scripts/collect-private-authority.ps1"
      via: "captures before/after maps and invokes the same production content verifier for both existing roots"
      pattern: "Assert-PrivateAuthorityContentSet"
---

<objective>
Apply Plan 11's strengthened content checks to both already-acquired private roots without falsifying Plan 11's completed summary.

Purpose: Close the safe-resume gap. Plan 11 executed and summarized synthetic collector hardening only; this separate incomplete plan owns the new reusable offline verification required before the final-contract rebuild.
Output: A reusable local content verifier, a no-transport existing-roots entry path, synthetic routing regressions, and an opt-in immutable dual-root proof.
</objective>

<execution_context>
@C:/Users/Dad/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Dad/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-rules-and-data-authority/01-CONTEXT.md
@.planning/phases/01-rules-and-data-authority/01-RESEARCH.md
@.planning/phases/01-rules-and-data-authority/01-VALIDATION.md
@.planning/phases/01-rules-and-data-authority/01-REVIEW.md
@.planning/phases/01-rules-and-data-authority/01-11-PLAN.md
@.planning/phases/01-rules-and-data-authority/01-11-SUMMARY.md
@scripts/collect-private-authority.ps1
@src/authority/official-card-api-adapter.ts
@src/authority/private-source-set.ts
@tests/authority/private-authority-collector.test.ts
@tests/private-authority/private-revision.test.ts
@data/authority/README.md

<interfaces>
Executed Plan 11 contract:
- `Get-NormalizedVisibleText`, `Assert-HtmlSource`, `Assert-CardJsonSource`, and `Get-ChangelogDate` enforce the fixed source-specific acceptance rules.
- `Invoke-PrivateAuthorityCollectionForLoopbackTest` is the synthetic collector seam.
- Production direct output is fixed and sanitized; the complete lock stays internal.

Existing evidence contract:
- `.local/authority/locks/official-2026-08-20/source-set-lock.json` records exactly seven entries plus primary and independent backup roots.
- `verifyPrivateSourceSet` rehashes and binds those roots structurally but does not apply Plan 11's strengthened HTML/card content checks.
- Both one-shot authorization records are consumed. This plan may read existing bytes only; it grants no reacquisition, retry, or broader-use authority.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract one reusable offline content verifier</name>
  <read_first>
    - .planning/phases/01-rules-and-data-authority/01-11-PLAN.md
    - .planning/phases/01-rules-and-data-authority/01-11-SUMMARY.md
    - scripts/collect-private-authority.ps1
    - src/authority/official-card-api-adapter.ts
    - src/authority/private-source-set.ts
    - tests/authority/private-authority-collector.test.ts
  </read_first>
  <files>scripts/collect-private-authority.ps1, tests/authority/private-authority-collector.test.ts</files>
  <behavior>
    - One `Assert-PrivateAuthorityContentSet` function accepts a local root plus the fixed seven descriptors and applies the exact executed Plan 11 HTML/date/card checks.
    - The ordinary collector calls that function on staged bytes before publication; all existing collector tests remain green without semantic drift.
    - A mutually exclusive `-VerifyExistingRoots -LockPath &lt;path&gt;` mode returns before request construction, transport, destination creation, copy/move/quarantine, or lock writing.
    - Offline-mode success and failure emit only fixed messages and nonzero status as appropriate; no private value is serialized.
  </behavior>
  <action>Write RED synthetic tests for reusable local-root verification, offline-mode routing, a throwing transport seam, incomplete primary/backup fixtures, and canary output. Refactor the already-executed source-specific visible-HTML, first-entry semantic date, fixed marker, strict official-card adapter, and exact 1,100-row checks behind one `Assert-PrivateAuthorityContentSet` function. Preserve the collector's existing staged-byte call by routing it through that function. Add the exact mutually exclusive parameter path `-VerifyExistingRoots -LockPath &lt;path&gt;`; load and schema-check the consumed lock, call `verifyPrivateSourceSet`, then apply the shared content verifier independently to its primary and backup roots. Branch into this path before any request/transport/publication setup and emit one fixed content-free result. Keep all executed Plan 11 bounds, ambiguity failures, no-retry behavior, atomic publication, sanitized errors, and child deadlines unchanged. Use PowerShell/.NET, Node, and existing project helpers only; do not add a dependency, inspect a live endpoint, reacquire, repair, mutate, or infer renewed authorization.</action>
  <verify>
    <automated>node --test --test-name-pattern="offline mode|local root|no transport|incomplete existing|sanitized" tests/authority/private-authority-collector.test.ts &amp;&amp; pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Normal collection and offline verification invoke the same source-specific content function.
    - Synthetic offline mode proves the transport seam is never called and no publication-owned path is created or changed.
    - All preexisting Plan 11 tests remain green and production output remains fixed/content-free.
    - No network, acquisition, retry, mutation, dependency, or broader-use path is introduced.
  </acceptance_criteria>
  <done>The executed completeness contract is reusable over local bytes without rewriting Plan 11's historical execution contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove both existing roots complete and immutable offline</name>
  <read_first>
    - scripts/collect-private-authority.ps1
    - src/authority/private-source-set.ts
    - tests/private-authority/private-revision.test.ts
    - data/authority/README.md
  </read_first>
  <files>tests/private-authority/private-source-completeness.test.ts</files>
  <behavior>
    - The consumed lock resolves exactly one primary and one independent backup root, each with the same seven locked entries.
    - Both roots pass `verifyPrivateSourceSet` and the shared Plan 11 content verifier before Plan 13 begins.
    - Sorted relative-path/length/SHA-256 maps for each root are identical before and after verification.
    - Fetch/HTTP/HTTPS/net and collector transport sentinels throw if called; fixed test output contains no private root, locator, excerpt, or lock field.
  </behavior>
  <action>Create one opt-in private test that reads the existing consumed lock using the same private-test configuration pattern as `private-revision.test.ts`. Capture bounded sorted relative-path/length/SHA-256 maps for primary and backup, install throwing fetch/HTTP/HTTPS/net and collector transport sentinels, invoke `collect-private-authority.ps1 -VerifyExistingRoots -LockPath &lt;existing-lock&gt;`, and require fixed success output. Independently call `verifyPrivateSourceSet`, require exactly seven entries and the locked source-set root, then recapture both maps and require byte-for-byte equality with their precheck maps. Add sanitized negative probes for a temporary incomplete local root and a copied lock with one mismatched entry; never print the real lock or resolved roots. Fail rather than skip when the expected private lock/roots are unavailable because this test is part of the explicit private release gate.</action>
  <verify>
    <automated>node --test --test-name-pattern="existing primary and backup completeness" tests/private-authority/private-source-completeness.test.ts &amp;&amp; pnpm verify</automated>
  </verify>
  <acceptance_criteria>
    - Both already-acquired roots pass the strengthened production content and locked-byte contracts offline.
    - Primary and backup before/after path/length/hash maps are exactly unchanged.
    - Network and collector transport sentinels are not invoked.
    - Missing, partial, mismatched, or mutated evidence fails with fixed content-free diagnostics.
  </acceptance_criteria>
  <done>DATA-01 has an explicit unexecuted-to-executed gate proving the existing evidence is complete under the final content contract before Plan 13 consumes it.</done>
</task>

</tasks>

<gap_coverage>
- Safe-resume blocker: Plan 11 remains historical and complete; all post-summary offline verification work is isolated here with no SUMMARY until execution.
- Plan-checker requirement-coverage blocker: both already-acquired roots receive the strengthened reusable checks before Plan 13.
</gap_coverage>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| executed collector checks -> reusable local verifier | Refactoring could weaken the already-green acquisition contract or create divergent acceptance logic. |
| consumed lock -> existing roots | Previously acquired bytes could be incomplete, substituted, linked, or mutated while still bypassing new content checks. |
| offline verifier -> transport/publication | A misrouted mode could reacquire, retry, mutate evidence, or disclose private values. |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan |
|-----------|----------|----------|-----------|-------------|-----------------|
| T-01-62 | Tampering | high | shared content verifier | mitigate (blocking) | One function serves collector and offline paths; all Plan 11 regressions plus routing tests must pass. |
| T-01-63 | Spoofing / Tampering | high | existing primary/backup roots | mitigate (blocking) | Structural rehash, shared content verification, and immutable before/after maps for both roots. |
| T-01-64 | Elevation / legal boundary | high | offline mode | mitigate (blocking) | Early no-transport/no-publication branch, consumed evidence only, throwing sentinels, no renewed permission. |
| T-01-65 | Information disclosure | high | output/errors | mitigate (blocking) | Fixed content-free output and canary assertions; never serialize lock/root/locator/content. |
| T-01-SC | Tampering | low | package installs | accept | No dependency is added; existing platform and project helpers suffice. |
</threat_model>

<verification>
Run the focused synthetic collector routing tests, the opt-in existing-root completeness test, and `pnpm verify`. Confirm both existing roots pass without network or mutation, the normal collector remains behaviorally unchanged, and no private value appears in output.
</verification>

<success_criteria>
- Plan 11's executed checks are reused, not duplicated or retroactively redefined.
- Both existing seven-file roots pass the final content and byte contracts offline.
- Verification changes neither root and invokes no acquisition/publication path.
- Plan 14 remains incomplete until its own `01-14-SUMMARY.md` is created by execution.
</success_criteria>

<output>
Create `.planning/phases/01-rules-and-data-authority/01-14-SUMMARY.md` when done. Do not modify or replace `01-11-SUMMARY.md`.
</output>
