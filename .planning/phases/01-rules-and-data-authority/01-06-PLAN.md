---
phase: 01-rules-and-data-authority
plan: "06"
type: execute
wave: 4
depends_on: [01-05]
files_modified:
  - docs/authority-precedence.md
  - docs/external-reuse-policy.md
autonomous: true
requirements: [DATA-01, DATA-03]
must_haves:
  truths:
    - "Official precedence is explicit, effective-dated, scoped, and fail-closed per D-01/D-02."
    - "Publisher material and external simulator code remain behind documented storage, permission, and clean-room review gates per D-08/D-09."
    - "No policy text claims that a URL or SHA-256 proves publisher authenticity or grants redistribution rights."
    - "The permission policy requires one durable exact-byte input path and canonical input lock before DATA-02 publication can proceed."
  artifacts:
    - path: "docs/authority-precedence.md"
      provides: "Reviewable official-source precedence and ambiguity policy"
    - path: "docs/external-reuse-policy.md"
      provides: "Publisher storage, permission, clean-room, provenance, and reviewer-signoff policy"
  key_links:
    - from: "docs/authority-precedence.md"
      to: "src/authority/validate-bundle.ts"
      via: "documents the exact D-01/D-02 order enforced by resolveAuthorityPrecedence"
      pattern: "unsupported"
    - from: "docs/external-reuse-policy.md"
      to: "src/commands/import-authority.ts"
      via: "defines which local source/derivative inputs may be published"
      pattern: "permission-required"
---

<objective>
Publish the official precedence and clean-room/reuse policies that govern authority artifacts.

Purpose: Make source authority, ambiguity, storage, external-reference reuse, and human approval rules auditable before any official corpus or derivative is committed.
Output: Two concise policy documents linked to the implemented validator and the following permission checkpoint.
</objective>

<execution_context>
@C:/Users/Dad/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Dad/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-rules-and-data-authority/01-CONTEXT.md
@.planning/phases/01-rules-and-data-authority/01-RESEARCH.md
@.planning/phases/01-rules-and-data-authority/01-VALIDATION.md
@.planning/phases/01-rules-and-data-authority/01-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document normative authority and precedence</name>
  <read_first>
    - .planning/REQUIREMENTS.md
    - .planning/phases/01-rules-and-data-authority/01-CONTEXT.md
    - .planning/phases/01-rules-and-data-authority/01-RESEARCH.md
    - src/authority/validate-bundle.ts
    - docs/authority-precedence.md (if it exists)
  </read_first>
  <files>docs/authority-precedence.md</files>
  <action>Create `docs/authority-precedence.md` implementing D-01/D-02 exactly: official rulebooks, official format rules, official Codex/FAQ, official card updates, and official card data are normative; community sources are provenance/examples only. Document explicit official supersession/reversal and effective date first, current card-specific updates for that card, explicitly selected scoped overlays only within scope, compatible rulebook/Codex/FAQ detail, retention of winning/superseded/contending refs, and `unsupported` for equal-rank, unclear-date, unclear-scope, or unresolved conflict. State this is the project fail-closed policy rather than a claim that the publisher issued a complete hierarchy. Cross-reference the exact validator function and test cases. Do not embed copyrighted rule text or infer missing rulings. Commit only after the automated policy check and full tests pass.</action>
  <verify>
    <automated>node -e "const s=require('node:fs').readFileSync('docs/authority-precedence.md','utf8').toLowerCase();for(const x of ['official rulebook','format','codex','faq','card update','effective date','scoped','superseded','contending','unsupported','community'])if(!s.includes(x))process.exit(1)" &amp;&amp; pnpm test</automated>
  </verify>
  <acceptance_criteria>
    - All five normative official source classes and community non-authority are named exactly.
    - The documented ordering matches the current/superseded/scoped/ambiguous assertions in `bundle.test.ts`.
    - Every unresolved conflict is `unsupported`; no guessed or silently flattened outcome is described.
  </acceptance_criteria>
  <done>D-01/D-02 precedence is explicit, source-linked, implementation-linked, and fail-closed.</done>
</task>

<task type="auto">
  <name>Task 2: Document storage permission and clean-room reuse gates</name>
  <read_first>
    - AGENTS.md
    - .planning/phases/01-rules-and-data-authority/01-CONTEXT.md
    - .planning/phases/01-rules-and-data-authority/01-RESEARCH.md
    - tests/authority/fixtures/README.md
    - docs/external-reuse-policy.md (if it exists)
  </read_first>
  <files>docs/external-reuse-policy.md</files>
  <action>Create `docs/external-reuse-policy.md` with D-08/D-09's exact boundary. Record the audited Contested Realms GPL-3.0 revision and spells.bar/playtest no-reusable-license revision; allow only independently reimplemented behavioral/UX observations unless an explicit product license decision or written permission changes the boundary. Forbid copied code, tests, card implementations, assets, and data. Set publisher PDFs, images, raw API/database corpora, automated API retrieval, and normalized derivatives to `permission-required`; URL/hash manifests are allowed by default. Include reviewer, review date, source/revision, allowed retrieval/caching/redistribution/image use, attribution, revocation/update expectations, and independent-implementation attestation fields, with initial status `permission-required`. Add a durable-input section that must remain unapproved until Plan 07 selects exactly one of: permitted fixed repository raw bytes, an immutable approved external archive/custodian, or an approved acquisition procedure that refuses mismatched bytes. Require the selected record to include non-confidential locator/procedure, exact relative-file list, per-file byte counts/SHA-256 values, canonical input-root SHA-256, custodian/revision, and access/update expectations. State that a mutable executor-local directory is not a durable source, official sources alone are normative, no network acquisition path exists yet, hashes prove integrity only, and confidential permission correspondence stays outside git. Commit only after automated policy assertions and `pnpm verify` pass.</action>
  <verify>
    <automated>node -e "const s=require('node:fs').readFileSync('docs/external-reuse-policy.md','utf8').toLowerCase();for(const x of ['permission-required','gpl-3.0','no reusable license','code','tests','assets','data','pdf','image','raw api','automated','normalized derivative','reviewer','attribution','revocation','independent','durable input','input-root','immutable','acquisition procedure','mismatch'])if(!s.includes(x))process.exit(1)" &amp;&amp; pnpm verify</automated>
  </verify>
  <acceptance_criteria>
    - Audited reference revisions, forbidden copied material, and clean-room attestation fields are present.
    - Publisher automation/storage/derivative categories remain `permission-required` and URL/hash-only manifests remain the default.
    - The policy rejects an executor-local directory as durable evidence and lists every field required for one exact-byte repository/archive/procedure input lock.
    - No confidential permission document, restricted artifact, automated retrieval tool, or legal conclusion beyond the research is added.
  </acceptance_criteria>
  <done>D-08/D-09 storage, durable-input, and clean-room boundaries are auditable and ready to block official publication until a human supplies written scope and one exact-byte rebuild lock.</done>
</task>

</tasks>

<must_haves>
- DATA-01's official-source inventory and precedence policy are reviewable without bundling protected rulebook bytes.
- DATA-03 provenance includes a clean-room/reviewer record for any external behavioral reference or publisher-derived artifact.
- D-01/D-02/D-08/D-09 are fully documented with no deferred or permissive branch.
</must_haves>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| research/source claims -> repository policy | Inaccurate source status could authorize substituted or restricted data. |
| external reference observation -> implementation | GPL or unlicensed material can contaminate code/tests/assets/data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-26 | Tampering / DoS | malicious JSON | accept | This plan writes no parser; Plans 02-05 already implement bounded strict parsing and tests remain part of `pnpm verify`. |
| T-01-27 | Tampering | path traversal/symlink escape | accept | This plan performs no authority file import; the documented pipeline points only to the Plan 04/05 confined commands. |
| T-01-28 | Spoofing | official source claims | mitigate | Policy requires official HTTPS identity, reviewed acquisition metadata, effective date/scope, and byte hash while disclaiming hash-based authenticity. |
| T-01-29 | Tampering | artifact/reference provenance | mitigate | Policy requires immutable IDs/hashes/refs and directs all artifacts through the recursive validator. |
| T-01-30 | Information disclosure / legal | publisher/reference publication | mitigate | Explicit permission-required defaults, forbidden-copy list, audited revisions, durable input-lock fields, reviewer fields, and separate blocking checkpoint. |
| T-01-SC | Tampering | package installs | accept | Documentation adds no package; full locked verification still runs. |
</threat_model>

<verification>
Run the exact document assertions and `pnpm verify`. Compare the documented precedence to `resolveAuthorityPrecedence` tests and confirm both policy files keep publisher automation/derivatives and external source copying closed pending the next plan's human checkpoint.
</verification>

<success_criteria>
- The authoritative source order, effective-date/scope rules, and unsupported ambiguity behavior are explicit and match code/tests.
- The publisher storage/automation, durable exact-byte input, and GPL/no-license clean-room boundaries contain all reviewer/signoff fields required by research.
- No protected source bytes, normalized official derivative, copied external material, or acquisition code is introduced.
</success_criteria>

<output>
Create `.planning/phases/01-rules-and-data-authority/01-06-SUMMARY.md` when done.
</output>
