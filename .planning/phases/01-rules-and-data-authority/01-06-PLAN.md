---
phase: 01-rules-and-data-authority
plan: "06"
type: execute
wave: 4
depends_on: [01-05]
files_modified:
  - .gitignore
  - docs/authority-precedence.md
  - docs/external-reuse-policy.md
autonomous: true
requirements: [DATA-01, DATA-03]
must_haves:
  truths:
    - "Official precedence is explicit, effective-dated, scoped, and fail-closed per D-01/D-02."
    - "The only remaining acquisition path is one manually saved complete official source set with independent private backup, entirely Git-ignored, per amended D-08."
    - "All official raw input, normalized data, and built authority revisions remain private/local and cannot be committed or packaged."
    - "External simulator implementations and community card corpora remain behavioral reference only per D-09; no community repository is treated as a licensed gameplay corpus."
    - "Sharing, release with content, automated updating, commercialization, or a public/network card API is blocked pending written publisher permission and a separate plan per D-11."
  artifacts:
    - path: ".gitignore"
      provides: "Repository exclusion for every private authority input, normalized snapshot, and built revision"
      contains: ".local/authority/"
    - path: "docs/authority-precedence.md"
      provides: "Reviewable official-source precedence and ambiguity policy"
    - path: "docs/external-reuse-policy.md"
      provides: "Private-local storage, community-license audit, clean-room, and future-permission policy"
  key_links:
    - from: "docs/authority-precedence.md"
      to: "src/authority/validate-bundle.ts"
      via: "documents the exact D-01/D-02 order enforced by resolveAuthorityPrecedence"
      pattern: "unsupported"
    - from: ".gitignore"
      to: ".local/authority/"
      via: "prevents official corpus bytes and local revisions entering Git"
      pattern: "^\\.local/authority/$"
    - from: "docs/external-reuse-policy.md"
      to: ".planning/phases/01-rules-and-data-authority/01-07-PLAN.md"
      via: "defines the exact private-local attestation and backup evidence required before the human checkpoint may release Plan 08"
      pattern: "private.*noncommercial"
---

<objective>
Publish the official precedence and private-local clean-room/reuse policies that govern authority artifacts.

Purpose: Make source authority, ambiguity, private storage, community-reference reuse, and restricted-content controls auditable before the user supplies any official corpus bytes.
Output: Two concise policy documents plus a repository exclusion that protects all real authority data.
</objective>

<execution_context>
@C:/Users/Dad/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Dad/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-rules-and-data-authority/01-CONTEXT.md
@.planning/phases/01-rules-and-data-authority/01-RESEARCH.md
@.planning/phases/01-rules-and-data-authority/01-VALIDATION.md
@.planning/phases/01-rules-and-data-authority/01-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document normative authority and precedence</name>
  <read_first>
    - .planning/REQUIREMENTS.md
    - .planning/phases/01-rules-and-data-authority/01-CONTEXT.md
    - .planning/phases/01-rules-and-data-authority/01-RESEARCH.md
    - src/authority/validate-bundle.ts
    - tests/authority/bundle.test.ts
    - docs/authority-precedence.md (if it exists)
  </read_first>
  <files>docs/authority-precedence.md</files>
  <action>Create `docs/authority-precedence.md` implementing D-01/D-02 exactly: official rulebooks, official format rules, official Codex/FAQ, official card updates, and official card data are normative; community sources are provenance/examples only. Document explicit official supersession/reversal and effective date first, current card-specific updates for that card, explicitly selected scoped overlays only within scope, compatible rulebook/Codex/FAQ detail, retention of winning/superseded/contending refs, and `unsupported` for equal-rank, unclear-date, unclear-scope, or unresolved conflict. State this is the project fail-closed policy rather than a claim that the publisher issued a complete hierarchy. Cross-reference `resolveAuthorityPrecedence` and its current/superseded/scoped/ambiguous tests. Do not embed copyrighted rule text or infer missing rulings. Commit this task atomically after the automated policy check and tests pass.</action>
  <verify>
    <automated>node -e "const s=require('node:fs').readFileSync('docs/authority-precedence.md','utf8').toLowerCase();for(const x of ['official rulebook','format','codex','faq','card update','effective date','scoped','superseded','contending','unsupported','community'])if(!s.includes(x))process.exit(1)" &amp;&amp; pnpm test</automated>
  </verify>
  <acceptance_criteria>
    - `docs/authority-precedence.md` names all five normative official source classes and identifies community material as non-authoritative.
    - Its ordering matches the current, superseded, scoped, and ambiguous assertions in `tests/authority/bundle.test.ts`.
    - Every unresolved conflict ends in `unsupported`; the document contains no guessed or silently flattened outcome.
  </acceptance_criteria>
  <done>D-01/D-02 precedence is explicit, source-linked, implementation-linked, and fail-closed.</done>
</task>

<task type="auto">
  <name>Task 2: Enforce the private-local and clean-room boundary</name>
  <read_first>
    - AGENTS.md
    - .gitignore
    - .planning/phases/01-rules-and-data-authority/01-CONTEXT.md
    - .planning/phases/01-rules-and-data-authority/01-RESEARCH.md
    - tests/authority/fixtures/README.md
    - docs/external-reuse-policy.md (if it exists)
  </read_first>
  <files>.gitignore, docs/external-reuse-policy.md</files>
  <action>Append the anchored directory rule `.local/authority/` to `.gitignore` before Plan 07 can place real data there; preserve existing ignores. Create `docs/external-reuse-policy.md` implementing amended D-08, D-09, and D-11. Fix the sole Phase 1 acquisition path to a user-controlled manual browser save of the exact current rulebook PDF, base Constructed page, Codex, FAQs, Codex changelog, official card-update notice, and full card API JSON beneath `.local/authority/inputs/official-2026-08-20/primary/`, plus an independent byte-identical private backup root; prohibit project fetch, scraping, polling, API clients, automated acquisition, and public/network HTTP card APIs. Require every raw source file, normalized snapshot, source/input lock, and built official revision to remain below `.local/authority/`, private, local, Git-ignored, and excluded from packages. Permit Git to contain only project TypeScript, schemas, policy/docs, synthetic or minimal fixtures, official source URL/date, independent hashes/receipts, and generic local-import/final-gate tests. Allow the current rulebook PDF and official HTML pages only inside the ignored primary/backup source roots; explicitly forbid them, card art, full official/API corpus bytes, copied community data, and copied external code/tests/assets/card implementations from the built revision, Git, or packages. Record the community audit: `sadkinglabs/sorcery-registry` has technically useful structure but its MIT license covers code only and its card data is reserved to Erik's Curiosa; Contested Realms is GPL-3.0; spells.bar/playtest has no reusable license; the other audited sources are incomplete, copyleft, or unlicensed, so none is an approved gameplay corpus. Allow behavioral observations only through independent clean-room implementation with source revision, reviewer, review date, and attestation fields. State that hashes prove byte identity, not publisher authenticity or legal permission; the official API page and Terms remain in tension. Require Plan 07 to record the user's private/noncommercial/no-redistribution attestation, a seven-entry source-set lock with relative paths, official URLs, dates, media types, byte lengths/SHA-256 values, canonical source-set root hash, and an independent durable private backup with matching bytes. State that sharing, release with content, automated updating, commercialization, or changed scope requires written publisher permission and a separate plan. Commit this task atomically only after the ignore/policy assertions and `pnpm verify` pass.</action>
  <verify>
    <automated>git check-ignore -q .local/authority/probe &amp;&amp; node -e "const s=require('node:fs').readFileSync('docs/external-reuse-policy.md','utf8').toLowerCase();for(const x of ['manual browser','private','noncommercial','no redistribution','.local/authority/','source set','rulebook','constructed','codex','faq','changelog','card update','git-ignored','no fetch','no scraping','no polling','public/network','art','pdf','full corpus','gpl-3.0','sadkinglabs/sorcery-registry','mit covers code only','reserved','no reusable license','clean-room','source revision','reviewer','sha-256','backup','hashes prove byte identity','written publisher permission','separate plan'])if(!s.includes(x))process.exit(1)" &amp;&amp; pnpm verify</automated>
  </verify>
  <acceptance_criteria>
    - `.gitignore` contains an anchored `.local/authority/` directory rule, and `git check-ignore -q .local/authority/probe` exits 0.
    - The policy identifies exactly one acquisition path: the enumerated seven-file manual primary source set plus an independent private byte-identical backup; it contains no project fetch, scraping, polling, or public/network API branch.
    - The policy states that all seven raw official sources, normalized data, source/input locks, and built official revisions are private/local, Git-ignored, and excluded from packages.
    - The community audit names `sadkinglabs/sorcery-registry`, Contested Realms, and spells.bar/playtest with their exact reuse limits; no audited community source is authorized as a gameplay corpus.
    - The policy names private/noncommercial/no-redistribution scope, matching primary/backup hashes, and all future written-permission triggers without claiming legal certainty.
  </acceptance_criteria>
  <done>D-08/D-09/D-11 are enforced by a preexisting Git exclusion and an auditable private-local, clean-room, no-leakage policy.</done>
</task>

</tasks>

<verification>
Run both exact document assertions and `pnpm verify`. Confirm `git check-ignore -q .local/authority/probe` succeeds, `git ls-files .local/authority` is empty, the precedence policy matches `resolveAuthorityPrecedence`, and the reuse policy contains no alternative repository-corpus, automated-acquisition, or public-API path.
</verification>

<success_criteria>
- D-01/D-02 authority order, effective-date/scope rules, and unsupported ambiguity behavior are explicit and match code/tests.
- D-08/D-09/D-11 make the complete private-local manual source-set path, community clean-room boundary, restricted-asset exclusions, and future permission triggers exact.
- `.local/authority/` is ignored before any real official input is supplied.
- No protected source bytes, normalized official derivative, copied external material, or acquisition code is introduced.
</success_criteria>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| research/source claims -> repository policy | Inaccurate source or license status could authorize substituted or restricted data. |
| external reference observation -> implementation | GPL, unlicensed, or publisher-reserved material can contaminate code/tests/assets/data. |
| private filesystem -> Git/package boundary | Official corpus bytes could leak through staging, packaging, or an incomplete ignore rule. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-26 | Tampering / DoS | malicious JSON | mitigate | Plans 02-05 already enforce strict bounded parsing; this plan keeps `pnpm verify` blocking and prohibits any alternate acquisition/parser path. |
| T-01-27 | Tampering | path traversal/symlink escape | mitigate | All real material is confined below the fixed `.local/authority/` tree; Plans 07-08 require ordinary-file and containment checks before hashing/building. |
| T-01-28 | Spoofing | official source claims/source substitution | mitigate | Policy fixes the seven required source classes/relative paths, browser-only acquisition, URLs/dates/media types, and exact primary/backup file and source-set hashes, and states hashes do not prove authenticity. |
| T-01-29 | Tampering | artifact/reference provenance | mitigate | Policy requires immutable IDs/hashes/refs and routes every private build through the recursive offline validator using an independently supplied root hash. |
| T-01-30 | Information disclosure / legal | restricted asset or corpus publication | mitigate | `.local/authority/` is ignored before acquisition; policy forbids Git/package inclusion of official corpus, art, PDFs, and community material and reserves broader use for a separate permission-gated plan. |
| T-01-SC | Tampering | package installs | accept | This plan adds no package; the locked dependency verification remains part of `pnpm verify`. |
</threat_model>

<must_haves>
- DATA-01's official-source inventory and precedence policy are reviewable without bundling protected rulebook or card bytes.
- DATA-03 provenance includes clean-room/reviewer evidence for every external behavioral reference and exact hash evidence for the future private input.
- D-01/D-02/D-08/D-09/D-11 are explicitly cited in tasks and have no permissive publication or automated-acquisition branch.
</must_haves>

<output>
Create `.planning/phases/01-rules-and-data-authority/01-06-SUMMARY.md` when done.
</output>
