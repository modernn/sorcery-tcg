---
phase: 01-rules-and-data-authority
plan: "08"
type: execute
wave: 6
depends_on: [01-07]
files_modified:
  - package.json
  - docs/external-reuse-policy.md
  - data/authority/official-2026-08-20/bundle.json
  - data/authority/official-2026-08-20/sources.json
  - data/authority/official-2026-08-20/formats.json
  - data/authority/official-2026-08-20/cards.normalized.json
  - data/authority/official-2026-08-20/raw/cards.raw.json
  - tests/authority/official-revision.test.ts
autonomous: true
requirements: [DATA-01, DATA-02, DATA-03]
user_setup:
  - service: "Approved local Sorcery authority inputs"
    why: "The public terms do not authorize automated retrieval; Plan 07 must approve permission plus one durable exact-byte rebuild path before local materialization."
    env_vars:
      - name: AUTHORITY_INPUT_DIR
        source: "Clean local materialization from the exact repository/archive/acquisition path approved in Plan 07; a pre-existing executor-local directory alone is insufficient."
      - name: AUTHORITY_INPUT_LOCK
        source: "Relative path beneath AUTHORITY_INPUT_DIR to the canonical input-lock file approved in Plan 07; it contains the exact relative file list, byte counts, SHA-256 values, durable locator/procedure fingerprint, and storage modes."
      - name: AUTHORITY_EXPECTED_INPUT_ROOT_HASH
        source: "Independent canonical input-root SHA-256 recorded in the approved Plan 07 response; never derive it from the local materialization being checked."
must_haves:
  truths:
    - "The repository contains one approved immutable official authority revision with exact official source URLs, retrieval/effective dates, media types, raw hashes, derivation, precedence, and content hashes."
    - "The official card snapshot rebuilds deterministically from the Plan 07-approved durable input lock and clean local materialization, with any mismatched byte refused, per DATA-02/D-03/D-05."
    - "Published validation rehashes approved stored source bytes and separately verifies manifest-only locator/hash/reference binding without claiming absent raw bytes were reread."
    - "Every stored bundle, source, format, and card artifact uses the common D-06 stable ID/schema/provenance/content-hash envelope."
    - "No PDF, image, raw API/database corpus, or copied reference-project material is committed; permission scope and clean-room review are recorded per D-08/D-09."
  artifacts:
    - path: "data/authority/official-2026-08-20/bundle.json"
      provides: "Root immutable authority bundle ID/hash and exact artifact/source references"
    - path: "data/authority/official-2026-08-20/sources.json"
      provides: "Official rulebook, format, Codex/FAQ/changelog/card-update/card-data URLs and provenance"
    - path: "data/authority/official-2026-08-20/formats.json"
      provides: "Versioned base Constructed format and explicitly scoped overlays"
    - path: "data/authority/official-2026-08-20/cards.normalized.json"
      provides: "Versioned deterministic approved normalized card snapshot"
    - path: "data/authority/official-2026-08-20/raw/cards.raw.json"
      provides: "Conditional fixed stored-source bytes, created only when Plan 07 explicitly approves repository storage; absent for manifest-only durable input"
    - path: "tests/authority/official-revision.test.ts"
      provides: "Independent offline double-rebuild, tamper-copy, restricted-content, and immutability release gate"
  key_links:
    - from: "data/authority/official-2026-08-20/bundle.json"
      to: "data/authority/official-2026-08-20/sources.json"
      via: "exact source refs and byte hashes"
      pattern: "sourceRefs"
    - from: "data/authority/official-2026-08-20/bundle.json"
      to: "data/authority/official-2026-08-20/cards.normalized.json"
      via: "artifact ref stable ID and content hash"
      pattern: "contentHash"
    - from: "docs/external-reuse-policy.md"
      to: "data/authority/official-2026-08-20"
      via: "recorded permission, clean-room, durable input-root, and independently captured bundle-root receipt"
      pattern: "approved|input root hash|published bundle root hash"
---

<objective>
Publish and prove the approved official authority revision consumed by every later phase.

Purpose: Close the phase with a real, immutable, offline-validatable rules/format/card authority rather than only a synthetic pipeline, while staying within written permission and clean-room boundaries.
Output: The `official-2026-08-20` authority revision, non-confidential approval record, and full phase verification evidence.
</objective>

<execution_context>
@C:/Users/Dad/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Dad/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-rules-and-data-authority/01-CONTEXT.md
@.planning/phases/01-rules-and-data-authority/01-RESEARCH.md
@.planning/phases/01-rules-and-data-authority/01-VALIDATION.md
@.planning/phases/01-rules-and-data-authority/01-05-SUMMARY.md
@.planning/phases/01-rules-and-data-authority/01-06-SUMMARY.md
@.planning/phases/01-rules-and-data-authority/01-07-SUMMARY.md
@docs/authority-precedence.md
@docs/external-reuse-policy.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Record approval and publish the official revision</name>
  <read_first>
    - .planning/REQUIREMENTS.md
    - .planning/phases/01-rules-and-data-authority/01-CONTEXT.md
    - .planning/phases/01-rules-and-data-authority/01-RESEARCH.md
    - .planning/phases/01-rules-and-data-authority/01-07-SUMMARY.md
    - docs/external-reuse-policy.md
    - src/commands/import-authority.ts
    - src/commands/validate-authority.ts
    - data/authority/official-2026-08-20/bundle.json (if it exists; abort rather than overwrite)
  </read_first>
  <files>docs/external-reuse-policy.md, data/authority/official-2026-08-20/bundle.json, data/authority/official-2026-08-20/sources.json, data/authority/official-2026-08-20/formats.json, data/authority/official-2026-08-20/cards.normalized.json, data/authority/official-2026-08-20/raw/cards.raw.json</files>
  <action>Read the Plan 07 approval summary and abort if any permission, clean-room, durable-input-kind, locator/procedure, exact-file-hash, or independent input-root-hash field is absent, narrower than these outputs, or still `permission-required`. If Plan 07 produced no approved `01-07-SUMMARY.md`, stop and create no Phase 1 completion artifact. Materialize `AUTHORITY_INPUT_DIR` only from the approved repository/archive/procedure, require `AUTHORITY_INPUT_LOCK` to match that approval, and invoke `pnpm authority:import -- --input "$AUTHORITY_INPUT_DIR" --input-lock "$AUTHORITY_INPUT_LOCK" --expected-input-root-hash "$AUTHORITY_EXPECTED_INPUT_ROOT_HASH" --output-root data/authority --revision-id official-2026-08-20`; never fetch or substitute fresh bytes inside the importer. Capture the verified input-root hash and calculated bundle root hash from import stdout before reading the candidate. Independently recompute `identityHash(bundle.identity)`, require equality with that captured root, and validate using the captured hash rather than `bundle.json.contentHash`. Record the non-confidential issuer/reviewer/date/scope/attribution/revocation summary, durable input kind/locator-or-procedure/input-root hash, and captured published bundle root hash in `docs/external-reuse-policy.md`. Require `sources.json` to cover the current official December 19, 2025 rulebook release URLs, current Constructed source, official Codex/FAQ/changelog through July 15, 2026, official card-update source, and approved official card-data export with URL, retrieval timestamp, effective date where available, media type, raw SHA-256, derivation, authority class, durable locator/procedure, and legal/storage status. Only `authorityClass: official` may be normative; reviewed community/reference records remain provenance. For `repository-stored`, copy only permission-covered raw card bytes to the fixed `raw/cards.raw.json` path and rehash them during offline validation. For both other input kinds, publish no raw upstream bytes and mark sources manifest-only so offline validation checks canonical locator/hash/reference binding without claiming absent bytes were reread. Require `formats.json` to separate the base Constructed format from explicitly scoped overlays and preserve source refs. Refuse an existing revision. Commit only after validation by the captured external ID/hash and the conditional allowlist scan passes.</action>
  <verify>
    <automated>node --input-type=module -e "import fs from 'node:fs';import cp from 'node:child_process';import path from 'node:path';import {identityHash} from './src/authority/hash.ts';const policy=fs.readFileSync('docs/external-reuse-policy.md','utf8');const expected=/Published bundle root hash:\\s*(sha256:[0-9a-f]{64})/i.exec(policy)?.[1];const inputRoot=/Approved input root hash:\\s*(sha256:[0-9a-f]{64})/i.exec(policy)?.[1];const kind=/Durable input kind:\\s*(repository-stored|immutable-custodian|approved-acquisition-procedure)/i.exec(policy)?.[1]?.toLowerCase();if(!expected||!inputRoot||!kind)process.exit(1);const root='data/authority/official-2026-08-20';const b=JSON.parse(fs.readFileSync(path.join(root,'bundle.json'),'utf8'));if(identityHash(b.identity)!==expected)process.exit(1);cp.execFileSync(process.execPath,['src/commands/validate-authority.ts','--root','data/authority','--bundle','official-2026-08-20/bundle.json','--id','official-2026-08-20','--hash',expected],{stdio:'inherit'});const found=[];for(const e of fs.readdirSync(root,{recursive:true,withFileTypes:true}))if(e.isFile())found.push(path.relative(root,path.join(e.parentPath,e.name)).replaceAll('\\\\','/'));const allowed=new Set(['bundle.json','sources.json','formats.json','cards.normalized.json']);if(kind==='repository-stored')allowed.add('raw/cards.raw.json');if(found.length!==allowed.size||found.some(f=&gt;!allowed.has(f)))process.exit(1)" &amp;&amp; pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `official-2026-08-20` is created once from a clean materialization only after `input-lock.json` and every byte match the independent Plan 07 input-root hash; an existing path or mismatch aborts without modification.
    - Source records cover every official authority class named in D-01 with URLs, timestamps, effective dates when available, media types, raw hashes, derivation, and permission/storage status.
    - A manifest-only approval publishes exactly four canonical JSON files. A repository-stored approval may additionally publish only `raw/cards.raw.json`, whose bytes are permission-covered and rehashed offline; no other PDF, image, raw corpus/API dump, copied external data, or unapproved derivative is present.
    - The importer stdout's calculated bundle root hash is captured before reading the candidate, independently recomputed from `bundle.identity`, recorded with the approved input-root hash outside the revision in `docs/external-reuse-policy.md`, and used for validation; `bundle.json` may not supply its own expected hash.
  </acceptance_criteria>
  <done>The approved official authority revision is immutable, bound to an independently expected durable input root and captured bundle root, storage-compliant, and valid by exact local ID/hash without trusting its self-declared hash.</done>
</task>

<task type="auto">
  <name>Task 2: Prove phase-wide reproducibility and offline consumption</name>
  <read_first>
    - .planning/phases/01-rules-and-data-authority/01-VALIDATION.md
    - docs/authority-precedence.md
    - docs/external-reuse-policy.md
    - data/authority/official-2026-08-20/bundle.json
    - data/authority/official-2026-08-20/sources.json
    - data/authority/official-2026-08-20/formats.json
    - data/authority/official-2026-08-20/cards.normalized.json
    - tests/authority/canonical-json.test.ts
    - tests/authority/provenance.test.ts
    - tests/authority/card-snapshot.test.ts
    - tests/authority/bundle.test.ts
    - tests/authority/official-revision.test.ts (if it exists)
  </read_first>
  <files>package.json, tests/authority/official-revision.test.ts</files>
  <behavior>
    - The test installs throwing network sentinels for `globalThis.fetch` and the `node:http`, `node:https`, and `node:net` request/connect entry points before invoking import/validation code; any network attempt fails the release gate.
    - Before any check, the test hashes every published revision file. It rebuilds twice from a clean materialization that matches `AUTHORITY_INPUT_LOCK` and the independently supplied `AUTHORITY_EXPECTED_INPUT_ROOT_HASH` into two isolated temporary roots, then compares the sorted relative path set and every byte.
    - The expected published bundle root comes from the import receipt recorded outside the candidate revision; the test independently recomputes `identityHash(bundle.identity)` and validates the official revision and both candidates by that expected ID/hash, never by each candidate's own `contentHash`.
    - Tampered copies cover canonical bundle/artifact/reference changes plus stored-source bytes when present or manifest-only locator/procedure/byteHash/`SourceRef` bindings when raw bytes are absent; every copy fails and the published revision is never touched.
    - A conditional allowlist, restricted extension/magic-byte/raw-corpus scan, and clean-room signature scan reject every publisher/reference payload not explicitly permitted by Plan 07; final published file hashes exactly match the initial baseline.
  </behavior>
  <action>Create `tests/authority/official-revision.test.ts` as the single release-gate harness and add `authority:verify-release` to `package.json`. Require all three approved input environment variables and the non-confidential receipt fields from `docs/external-reuse-policy.md`; fail rather than skip when any is missing. Materialize/check the approved durable input lock before either build, install explicit network denial before importing the authority command/library entry points, capture a baseline map of published relative paths and SHA-256 bytes, and run two imports into distinct `mkdtemp` roots using the same revision ID and independently expected input-root hash. Compare complete sorted trees byte-for-byte. Independently recompute the bundle identity hash and cross-check the receipt's captured import hash before calling validation for the official and rebuilt copies. Create isolated tamper copies for bundle identity/ref, normalized cards, formats, sources, and either `raw/cards.raw.json` stored bytes or manifest-only durable locator/procedure/byteHash/ref fields; require each validation to fail by the external expected ID/hash. Scan the published tree and staged diff filenames/content for prohibited PDF/image/archive/database/raw-dump extensions, matching magic bytes, unapproved raw records, and copied Contested Realms/spells.bar implementation markers, while allowing only the exact `raw/cards.raw.json` hash when the recorded Plan 07 kind is `repository-stored`. Hash the published tree again after all builds/tamper tests and require exact equality with the baseline. Clean only verified temporary roots in `finally`. Do not rewrite, regenerate, or use the published revision as an expected-hash oracle. Commit the test/script only after the full automated command passes; record the input-root hash, captured bundle-root hash, two tree hashes, tamper cases, scan result, and immutable before/after map in `01-08-SUMMARY.md`.</action>
  <verify>
    <automated>pnpm typecheck &amp;&amp; pnpm lint &amp;&amp; pnpm test &amp;&amp; pnpm authority:verify-release</automated>
  </verify>
  <acceptance_criteria>
    - The final command fails on any network attempt and runs every authority test with no todo/skip.
    - Two clean builds from the approved durable input lock have identical path sets and bytes; independently expected/recomputed root hashes agree; every required tampered copy fails.
    - All Phase 1 artifact files resolve through the common stable ID/schema/source-parent refs/content-hash envelope and the root bundle selects by exact ID/hash.
    - Restricted-extension, magic-byte, unapproved-raw, staged-diff, and clean-room scans find no prohibited publisher or external-project content; any approved stored raw file matches its locked hash exactly.
    - The published revision's complete before/after relative-path and SHA-256 map is identical.
  </acceptance_criteria>
  <done>DATA-01/02/03, D-01 through D-10, the durable input contract, the Nyquist contract, and the ASVS L1 authority threats are proven by one explicit no-network, double-rebuild, tamper-copy, restricted-content, immutable-publication gate.</done>
</task>

</tasks>

<must_haves>
- Developers can identify, clean-materialize, rebuild, and validate the exact official rulebook/format/Codex/FAQ/card-update/card-data authority from the Plan 07-approved durable input lock and immutable bundle ID/hash.
- The normalized card snapshot is deterministic from exact locked bytes, refuses input mismatches, and is never fetched during a game or experiment.
- Bundle, source, format, and card artifacts all use the one D-06 provenance envelope and reject tampering or mutation.
- Written permission and clean-room review authorize exactly the stored derivative/source files; stored raw bytes are rehashed, manifest-only evidence is canonically bound without false raw-byte claims, and restricted upstream/reference material remains absent.
</must_haves>

<source_coverage_audit>

| SOURCE | ID | Feature / requirement | Plan(s) | Status | Notes |
|--------|----|-----------------------|---------|--------|-------|
| GOAL | - | Identify and reproduce exact official rules, format, card data, and provenance | 04-08 | COVERED | Validator, commands, policies, permission gate, and official revision. |
| REQ | DATA-01 | Immutable official authority bundle with source metadata, precedence, and SHA-256 | 01, 04-08 | COVERED | Contract, validator, atomic build, policy, approval, real bundle. |
| REQ | DATA-02 | Versioned normalized card snapshot build/validate offline | 01, 03, 05, 07-08 | COVERED | Pure normalizer, import-time exact-byte/input-root binding, human-approved durable path, and clean no-network double rebuild. |
| REQ | DATA-03 | Common stable ID/schema/provenance/content-hash envelope | 01-04, 06-08 | COVERED | Canonical identity, strict schemas, recursive refs, policy, real artifacts. |
| RESEARCH | - | Pinned Node/TypeScript/pnpm and node:test stack | 01 | COVERED | Exact audited versions; Zod only at trust boundaries. |
| RESEARCH | - | Canonical JSON/SHA-256 and strict bounded schemas | 02 | COVERED | RFC-derived vectors, duplicate-key rejection, exact paths. |
| RESEARCH | - | Pure deterministic local card normalization | 03 | COVERED | No network/clock/random/filesystem dependency. |
| RESEARCH | - | Offline recursive validation, confinement, precedence, storage policy | 04 | COVERED | Stored bytes are rehashed; manifest-only locator/hash/ref evidence is validated without claiming absent bytes. |
| RESEARCH | - | Atomic write-once local build and validate commands | 05 | COVERED | Import validates the durable input lock and emits an independently usable root receipt; validator has no acquisition/raw-input argument. |
| RESEARCH | - | Permission, durable input, and clean-room legal guardrails | 06-08 | COVERED | Separate blocking human gate precedes publication and halts without summaries when unresolved. |
| CONTEXT | D-01 | Official sources only are normative | 04, 06, 08 | COVERED | Code, policy, and source manifest. |
| CONTEXT | D-02 | Effective-dated precedence; ambiguity unsupported | 04, 06, 08 | COVERED | Resolution tests and policy. |
| CONTEXT | D-03 | Runtime selects validated local bundle by ID/hash; no live data | 04, 05, 08 | COVERED | Offline validator/commands/final bundle plus explicit no-network release gate. |
| CONTEXT | D-04 | New immutable revision with complete source metadata | 02, 05, 08 | COVERED | Envelope, atomic write-once build, real revision. |
| CONTEXT | D-05 | Official/stable card IDs and deterministic normalization | 03, 08 | COVERED | Pure normalizer and real snapshot. |
| CONTEXT | D-06 | Shared minimal provenance envelope for every artifact kind | 02, 08 | COVERED | Exported common schemas and real authority artifacts. |
| CONTEXT | D-07 | Project canonical JSON; exact paths; no silent repair/drop | 02-05 | COVERED | Exact vectors, strict validation, sorted diagnostics. |
| CONTEXT | D-08 | Store permitted derivatives/manifests; restrict PDFs/images | 01, 04-08 | COVERED | Fixture/storage policy, conditional approved stored-source branch, manifest-only branch, and restricted extension/magic/content scan. |
| CONTEXT | D-09 | Contested Realms/spells.bar behavior-only clean room | 01, 06-08 | COVERED | Fixture policy, reuse doc, human attestation. |
| CONTEXT | D-10 | TypeScript/Node stdlib first; dependency only for safer boundaries | 01-05 | COVERED | One package, Node built-ins, audited Zod only. |

No deferred idea is planned: collection/deck acquisition, game/card behavior, model competitors, and browser play remain in their assigned later phases.
</source_coverage_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| approved local official bytes -> repository revision | Final publication can still ingest malicious, substituted, or over-scoped data. |
| immutable revision -> later runtime consumers | Any tampering, path/reference break, or policy violation must prevent selection. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-36 | Tampering / DoS | approved official JSON | mitigate | The production import applies the tested byte/depth/record/ref/diagnostic caps and strict duplicate-aware schemas. |
| T-01-37 | Tampering / Elevation | `AUTHORITY_INPUT_DIR` / lock and output paths | mitigate | Resolve/realpath within approved roots; verify exact lock/file/root hashes before parsing; reject traversal/symlink escape, input mismatch, and existing revision. |
| T-01-38 | Spoofing | official source substitution | mitigate | Match Plan 07 authority class, durable locator/procedure, reviewed URLs/scope, and independently expected input-root hash; never fresh-fetch inside import/validation. |
| T-01-39 | Tampering / Repudiation | final artifact/reference graph | mitigate | Capture import root outside the candidate, independently recompute identity, validate exact root ID/hash, rehash stored bytes/canonical artifacts, verify manifest-only bindings, tamper copies, and prove published before/after hashes unchanged. |
| T-01-40 | Information disclosure / legal | final committed files | mitigate | Conditionally allow only four canonical JSON files or those plus the one permission-covered fixed raw file; scan extensions, magic bytes, raw payloads, staged diff, and external-project signatures. |
| T-01-SC | Tampering | package installs | mitigate | Final gate uses the exact Approved pnpm lockfile and introduces no dependency. |
</threat_model>

<verification>
Run `pnpm typecheck && pnpm lint && pnpm test && pnpm authority:verify-release`. The release test must install explicit fetch/http/https/net denial, verify the durable input lock, rebuild twice into isolated roots, compare every path/byte, independently cross-check the captured root hash, validate by external ID/hash, reject stored-or-manifest tamper copies, scan restricted/copied content, and prove the published revision's complete byte map is unchanged. Record exact commands and hashes in `01-08-SUMMARY.md`.
</verification>

<success_criteria>
- The approved `official-2026-08-20` revision contains exactly four canonical JSON artifacts for manifest-only approval, or those four plus only the fixed permission-covered `raw/cards.raw.json` for repository-stored approval, and validates offline by independently captured/recomputed stable ID/content hash.
- Its source manifest identifies every official authority class with complete provenance, and its precedence/format scope matches policy and tests.
- Repeated clean builds from the durable exact-byte lock are byte-identical; stored-byte/manifest-binding/artifact tampering, ambiguity, path escape, cycles, restricted storage, overwrite, and partial failure are rejected while the published revision remains unchanged.
- All DATA requirements and D-01 through D-10 are covered with no deferred-scope leakage and no runtime network path.
</success_criteria>

<output>
Create `.planning/phases/01-rules-and-data-authority/01-08-SUMMARY.md` when done.
</output>
