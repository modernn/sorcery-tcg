---
phase: 01-rules-and-data-authority
plan: "08"
type: execute
wave: 6
depends_on: [01-07]
files_modified:
  - package.json
  - data/authority/README.md
  - data/authority/receipts/official-2026-08-20.json
  - tests/private-authority/private-revision.test.ts
  - tests/private-authority/repository-boundary.test.ts
autonomous: true
requirements: [DATA-01, DATA-02, DATA-03]
must_haves:
  truths:
    - "The complete seven-source primary and independent backup sets are reverified from private bytes before importer inputs or an authority revision are built."
    - "The evidence gate recognizes only legacy `user-run-one-shot-powershell` with no `authorizationReference` and closed `user-authorized-agent-run-one-shot-powershell` with an exact case-sensitive reference plus its matching consumed record; this revision is bound specifically to `quick-260825-mhh-retry-1`, while the consumed failed-attempt `quick-260825-mhh` record is never publication evidence or standing permission."
    - "Both private source roots independently produce byte-identical canonical `cards.raw.json`, `formats.json`, `sources.json`, `input-lock.json`, and authority revision trees."
    - "The selected `official-2026-08-20` revision is write-once under Git-ignored `.local/authority/` and validates offline as `bundle:official-2026-08-20` by an independently recomputed root hash."
    - "Every official source record carries the locally computed URL/date/media/hash evidence from Plan 07; no missing hash is invented or borrowed from research prose."
    - "The committed receipt contains only non-sensitive labels, relative paths, provenance, and hashes—never local absolute locators, source bytes/excerpts, normalized corpus, art, or PDFs."
    - "The deterministic integrity gate and the no-network/Git-package leakage gate are separate focused tests, while default `pnpm test` remains clean-clone-safe."
  artifacts:
    - path: ".local/authority/revisions/official-2026-08-20"
      provides: "Selected validated private official authority revision; Git-ignored and never committed or packaged"
    - path: "data/authority/receipts/official-2026-08-20.json"
      provides: "Safe non-content seven-source/revision hash receipt without machine-specific absolute locators"
      contains: "bundle:official-2026-08-20"
    - path: "data/authority/README.md"
      provides: "Private source-set rebuild, local ID/hash selection, no-network, backup, and future-permission instructions"
    - path: "tests/private-authority/private-revision.test.ts"
      provides: "Opt-in full-set double-build, canonical integrity, tamper, and immutability gate"
    - path: "tests/private-authority/repository-boundary.test.ts"
      provides: "Opt-in no-network plus tracked/staged/package restricted-content leakage gate"
    - path: "package.json"
      provides: "Explicit authority:verify-private command outside the clean-clone default test glob"
  key_links:
    - from: ".local/authority/locks/official-2026-08-20/source-set-lock.json"
      to: ".local/authority/build-inputs/official-2026-08-20/{primary,backup}/input-lock.json"
      via: "reusable source-set verification and independent deterministic generation from each root"
      pattern: "sourceSetRootHash"
    - from: ".local/authority/build-inputs/official-2026-08-20/primary"
      to: ".local/authority/revisions/official-2026-08-20"
      via: "authority:import with input-root-relative input-lock.json and independently expected input root hash"
      pattern: "bundle:official-2026-08-20"
    - from: "tests/private-authority/private-revision.test.ts"
      to: "src/commands/import-authority.ts"
      via: "independent primary/backup full-set materialization and byte-identical builds"
      pattern: "importAuthority"
    - from: "tests/private-authority/repository-boundary.test.ts"
      to: "Git reachable history, index, staged diff, pnpm package dry-run, and authority modules"
      via: "network sentinels and exact private source/revision hash leakage scans"
      pattern: "fetch.*http.*https.*net"
---

<objective>
Build the real official authority revision from the complete private source set, prove deterministic integrity separately from repository/network isolation, and commit only safe non-content evidence and generic tests.

Purpose: Complete DATA-01/02/03 honestly: every official authority source has exact private primary/backup bytes, and reproducibility depends on those independently stored bytes plus recorded hashes—not Git alone.
Output: One validated Git-ignored local revision, a safe receipt/README, and two focused explicit private verification tests.
</objective>

<execution_context>
@C:/Users/Dad/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Dad/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.gitignore
@package.json
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-rules-and-data-authority/01-CONTEXT.md
@.planning/phases/01-rules-and-data-authority/01-DISCUSSION-LOG.md
@.planning/phases/01-rules-and-data-authority/01-RESEARCH.md
@.planning/phases/01-rules-and-data-authority/01-VALIDATION.md
@.planning/phases/01-rules-and-data-authority/01-PATTERNS.md
@.planning/phases/01-rules-and-data-authority/01-05-SUMMARY.md
@.planning/phases/01-rules-and-data-authority/01-06-SUMMARY.md
@.planning/phases/01-rules-and-data-authority/01-07-SUMMARY.md
@docs/authority-precedence.md
@docs/external-reuse-policy.md
@src/authority/private-source-set.ts
@src/commands/import-authority.ts
@src/commands/validate-authority.ts
@src/authority/canonical-json.ts
@src/authority/hash.ts
@src/authority/validate-bundle.ts
@tests/authority/bundle.test.ts
@tests/authority/private-source-set.test.ts
@tests/authority/fixtures/bundle-input/input-lock.json
</context>

<interfaces>
Completed Plan 05 importer contract:
- The input root contains exactly `cards.raw.json`, `formats.json`, `sources.json`, and `input-lock.json`; the lock covers exactly the first three in that order.
- Input-lock modes are `manifest-only` for `cards.raw.json` and `stored` for `formats.json` plus `sources.json`; raw publisher bytes are not copied into the revision.
- `--input-lock` is input-root-relative `input-lock.json`; import CLI flags are `--input`, `--input-lock`, `--expected-input-root-hash`, `--output-root`, and `--revision-id`.
- Import CLI stdout emits only candidate `verifiedInputRootHash` and `bundleRootHash`; the stable ID is independently fixed as `bundle:official-2026-08-20`.
- Each JSON importer input is at most 10,000,000 bytes and the three locked inputs total at most 32,000,000 bytes.

Completed validation CLI contract:
- Use exactly `--root .local/authority/revisions/official-2026-08-20 --bundle bundle.json --id bundle:official-2026-08-20 --hash <independently captured root>`.
- Validation resolves `bundle.json` relative to the supplied revision root and accepts no raw-input/source-set argument.

Source-record and format linkage contracts:
- Every format row references one derived format-input source record whose `byteHash` equals the exact canonical `formats.json` input hash.
- That derived manifest-only record carries `derivation.parentByteHashes` for the locked official rulebook/Constructed/Codex/FAQ/changelog/card-update sources used to transcribe the format definitions. Every raw publisher record contains exactly the current manifest-only `SourceRecord` keys—`sourceId`, `url`, `authorityClass`, `retrievedAt`, `effectiveDate`, `mediaType`, `byteHash`, `derivation`, `licenseStatus`, `storageMode`, `storagePolicy`, `durableLocator`, and `acquisitionProcedureHash`—with no extras. The raw rulebook source record's `url` is the official December 2025 release page, `byteHash` is the exact saved PDF hash, `derivation` is `{method: 'verbatim', parentByteHashes: [], notes: 'acquired by the fixed one-shot PowerShell collector under Plan 07's closed evidence pair from the exact official release page; observed filename and retrieval time recorded in the private lock'}`, `durableLocator` is the SHA-256 URN, and `acquisitionProcedureHash` is `null`; `sourceUrl`, observed-filename, and private-locator fields never enter `sources.json`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Materialize the complete locked inputs and build the selected private revision</name>
  <read_first>
    - .gitignore
    - .planning/REQUIREMENTS.md
    - .planning/phases/01-rules-and-data-authority/01-CONTEXT.md
    - .planning/phases/01-rules-and-data-authority/01-RESEARCH.md
    - .planning/phases/01-rules-and-data-authority/01-07-SUMMARY.md
    - .local/authority/locks/official-2026-08-20/source-set-lock.json
    - docs/authority-precedence.md
    - docs/external-reuse-policy.md
    - src/authority/private-source-set.ts
    - src/commands/import-authority.ts
    - src/commands/validate-authority.ts
    - src/authority/canonical-json.ts
    - src/authority/hash.ts
    - tests/authority/fixtures/bundle-input/input-lock.json
    - data/authority/README.md (if it exists)
    - data/authority/receipts/official-2026-08-20.json (if it exists)
  </read_first>
  <files>.local/authority/build-inputs/official-2026-08-20/{primary,backup}/{cards.raw.json,formats.json,sources.json,input-lock.json} (private, Git-ignored), .local/authority/revisions/official-2026-08-20 (private, Git-ignored), data/authority/README.md, data/authority/receipts/official-2026-08-20.json</files>
  <action>Before opening source bytes or writing build output, accept only the two structurally valid evidence forms per D-08/D-11: legacy `acquisitionMethod: user-run-one-shot-powershell` with the `authorizationReference` property absent, or `acquisitionMethod: user-authorized-agent-run-one-shot-powershell` with an exact case-sensitive closed reference and its matching durable ignored consumed record. Reject null/empty/padded/case-changed/arbitrary references, a mismatched method/reference, missing consumption, or any record without the exact five fields `schemaVersion`, `revisionId`, `acquisitionMethod`, `authorizationReference`, and `consumedAt` matching the lock and `official-2026-08-20`. Then bind this revision build to the actual publication evidence: both `01-07-SUMMARY.md` and the private lock must contain exactly `user-authorized-agent-run-one-shot-powershell` / `quick-260825-mhh-retry-1`, and `.local/authority/authorizations/quick-260825-mhh-retry-1.consumed.json` must pass that strict check. The separately consumed `quick-260825-mhh` record documents only the failed first attempt; never accept it as this publication receipt, remove/reset either record, reuse either reference, or treat either authorization as standing permission. Also require the exact private-risk/no-permission acknowledgment, all seven exact entries/metadata, source-set root hash, and independent-root verification result. Call `verifyPrivateSourceSet` again using the exact private roots from the ignored lock; require the returned path-sorted entries/root hash to match the lock and safe summary, and never trust summary hashes alone.

Independently materialize two fresh importer roots at `.local/authority/build-inputs/official-2026-08-20/{primary,backup}`: each `cards.raw.json` is copied byte-for-byte from its respective verified `cards/cards.raw.json`; each `formats.json` is generated canonically and independently from the same explicit D-01/D-02 format mapping using its own root's verified rulebook/Constructed/Codex/FAQ/changelog/card-update hashes; each `sources.json` is generated independently from that root's locally rehashed seven-source evidence. All seven publisher sources must be strict current-schema manifest-only records with `licenseStatus: permission-required`, `storageMode: manifest-only`, `storagePolicy: prohibited`, and their own `urn:sha256:` durable locator, `url`, retrieval/effective date, media type, byte hash, and derivation. For the rulebook record specifically, set `url` to `https://sorcerytcg.com/news/sorcery-contested-realm-december-2025-rulebook-update`, set `byteHash` to the locally rehashed saved PDF, and set `derivation` to `method: verbatim`, empty parent hashes, and notes stating the fixed one-shot PowerShell collector followed the exact official release page under Plan 07's closed evidence pair and recorded the observed filename/retrieval time; never copy the optional private locator or invent `sourceUrl`/observed-filename keys in `sources.json`. Add exactly one derived format-input manifest-only source whose byte hash equals the canonical `formats.json` bytes and whose `derivation.parentByteHashes` cites every raw official source used by the format mapping; every format row references that source ID. Exactly one JSON source must match the locked card bytes. Create canonical `input-lock.json` in each importer root covering exactly `cards.raw.json` as `manifest-only`, then `formats.json` and `sources.json` as `stored`, with exact byte lengths/hashes and independently computed input-root hash. Enforce the completed importer bounds: each of the three JSON inputs is at most 10,000,000 bytes and their total is at most 32,000,000 bytes. Refuse extras, path aliases, or unequal complete primary/backup importer trees; compare their sorted path/length/SHA-256 maps before building.

Refuse an existing `.local/authority/revisions/official-2026-08-20`. Invoke exactly `pnpm authority:import -- --input .local/authority/build-inputs/official-2026-08-20/primary --input-lock input-lock.json --expected-input-root-hash <independently computed primary root> --output-root .local/authority/revisions --revision-id official-2026-08-20`. Parse stdout only for the candidate verified input-root hash and bundle-root hash. Require fixed stable ID `bundle:official-2026-08-20`, independently read the resulting bundle identity and recompute its root hash, then invoke exactly `pnpm authority:validate -- --root .local/authority/revisions/official-2026-08-20 --bundle bundle.json --id bundle:official-2026-08-20 --hash <captured/recomputed root>`. Create `data/authority/receipts/official-2026-08-20.json` with schema version, the exact current `acquisitionMethod` and `authorizationReference`, revision/stable ID, bundle/input/source-set root hashes, a seven-row relative-path/URL/date/media/byte-length/SHA-256 table, non-sensitive primary/backup labels, D-11 attestation, and `gitContainsCorpus: false`; do not copy `consumedAt`, a consumed-record locator, an absolute local locator, or source excerpts into the committed receipt. Create `data/authority/README.md` stating exact private lock location, Git-alone insufficiency, rebuild from either complete independent set, local runtime selection by ID/hash, no network/API service, no official bytes in Git/package, and future written-permission triggers. Leave README, receipt, and every other Plan 08 tracked artifact unstaged and uncommitted; Task 3 owns the first commit only after its staged/index/package/reachable-history leakage gate passes. Never force-add private roots.</action>
  <verify>
    <automated>node --input-type=module -e "import fs from 'node:fs';import cp from 'node:child_process';import {identityHash} from './src/authority/hash.ts';import {verifyPrivateSourceSet} from './src/authority/private-source-set.ts';const lock=JSON.parse(fs.readFileSync('.local/authority/locks/official-2026-08-20/source-set-lock.json','utf8')),r=JSON.parse(fs.readFileSync('data/authority/receipts/official-2026-08-20.json','utf8')),hasRef=Object.hasOwn(lock,'authorizationReference'),refs=new Set(['quick-260825-mhh','quick-260825-mhh-retry-1']),user=lock.acquisitionMethod==='user-run-one-shot-powershell'&amp;&amp;!hasRef,agent=lock.acquisitionMethod==='user-authorized-agent-run-one-shot-powershell'&amp;&amp;typeof lock.authorizationReference==='string'&amp;&amp;refs.has(lock.authorizationReference),current='quick-260825-mhh-retry-1';if(!(user||agent)||!agent||lock.authorizationReference!==current)process.exit(1);const c=JSON.parse(fs.readFileSync(`.local/authority/authorizations/${current}.consumed.json`,'utf8')),keys=['acquisitionMethod','authorizationReference','consumedAt','revisionId','schemaVersion'];if(JSON.stringify(Object.keys(c).sort())!==JSON.stringify(keys)||c.schemaVersion!==1||c.revisionId!=='official-2026-08-20'||c.acquisitionMethod!==lock.acquisitionMethod||c.authorizationReference!==current||typeof c.consumedAt!=='string'||r.acquisitionMethod!==lock.acquisitionMethod||r.authorizationReference!==current)process.exit(1);const v=await verifyPrivateSourceSet({primaryRoot:lock.primaryRoot,backupRoot:lock.backupRoot,repositoryRoot:'.',entries:lock.entries});if(v.entries.length!==7||v.sourceSetRootHash!==lock.sourceSetRootHash||r.sourceSetRootHash!==lock.sourceSetRootHash)process.exit(1);const b=JSON.parse(fs.readFileSync('.local/authority/revisions/official-2026-08-20/bundle.json','utf8'));if(b.identity.stableId!=='bundle:official-2026-08-20'||identityHash(b.identity)!==r.bundleRootHash)process.exit(1);const x=cp.spawnSync('pnpm',['authority:validate','--','--root','.local/authority/revisions/official-2026-08-20','--bundle','bundle.json','--id','bundle:official-2026-08-20','--hash',r.bundleRootHash],{stdio:'inherit',shell:process.platform==='win32'});process.exit(x.status??1)"</automated>
  </verify>
  <acceptance_criteria>
    - Both complete source roots are reverified through the reusable realpath/identity/hash helper immediately before materialization.
    - Primary and backup importer roots each contain exactly four files; their locks cover `cards.raw.json`, `formats.json`, and `sources.json` in that order with modes manifest-only/stored/stored and satisfy 10,000,000/32,000,000-byte bounds.
    - The generic gate recognizes only legacy user-run/no-reference and closed agent-run/exact-reference-plus-record structures, while the live lock, Plan 07 summary, committed safe receipt, and matching five-field consumed record agree exactly on `user-authorized-agent-run-one-shot-powershell` / `quick-260825-mhh-retry-1`; the consumed failed-attempt reference, missing consumption, reuse, padding, case changes, arbitrary references, or method/reference mismatches fail before materialization.
    - All source hashes come from locally rehashed private bytes; the rulebook source uses the official release-page `url`, exact PDF hash, and schema-valid collector-route derivation without private locator fields; the derived format-input source hash equals canonical `formats.json` and cites its raw parent hashes.
    - Complete primary/backup importer input maps are byte-identical before the selected write-once revision is built.
    - Validation uses revision root plus relative `bundle.json`, fixed external stable ID, and independently captured/recomputed root hash; it never duplicates the revision path or expects `bundleId` in import stdout.
    - The committed receipt/README contain no absolute private locator, source byte/excerpt, normalized corpus, art, or PDF.
    - After Task 1, README, receipt, and every other Plan 08 tracked artifact remain unstaged and uncommitted for Task 3's whole-set pre-commit leakage gate.
  </acceptance_criteria>
  <done>The complete private source set has produced one write-once offline-valid revision and a safe non-content receipt using the exact completed CLI contracts.</done>
</task>

<task type="auto">
  <name>Task 2: Prove full-set deterministic rebuild integrity and tamper resistance</name>
  <read_first>
    - package.json
    - .planning/phases/01-rules-and-data-authority/01-CONTEXT.md
    - .planning/phases/01-rules-and-data-authority/01-07-SUMMARY.md
    - .local/authority/locks/official-2026-08-20/source-set-lock.json
    - data/authority/receipts/official-2026-08-20.json
    - src/authority/private-source-set.ts
    - src/commands/import-authority.ts
    - src/commands/validate-authority.ts
    - src/authority/canonical-json.ts
    - src/authority/hash.ts
    - src/authority/validate-bundle.ts
    - tests/authority/bundle.test.ts
    - tests/authority/private-source-set.test.ts
    - tests/private-authority/private-revision.test.ts (if it exists)
  </read_first>
  <files>tests/private-authority/private-revision.test.ts</files>
  <action>Create `tests/private-authority/private-revision.test.ts` outside the default test glob. Its evidence cases must recognize only legacy user-run with no reference and closed agent-run with an exact case-sensitive reference plus its matching five-field consumed record. The live build must fail rather than skip unless the private lock, Plan 07 summary, safe receipt, and durable record agree on `user-authorized-agent-run-one-shot-powershell` / `quick-260825-mhh-retry-1`; reject the consumed failed-attempt `quick-260825-mhh` as publication evidence, missing/mismatched consumption, arbitrary/case-changed/padded references, method/reference mismatch, and any reuse or standing-permission claim. It must also fail if either complete source root, selected revision, or attestation is absent. Re-run `verifyPrivateSourceSet`; independently regenerate both four-file importer roots from the primary versus backup seven-file sets rather than copying Task 1's derived inputs, recompute both exact input locks/root hashes, and require their complete maps to match. Run `importAuthority()` into two distinct verified `mkdtemp` output roots and compare the complete revision path/length/SHA-256 maps byte-for-byte. Require both returned IDs to equal `bundle:official-2026-08-20`, both returned bundle roots to equal each other and the safe receipt, and validate each candidate plus the selected revision using an external stable ID/root. Create isolated copies only—never modify private originals—for tampering with a raw source-set byte/metadata/hash, source-set root, acquisition evidence, cards input/lock, derived format source/parent hashes, sources manifest, normalized cards, format artifact, bundle identity, every `SourceRef`, and a reference cycle; require each relevant helper/import/validator boundary to fail. Capture the selected revision's recursive path/byte/hash map before and after all cases and require exact equality. Bound diagnostics/work, preserve all originals, and clean only verified temp roots in `finally`. Record test behavior in assertions; do not write summary or repository metadata from the test. Do not stage or commit this test or any other Plan 08 tracked artifact; Task 3 owns the first commit after the leakage gate.</action>
  <verify>
    <automated>node --test tests/private-authority/private-revision.test.ts &amp;&amp; pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - The focused test independently re-verifies all seven primary/backup sources and independently derives both complete importer roots.
    - Two clean imports from separate primary/backup derivations yield identical complete revision trees and the safe receipt's fixed stable ID/root hash.
    - Source-set, input-lock, derivation-parent, canonical artifact, source-reference, and cycle tamper cases all fail closed.
    - The selected local revision is byte-identical before and after every build/tamper case and private originals are never modified.
  </acceptance_criteria>
  <done>Deterministic reproducibility and integrity are proven from both complete independent private source sets, not from Git or card bytes alone.</done>
</task>

<task type="auto">
  <name>Task 3: Prove no network path and zero Git/package leakage</name>
  <read_first>
    - package.json
    - .gitignore
    - data/authority/README.md
    - data/authority/receipts/official-2026-08-20.json
    - .local/authority/locks/official-2026-08-20/source-set-lock.json
    - src/authority/private-source-set.ts
    - src/commands/import-authority.ts
    - src/commands/validate-authority.ts
    - tests/private-authority/private-revision.test.ts
    - tests/private-authority/repository-boundary.test.ts (if it exists)
  </read_first>
  <files>package.json, tests/private-authority/repository-boundary.test.ts, .planning/phases/01-rules-and-data-authority/01-08-SUMMARY.md</files>
  <action>Create `tests/private-authority/repository-boundary.test.ts` as the focused isolation/leakage gate and add `authority:verify-private` to `package.json` as `node --test tests/private-authority/*.test.ts`; leave default `test`/`verify` unchanged and clean-clone-safe. Before dynamically importing the source-set helper, importer, or validator, install throwing sentinels for `globalThis.fetch` and `node:http`, `node:https`, and `node:net` request/connect entry points. Under those sentinels, reverify the private source sets, import once to a verified temp root from the locked private input, and validate using exactly revision-root plus relative `bundle.json`; any network attempt fails. Inspect source/package scripts and allow acquisition code only at `scripts/collect-private-authority.ps1` when its committed loopback suite proves the fixed seven-source manifest, inert dot-source guard, explicit acknowledgment, no artwork, no recurring/retry/evasion surface, and production-inaccessible test seams; fail on every other acquisition client, fetch/poll/scrape/update command, public/network HTTP card API surface, mutable-latest selection, or new dependency. Enumerate every blob reachable from all refs with `git rev-list --objects --all` plus Git worktree/tracked paths/blobs, staged/index paths/blobs, and `pnpm pack --dry-run --json`: fail on any `.local/authority` path, any exact SHA-256 from all seven primary/backup files, any selected raw/normalized/revision artifact hash, full-corpus JSON shape/size, publisher PDF/HTML page or card-art extension/magic, absolute private locator, or copied Contested Realms/spells.bar/community corpus marker. Allow only the safe relative-path/URL/date/media/hash receipt, README, project code, synthetic/minimal fixtures, and generic tests. Stage the five frontmatter-listed repository files and any changed normal tracking, then run the full gate once without committing to obtain the leakage result. Create `01-08-SUMMARY.md` with `acquisitionMethod`, `authorizationReference`, confirmation that the matching consumed record passed without copying its timestamp or locator, source-set root, both importer roots/tree hashes, selected/candidate bundle IDs/hashes, tamper count, no-network result, leakage scan result, and selected before/after map—never source bytes, excerpts, or absolute private locators. Restage together all five frontmatter-listed repository files, this summary, and every changed normal tracking artifact; no Plan 08 implementation artifact may already be committed. Run `pnpm verify` and `pnpm authority:verify-private` against that exact staged set, requiring the enhanced boundary test to pass its reachable-history, worktree, staged/index, and package scans. Only then create the first Plan 08 implementation commit from the already-scanned staged set. If any intended commit content changes after the gate, restage everything and rerun the full gate before committing; no intermediate or follow-up Plan 08 commit may contain unscanned content.</action>
  <verify>
    <automated>node --input-type=module -e "import cp from 'node:child_process';const s=new Set(cp.execFileSync('git',['diff','--cached','--name-only'],{encoding:'utf8'}).trim().split(/\r?\n/)),required=['package.json','data/authority/README.md','data/authority/receipts/official-2026-08-20.json','tests/private-authority/private-revision.test.ts','tests/private-authority/repository-boundary.test.ts','.planning/phases/01-rules-and-data-authority/01-08-SUMMARY.md'];if(required.some(x=&gt;!s.has(x)))process.exit(1)" &amp;&amp; pnpm verify &amp;&amp; pnpm authority:verify-private</automated>
  </verify>
  <acceptance_criteria>
    - The boundary test dynamically exercises helper/import/validation while fetch/http/https/net sentinels are installed and observes no network call.
    - No acquisition/update/public API/mutable-latest path or new dependency exists except the exact audited fixed one-shot collector; no runtime/import/validation path can invoke it.
    - Before the first Plan 08 implementation commit, reachable-history, worktree, staged/index, and package dry-run scans compare all seven exact private source hashes plus selected raw/normalized/revision hashes and find no restricted bytes or private absolute locator.
    - All five frontmatter-listed files, the summary, and changed normal tracking are staged together before the gate; no intermediate commit exists, and any post-gate change requires restaging and a full rerun before commit.
    - Default `pnpm verify` passes without private data on a clean clone; explicit `pnpm authority:verify-private` fails rather than skips when private evidence is absent.
    - `01-08-SUMMARY.md` records hashes/counts/results only and contains no source content or machine-specific private path.
  </acceptance_criteria>
  <done>The separate boundary gate proves the full private build remains offline and no official corpus, PDF/page, art, normalized data, revision, or private locator leaks through Git or packaging.</done>
</task>

</tasks>

<verification>
Before the first Plan 08 implementation commit, stage all five frontmatter-listed files, `01-08-SUMMARY.md`, and changed normal tracking together. Run `pnpm verify && pnpm authority:verify-private`; independently inspect all reachable blobs via `git rev-list --objects --all`, `git status --short`, `git ls-files .local/authority`, every staged/index blob, and `pnpm pack --dry-run --json`. Require the two-form structural evidence gate, the exact current retry pair and matching immutable consumed record, full-set helper verification, byte-identical primary/backup derived inputs and revisions, exact CLI validation, all tamper failures, unchanged selected revision, quiet network sentinels, and zero official/private content leakage. Commit only that scanned staged set; restage and rerun the gate after any change.
</verification>

<success_criteria>
- DATA-01: the current rulebook, base format, Codex, FAQs, changelog, card updates, and card API are all privately pinned by exact URL/date/media/hash and represented in one write-once offline-valid bundle.
- DATA-02: either complete independent source set produces the same versioned normalized card snapshot and complete revision without network access.
- DATA-03: stable IDs, common envelopes, provenance, hashes, derivation parents, and reference graph validate or fail closed.
- Runtime selection is local-only by `bundle:official-2026-08-20` plus exact root hash; no runtime fetch, polling, scraping, latest alias, public HTTP API, or official content exists in Git/package, and the separately tested fixed one-shot collector is unreachable from runtime/import/validation paths.
- Only the five frontmatter-listed source files plus normal summary/tracking are committed; every official/private byte remains ignored or in the independent backup.
</success_criteria>

<source_coverage_audit>

| Source | Item | Coverage | Evidence |
|--------|------|----------|----------|
| GOAL | Reproduce exact official rules, format, card data, and provenance | COVERED | Task 1 binds all seven authority inputs; Task 2 double-builds; Task 3 proves isolation. |
| REQ | DATA-01 | COVERED | Tasks 1-2 build/validate from independently hashed rulebook, format, Codex/FAQ/changelog/update, and card-data bytes. |
| REQ | DATA-02 | COVERED | Task 2 independently rebuilds from both complete source roots and compares every revision byte. |
| REQ | DATA-03 | COVERED | Tasks 1-2 preserve common envelopes, IDs, source/derivation refs, and validate/tamper the graph. |
| RESEARCH | Strict bounded canonical offline pipeline | COVERED | Exact source-set/input locks, 10MB/32MB importer limits, canonical generation, write-once build, external hashes, and network denial. |
| RESEARCH | API-page/Terms/robots conflict and clean-room boundary | COVERED | Fixed one-shot risk acknowledgment, exact current acquisition pair, manifest-only permission-required records, no legal claim, and leakage/community scans. |
| CONTEXT | D-01 | COVERED | Exact rulebook/format/Codex-FAQ/card-update/card-data inputs are official-only normative sources. |
| CONTEXT | D-02 | COVERED | Formats/source metadata retain dates/scope/supersession and ambiguity stays fail-closed. |
| CONTEXT | D-03 | COVERED | README/receipt and gates select only the validated local bundle by fixed ID/root hash. |
| CONTEXT | D-04 | COVERED | New revision is write-once and records URL, retrieval/effective dates, media type, SHA-256, and derivation. |
| CONTEXT | D-05 | COVERED | Pinned card bytes normalize deterministically while retaining source IDs plus stable project IDs. |
| CONTEXT | D-06 | COVERED | Bundle, source manifest, card snapshot, and formats use the common provenance envelope. |
| CONTEXT | D-07 | COVERED | One project canonical serializer precedes SHA-256; exact-path validation never repairs/drops data. |
| CONTEXT | D-08 amended | COVERED | The gate recognizes only legacy user-run/no-reference and closed agent-run/exact-reference-plus-record evidence, while this build requires the verified retry pair and its matching consumed record; all raw/normalized/built bytes remain private and ignored. |
| CONTEXT | D-09 | COVERED | Plan 06 policy and Task 3 prevent copied external implementation/community material. |
| CONTEXT | D-10 | COVERED | Helper/gates use TypeScript, Node built-ins, and existing pinned dependencies only. |
| CONTEXT | D-11 | COVERED | Private/noncommercial/no redistribution/no public API scope remains; broader use needs permission and a separate plan. |

</source_coverage_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| complete primary/backup source sets -> derived importer inputs | Malicious/missing/substituted bytes or metadata could omit authority, exhaust resources, or forge provenance. |
| derived input roots -> importer/private revision | Wrong lock modes/order, path aliases, self-reported roots, or partial writes could break determinism/integrity. |
| private revision -> local runtime | Consumers could select mutable/latest/network data rather than fixed ID/hash. |
| all private roots -> Git/package | Official corpus, PDF/pages, normalized data, local locators, or copied external material could leak. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-36 | Tampering / DoS | full source-set and derived JSON | mitigate | Reusable exact-set verifier, streaming source bounds, 10MB-per/32MB-total importer bounds, strict JSON/schema/cardinality diagnostics, and duplicate/extra rejection. |
| T-01-37 | Tampering / Elevation | source/build paths and backup identity | mitigate | Repeat realpath/path.relative/platform-aware containment and dev/ino/symlink/junction/hardlink checks; use fixed ignored roots and verified temp cleanup. |
| T-01-38 | Spoofing | source substitution/incomplete DATA-01 evidence | mitigate | Locally rehash every one of seven files in both roots, require exact metadata/set/root equality, derive all manifest hashes from those bytes, and state hashes are not authenticity proof. |
| T-01-39 | Tampering / Repudiation | input locks/artifact/reference graph | mitigate | Exact lock order/modes, external candidate hashes/fixed stable ID, independent recomputation, full-set double build, derivation/ref/cycle tamper cases, and selected before/after immutability. |
| T-01-40 | Information disclosure / legal | restricted source/corpus publication | mitigate | Before the first Plan 08 implementation commit, reachable-history/worktree/staged-index/package scans compare all private hashes/extensions/magic/content and reject absolute locator or external/community copy leakage. |
| T-01-41 | Spoofing / Information disclosure | offline runtime/network surface | mitigate | Exact local ID/root contract, fetch/http/https/net sentinels during real helper/import/validation, and a source/script scan allowing only the separately tested fixed one-shot collector while rejecting every other acquisition/public/latest path. |
| T-01-SC | Tampering | package installs | accept | No package is added; all new work uses Node/TypeScript and existing locked dependencies. |
</threat_model>

<must_haves>
- DATA-01/02/03 and D-01 through D-11 all have explicit task/source-audit coverage; all remain pending until this plan and phase verification pass.
- Reproducibility means either complete seven-file independent private source set produces byte-identical derived importer inputs and revision bytes; Git alone is explicitly insufficient.
- The structural gate recognizes only legacy user-run/no-reference and closed agent-run/exact-reference-plus-record evidence; the private lock, Plan 07 summary, safe Plan 08 receipt, and durable consumed record for this build all agree exactly on `user-authorized-agent-run-one-shot-powershell` / `quick-260825-mhh-retry-1`, and arbitrary, case-changed, padded, mismatched, missing-consumption, failed-attempt-substitution, or reuse cases fail closed.
- The completed importer/validator contracts are used exactly: relative input lock, fixed lock order/modes/bounds, deterministic bundle ID, candidate hashes only from stdout, and revision-root-relative validation.
- Deterministic/tamper integrity and no-network/Git-package leakage are separate focused tests with runnable commands.
- No Plan 08 tracked artifact enters reachable history until all intended outputs are staged together and the reachable-history/worktree/index/package leakage gate passes; any changed commit content is restaged and rescanned.
</must_haves>

<output>
Run the preliminary leakage gate on the staged five frontmatter-listed repository files and changed normal tracking without committing, then create `.planning/phases/01-rules-and-data-authority/01-08-SUMMARY.md` from those results and restage the complete set before the final leakage gate. Make the first Plan 08 implementation commit only after the enhanced reachable-history/worktree/staged-index/package gate passes on that exact staged set; if any content changes, restage and rerun before commit. Never force-add `.local/authority/` or the independent backup. Leave DATA-01/02/03 pending until phase verification confirms this output.
</output>
