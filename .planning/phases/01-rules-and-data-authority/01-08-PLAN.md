---
phase: 01-rules-and-data-authority
plan: "08"
type: execute
wave: 6
depends_on: [01-07]
files_modified:
  - package.json
  - data/authority/README.md
  - data/authority/receipts/official-2026-08-20.json
  - tests/private-authority/private-revision.test.ts
autonomous: true
requirements: [DATA-01, DATA-02, DATA-03]
must_haves:
  truths:
    - "The official `official-2026-08-20` authority revision is built and validated entirely offline under Git-ignored `.local/authority/` from the exact Plan 07 primary/backup bytes."
    - "A second build from the durable backup produces the same complete relative-path and byte map as the primary build."
    - "Runtime consumers can select the private revision by recorded stable ID and independently computed root hash, never by a network or mutable-latest path."
    - "The committed receipt contains provenance and hashes but no card records, art, publisher PDF, raw response, normalized corpus, or built revision bytes."
    - "The final gate rejects source substitution, malicious/oversized JSON, symlinks/path escapes, artifact/reference tampering, any network call, and any official corpus/art/PDF leakage into Git or package contents."
    - "Default `pnpm test` remains runnable on a clean clone with synthetic fixtures; the explicit `pnpm authority:verify-private` gate requires the user's private files and fails rather than skips when they are absent."
  artifacts:
    - path: ".local/authority/revisions/official-2026-08-20"
      provides: "Validated private official authority revision; Git-ignored and never committed or packaged"
    - path: "data/authority/receipts/official-2026-08-20.json"
      provides: "Safe non-content source/input/revision hash receipt and durable private backup locator"
      contains: "official-2026-08-20"
    - path: "data/authority/README.md"
      provides: "Private-local rebuild, selection, backup, non-release, and Git-insufficiency instructions"
    - path: "tests/private-authority/private-revision.test.ts"
      provides: "Opt-in independent double-build, tamper, no-network, and restricted-content release gate"
    - path: "package.json"
      provides: "Explicit authority:verify-private developer command outside the clean-clone default test glob"
  key_links:
    - from: ".planning/phases/01-rules-and-data-authority/01-07-SUMMARY.md"
      to: "data/authority/receipts/official-2026-08-20.json"
      via: "source URL/date, primary/backup locator, byte length, SHA-256, and private-use attestation copied without corpus content"
      pattern: "sha256"
    - from: ".local/authority/inputs/official-2026-08-20"
      to: ".local/authority/revisions/official-2026-08-20"
      via: "authority:import with exact input lock and independently expected input-root hash"
      pattern: "official-2026-08-20"
    - from: "tests/private-authority/private-revision.test.ts"
      to: "src/commands/import-authority.ts"
      via: "two isolated imports from independently rehashed primary and backup bytes with network denial installed"
      pattern: "importAuthority"
    - from: "tests/private-authority/private-revision.test.ts"
      to: "src/authority/validate-bundle.ts"
      via: "validation/tamper checks use receipt stable ID and independently recomputed root hash"
      pattern: "validateAuthorityBundle"
    - from: "data/authority/receipts/official-2026-08-20.json"
      to: ".local/authority/revisions/official-2026-08-20/bundle.json"
      via: "offline runtime selection by exact revision ID, stable bundle ID, and root hash"
      pattern: "bundleRootHash"
---

<objective>
Build the real official authority revision privately, prove its exact reproducibility and integrity, and commit only a safe non-content receipt, instructions, and generic final-gate test.

Purpose: Complete DATA-01/02/03 honestly: reproducibility comes from the user's durable private exact input plus its recorded hash, not from Git alone.
Output: A validated Git-ignored local revision, safe receipt/README, and an explicit independent private-revision verification command.
</objective>

<execution_context>
@C:/Users/Dad/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Dad/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.gitignore
@package.json
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-rules-and-data-authority/01-CONTEXT.md
@.planning/phases/01-rules-and-data-authority/01-DISCUSSION-LOG.md
@.planning/phases/01-rules-and-data-authority/01-RESEARCH.md
@.planning/phases/01-rules-and-data-authority/01-VALIDATION.md
@.planning/phases/01-rules-and-data-authority/01-05-SUMMARY.md
@.planning/phases/01-rules-and-data-authority/01-06-SUMMARY.md
@.planning/phases/01-rules-and-data-authority/01-07-SUMMARY.md
@docs/authority-precedence.md
@docs/external-reuse-policy.md
@src/commands/import-authority.ts
@src/commands/validate-authority.ts
@src/authority/canonical-json.ts
@src/authority/hash.ts
@src/authority/validate-bundle.ts
@tests/authority/bundle.test.ts
@tests/authority/card-snapshot.test.ts
@tests/authority/fixtures/bundle-input/input-lock.json
</context>

<interfaces>
From `src/commands/import-authority.ts`:
- `ImportAuthorityArguments = { inputRoot, inputLockPath, expectedInputRootHash, outputRoot, revisionId }`.
- `ImportAuthorityResult = { revisionPath, bundleId, verifiedInputRootHash, bundleRootHash }`.
- CLI flags are exactly `--input`, `--input-lock`, `--expected-input-root-hash`, `--output-root`, and `--revision-id`.

From `src/commands/validate-authority.ts`:
- CLI flags are exactly `--root`, `--bundle`, `--id`, and `--hash`.
- Validation takes expected stable ID/root hash externally; it must not trust the candidate bundle as its oracle.

From the implemented input contract:
- An input root contains exactly `cards.raw.json`, `formats.json`, and `sources.json`; its canonical `input-lock.json` records every relative path, byte length, lowercase SHA-256, and input-root hash.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Build and receipt the private official revision</name>
  <read_first>
    - .gitignore
    - .planning/phases/01-rules-and-data-authority/01-CONTEXT.md
    - .planning/phases/01-rules-and-data-authority/01-RESEARCH.md
    - .planning/phases/01-rules-and-data-authority/01-07-SUMMARY.md
    - docs/authority-precedence.md
    - docs/external-reuse-policy.md
    - src/commands/import-authority.ts
    - src/commands/validate-authority.ts
    - src/authority/canonical-json.ts
    - tests/authority/fixtures/bundle-input/input-lock.json
    - data/authority/README.md (if it exists)
    - data/authority/receipts/official-2026-08-20.json (if it exists)
  </read_first>
  <files>.local/authority/inputs/official-2026-08-20/{cards.raw.json,sources.json,formats.json,input-lock.json} (private, Git-ignored), .local/authority/revisions/official-2026-08-20 (private, Git-ignored), data/authority/README.md, data/authority/receipts/official-2026-08-20.json</files>
  <action>Abort before writing anything if `01-07-SUMMARY.md` lacks the exact API URL, UTC retrieval timestamp, fixed primary path, absolute durable backup locator, byte length, lowercase SHA-256, complete private/noncommercial/no-redistribution/manual-browser/no-art-or-PDF attestation, or no-legal-conclusion statement. Independently `lstat` and hash the fixed primary plus recorded backup again; require ordinary non-symlink files, the backup outside the repository, exact byte-length/hash equality with each other and the Plan 07 values, valid JSON, and the importer size/depth/record bounds. Prove `.local/authority/` is ignored and untracked before building. Under `.local/authority/inputs/official-2026-08-20/`, create only the canonical local `sources.json`, `formats.json`, and `input-lock.json` needed by the implemented importer; keep the existing user-supplied `cards.raw.json` byte-for-byte unchanged. `sources.json` must use only official authority records, include the API URL/date/raw SHA-256 and the current official rulebook/format/Codex-FAQ/card-update URL/date/hash provenance required by D-01/D-02/D-04, mark the card response as `manifest-only` with `licenseStatus: permission-required`, `storagePolicy: prohibited`, and `durableLocator: urn:sha256:<raw digest>` so the raw response is never copied into the built revision. Keep every non-card official source manifest-only; use only independently observed hashes already recorded by the source audit, and stop rather than download bytes or fabricate a missing hash. Contain no copyrighted rule text or external-community authority. `formats.json` must separate base Constructed rules from explicitly selected overlays per D-02 and cite source refs; it stays private. Generate `input-lock.json` canonically from exactly those three files and independently compute its `inputRootHash`; never fetch, poll, scrape, or use a network-derived timestamp. Refuse any existing `.local/authority/revisions/official-2026-08-20`; run `pnpm authority:import` with the exact five implemented flags and the independently computed expected input-root hash, targeting `.local/authority/revisions` and revision ID `official-2026-08-20`. Capture the command's `bundleId`, verified input-root hash, and bundle-root hash from stdout; independently recompute `identityHash(bundle.identity)`, require equality, then run `pnpm authority:validate` with the captured external stable ID/hash. Create `data/authority/receipts/official-2026-08-20.json` containing only schema version, revision ID, bundle stable ID/root hash, verified input-root hash, API URL/retrieval date, raw byte length/SHA-256, fixed primary locator, durable backup locator, D-11 scope attestation, and `gitContainsCorpus: false`; include no card records/excerpts or raw/normalized bytes. Create `data/authority/README.md` stating that all real input/normalized/build bytes stay beneath ignored `.local/authority/`, Git alone cannot rebuild the revision, another build requires either exact private copy matching the receipt, runtime consumers select the local revision by ID/hash, normal tests use synthetic fixtures, and future sharing/release/automation/commercialization/public API work requires written permission and a separate plan. Commit only the README and receipt after external-ID/hash validation and the automated checks pass; never add `.local/authority/` with force.</action>
  <verify>
    <automated>node -e "const fs=require('node:fs'),p=require('node:path'),c=require('node:crypto'),cp=require('node:child_process');const r=JSON.parse(fs.readFileSync('data/authority/receipts/official-2026-08-20.json','utf8')),a=fs.readFileSync('.local/authority/inputs/official-2026-08-20/cards.raw.json'),b=fs.readFileSync(r.backupLocator),h=x=&gt;'sha256:'+c.createHash('sha256').update(x).digest('hex');if(a.length!==r.rawByteLength||b.length!==a.length||h(a)!==r.rawSha256||h(b)!==r.rawSha256)process.exit(1);if(cp.spawnSync('git',['check-ignore','-q','.local/authority/revisions/official-2026-08-20']).status!==0)process.exit(1);if(cp.execFileSync('git',['ls-files','.local/authority'],{encoding:'utf8'}).trim())process.exit(1);const q=['--root','.local/authority/revisions/official-2026-08-20','--bundle','.local/authority/revisions/official-2026-08-20/bundle.json','--id',r.bundleId,'--hash',r.bundleRootHash];const v=cp.spawnSync('pnpm',['authority:validate','--',...q],{stdio:'inherit',shell:process.platform==='win32'});process.exit(v.status??1)"</automated>
  </verify>
  <acceptance_criteria>
    - The primary and durable backup are independently rehashed as ordinary non-symlink files and match Plan 07 byte length/SHA-256 before any build.
    - The canonical private input lock covers exactly `cards.raw.json`, `formats.json`, and `sources.json`; the raw response is unchanged and every source/format record is official, scoped, and provenance-linked.
    - `.local/authority/revisions/official-2026-08-20` is created write-once and validates offline with the stable ID/root hash captured outside and independently recomputed from the candidate.
    - `data/authority/receipts/official-2026-08-20.json` and `data/authority/README.md` contain only non-content provenance/rebuild metadata and explicitly say Git alone is insufficient.
    - D-03 is preserved: README/receipt/runtime instructions select only the validated local revision by exact stable ID and content hash.
    - D-04 is preserved: the new revision is write-once and records URL, retrieval/effective dates, media type, SHA-256, and derivation metadata.
    - D-05 is preserved: deterministic schema-versioned normalization retains official source IDs and separate stable project IDs from the pinned raw input.
    - D-06 is preserved: bundle, source-manifest, card-snapshot, and format artifacts use the common kind/stable-ID/schema-version/content-hash/parent-source-ref envelope.
    - D-07 is preserved: all hashes use the one project canonical serializer and validation reports exact paths without repair or dropped fields.
    - `git ls-files .local/authority` returns no paths; no raw input, normalized corpus, built revision, art, or PDF is committed or packaged.
  </acceptance_criteria>
  <done>The official revision exists and validates privately by exact ID/hash, while Git contains only its safe receipt and operating instructions.</done>
</task>

<task type="auto">
  <name>Task 2: Add and run the independent private-revision final gate</name>
  <read_first>
    - package.json
    - .gitignore
    - data/authority/README.md
    - data/authority/receipts/official-2026-08-20.json
    - .planning/phases/01-rules-and-data-authority/01-07-SUMMARY.md
    - src/commands/import-authority.ts
    - src/authority/canonical-json.ts
    - src/authority/hash.ts
    - src/authority/validate-bundle.ts
    - tests/authority/bundle.test.ts
    - tests/authority/provenance.test.ts
    - tests/private-authority/private-revision.test.ts (if it exists)
  </read_first>
  <files>package.json, tests/private-authority/private-revision.test.ts</files>
  <action>Create `tests/private-authority/private-revision.test.ts` outside the default `tests/authority/*.test.ts` glob and add `authority:verify-private` to `package.json` as `node --test tests/private-authority/private-revision.test.ts`; leave `test` and `verify` clean-clone-safe and synthetic. The opt-in test must fail, never skip, when the fixed primary, receipt, recorded backup, selected revision, or attestation is missing. Before importing project modules, install throwing sentinels for `globalThis.fetch`, `node:http`, `node:https`, and `node:net` connection/request entry points so any network path fails. Independently `lstat`, bound, parse, byte-count, and SHA-256 the primary and backup; reject symlinks, backup paths inside Git, receipt mismatch, extra/missing input-lock files, and source substitution. Build twice into distinct `mkdtemp` output roots: once from a private temporary input tree using the primary raw bytes and once from an independently constructed tree using the backup raw bytes, with canonical source/format metadata and separately recomputed locks/expected input-root hashes. Compare the complete sorted relative-path, byte-length, and SHA-256 maps and require byte-identical output. Independently recompute each bundle identity/root hash and validate both builds plus the selected private revision using only the receipt's external stable ID/hash. Create isolated tamper cases for bundle identity, normalized cards, formats, sources, isolated copies of the primary raw input/backup, manifest durable-locator/byteHash bindings, every `SourceRef`, and a reference cycle; require every tamper to fail without changing the selected revision. Capture a before/after recursive path/byte/hash map of the selected revision and require exact equality after all failures. Enumerate tracked and staged Git paths/blobs plus `pnpm pack --dry-run --json` output: fail on any `.local/authority` path, exact official raw/normalized corpus hash, full-corpus JSON shape/size, publisher PDF or card-art extension/magic, private revision artifact, or copied Contested Realms/spells.bar/community corpus marker; allow only the safe receipt/README and synthetic/minimal fixtures. Assert no acquisition dependency, URL-fetch command, or HTTP card API surface exists in source/package scripts. Always clean only verified temporary roots in `finally`. Run `pnpm verify` first, then the explicit private gate, and commit `package.json` plus the generic test atomically only after both pass. Record primary/backup SHA-256, both input-root hashes, both complete tree hashes, selected revision stable ID/root, tamper case count, no-network result, Git/package leakage result, and unchanged before/after map in `01-08-SUMMARY.md`; include no corpus bytes or card excerpts.</action>
  <verify>
    <automated>pnpm verify &amp;&amp; pnpm authority:verify-private</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm verify` passes without any private file on a clean clone; `pnpm authority:verify-private` fails rather than skips when private evidence is absent and passes with the Plan 07 primary/backup.
    - Network sentinels remain installed across both imports and validation, and any attempted fetch/http/https/net access fails the gate.
    - D-10 is preserved: the final gate uses TypeScript and Node standard-library APIs plus existing pinned dependencies and adds no package.
    - Independently constructed primary and backup builds have identical complete path/byte/hash maps and validate with the receipt's external stable ID/root hash.
    - Every identity/artifact/raw-input/manifest-binding/source-ref/cycle tamper case fails; the selected local revision's before/after map is byte-identical.
    - Git tracked/staged blobs and package dry-run contain no `.local/authority` data, full official raw/normalized corpus, private revision, card art, publisher PDF, or copied external/community material.
    - `01-08-SUMMARY.md` records only hashes/counts/results and no card content.
  </acceptance_criteria>
  <done>The opt-in gate independently proves exact private rebuild reproducibility, offline validation, tamper resistance, immutability, and zero restricted-content publication.</done>
</task>

</tasks>

<verification>
Run `pnpm verify && pnpm authority:verify-private`. Independently inspect `git status --short`, `git ls-files .local/authority`, `git diff --cached --name-only`, and `pnpm pack --dry-run --json`. Require the fixed private revision to validate by receipt ID/hash, the two independent builds to match byte-for-byte, all tamper cases to fail, all network sentinels to remain quiet, and no official corpus/art/PDF/private-revision content to be tracked, staged, or packaged.
</verification>

<success_criteria>
- DATA-01: one immutable private official revision records all required source metadata and validates offline by independently supplied stable ID/root hash.
- DATA-02: another build is byte-identical from either the user's exact private primary or durable backup matching the recorded hash; documentation explicitly says Git alone is insufficient.
- DATA-03: stable card IDs/source IDs/metadata survive normalization and every source/artifact/reference edge validates or fails closed.
- Runtime selection is local-only by revision ID/hash; no fetch, polling, scraping, latest alias, public/network HTTP card API, art, or PDF path exists.
- Only project code, policy/docs, safe hashes/receipt, synthetic/minimal fixtures, generic test, summary, and allowed planning tracking are committed.
</success_criteria>

<source_coverage_audit>

| Source | Item | Coverage | Evidence |
|--------|------|----------|----------|
| GOAL | Immutable, reproducible, provenance-rich official authority bundles | COVERED | Tasks 1-2 build private write-once output, double-build, validate, tamper, and receipt it. |
| REQ | DATA-01 | COVERED | Tasks 1-2 create/validate the official revision by external ID/hash and safe source receipt. |
| REQ | DATA-02 | COVERED | Task 2 rebuilds independently from primary and durable backup and compares every output byte; Git-alone insufficiency is explicit. |
| REQ | DATA-03 | COVERED | Task 1 requires official source refs/stable IDs; Task 2 tampers every binding and cycle. |
| RESEARCH | Strict bounded canonical offline pipeline | COVERED | Both tasks use implemented strict importer, canonical locks, independent hashes, network denial, and write-once outputs. |
| RESEARCH | API-page/Terms conflict and community-license audit | COVERED | Plan 06 policy plus Plan 07 attestation; Task 2 proves no acquisition/publication path or community corpus entered Git. |
| CONTEXT | D-01 | COVERED | Task 1 stages only official rulebook/format/Codex-FAQ/card-update/card-data provenance classes. |
| CONTEXT | D-02 | COVERED | Task 1 uses the documented fail-closed precedence policy; Task 2 tampers references and ambiguity paths. |
| CONTEXT | D-03 | COVERED | Task 1 README/receipt binds runtime consumers to one validated local revision by stable ID/content hash; Task 2 rejects network/latest fallbacks. |
| CONTEXT | D-04 | COVERED | Task 1 creates a new write-once revision and records URL, retrieval/effective dates, media type, SHA-256, and derivation metadata. |
| CONTEXT | D-05 | COVERED | Task 1 pins raw input and preserves official source IDs plus stable project IDs through deterministic schema-versioned normalization. |
| CONTEXT | D-06 | COVERED | Task 1/implemented schemas use the common provenance envelope for bundle, source manifest, card snapshot, and formats; Task 2 validates all links. |
| CONTEXT | D-07 | COVERED | Tasks 1-2 use the project canonical serializer before SHA-256 and require exact-path fail-closed validation without repair/drop. |
| CONTEXT | D-08 amended | COVERED | Plan 06 ignores `.local/authority/`; Plan 07 supplies one manual response; Tasks 1-2 keep all real bytes private and scan Git/package leakage. |
| CONTEXT | D-09 | COVERED | Plan 06 clean-room policy records audited external limits; Task 2 scans for copied reference/community content. |
| CONTEXT | D-10 | COVERED | Task 2 implements the opt-in gate with TypeScript/Node standard-library APIs and existing pinned dependencies; no package is added. |
| CONTEXT | D-11 | COVERED | Plans 06-07 and both tasks enforce private/noncommercial/no redistribution, no public API, and separate permission-gated expansion. |

</source_coverage_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user-supplied JSON/backup -> private staging | Untrusted, mutable, potentially oversized or structurally malicious bytes enter strict parsing. |
| private staging paths -> importer/output root | Traversal, symlinks, or substituted files could escape confinement or alter the build. |
| importer candidate -> selected private revision | Candidate-controlled IDs/hashes or partial writes could defeat integrity and immutability. |
| private revision -> offline runtime consumer | Runtime could select mutable/latest or network data instead of the receipt-bound revision. |
| private files -> Git/package | Official corpus, art, PDF, normalized data, or copied reference material could leak. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-36 | Tampering / DoS | malicious JSON | mitigate | Independent size/JSON checks plus implemented strict schema, depth/record bounds, duplicate rejection, and explicit failure tests run before build selection. |
| T-01-37 | Tampering / Elevation | path traversal/symlink escape | mitigate | Realpath/lstat every input, reject symlinks and out-of-root lock paths, use fixed ignored roots, validate temp-root ownership before cleanup, and retain existing confinement tests. |
| T-01-38 | Spoofing | source substitution | mitigate | Rehash primary and durable backup independently against Plan 07/receipt, require matching canonical locks, and build once from each exact copy. |
| T-01-39 | Tampering / Repudiation | artifact/reference graph | mitigate | Capture IDs/hashes outside candidates, independently recompute identity/tree maps, validate recursively, exercise artifact/raw-input/manifest-binding/ref/cycle tamper cases, and prove selected before/after immutability. |
| T-01-40 | Information disclosure / legal | restricted asset/corpus publication | mitigate | Ignore every real authority path, inspect tracked/staged Git blobs and package dry-run, compare exact corpus hashes and restricted magic/extensions, and fail on public/network/acquisition/community-copy markers. |
| T-01-41 | Spoofing / Information disclosure | offline runtime selection | mitigate | README/receipt require exact local revision ID/hash; final gate installs fetch/http/https/net denial and rejects latest/network fallback. |
| T-01-SC | Tampering | package installs | accept | No package is added; package scripts use built-in Node APIs and existing locked dependencies only. |
</threat_model>

<must_haves>
- DATA-01/02/03 and D-01 through D-11 all have explicit task or source-audit coverage; no source item is deferred or silently reduced.
- The only official-data path is the user's Plan 07 manual response plus matching durable private backup; every real byte stays below ignored `.local/authority/` or the recorded external backup.
- Reproducibility is honestly defined as another byte-identical build from the durable private exact bytes and recorded hash, not from Git alone.
- The explicit final gate covers canonicalization, strict paths, deterministic normalization, offline bundle validation, atomic immutability, cycles/tampering, storage policy, clean-room guardrails, network denial, and Git/package leakage.
</must_haves>

<output>
Create `.planning/phases/01-rules-and-data-authority/01-08-SUMMARY.md` after both tasks and all final gates pass. Commit only the four frontmatter-listed repository files plus the normal summary/tracking files; never force-add `.local/authority/` or the durable backup.
</output>
