---
phase: 01-rules-and-data-authority
plan: "09"
type: execute
wave: 7
depends_on: ["01-08"]
files_modified:
  - src/authority/schemas.ts
  - src/authority/official-card-api-adapter.ts
  - src/authority/normalize-cards.ts
  - tests/authority/card-snapshot.test.ts
  - tests/authority/provenance.test.ts
  - tests/authority/fixtures/cards-valid.json
  - tests/authority/fixtures/cards-duplicates.json
  - tests/authority/fixtures/cards-malformed.json
  - tests/authority/fixtures/bundle-input/cards.raw.json
  - tests/authority/fixtures/bundle-input/sources.json
  - tests/authority/fixtures/bundle-input/input-lock.json
autonomous: true
requirements: [DATA-02, DATA-03]
gap_closure: true
must_haves:
  truths:
    - "A normalized official card preserves avatar life and each air, earth, fire, and water threshold exactly."
    - "The same publisher card keeps one project card stable ID across authority revisions while each revision remains bound through SourceRef and content hash."
    - "Strict raw and normalized schemas reject missing, malformed, negative, unsafe-integer, or unknown gameplay fields without repair."
  artifacts:
    - path: "src/authority/schemas.ts"
      provides: "Required lossless life and four-element threshold contracts"
    - path: "src/authority/official-card-api-adapter.ts"
      provides: "Strict official API mapping that retains validated gameplay fields"
    - path: "src/authority/normalize-cards.ts"
      provides: "Lossless normalization and revision-independent official card identity"
    - path: "tests/authority/card-snapshot.test.ts"
      provides: "Gameplay-field and cross-revision identity regressions"
  key_links:
    - from: "src/authority/official-card-api-adapter.ts"
      to: "src/authority/schemas.ts"
      via: "adaptCard returns a complete RawCard accepted by validateRawCardSnapshot"
      pattern: "life|thresholds"
    - from: "src/authority/normalize-cards.ts"
      to: "src/authority/schemas.ts"
      via: "normalizeCard copies complete RawCard gameplay fields into NormalizedCard"
      pattern: "life|thresholds"
    - from: "src/authority/normalize-cards.ts"
      to: "CanonicalArtifact.sourceRefs"
      via: "logical card ID excludes revision-qualified sourceId while provenance retains it"
      pattern: "sourceRefs"
---

<objective>
Close CR-07 and CR-08 by making the official card snapshot lossless for game-critical fields and stable across authority refreshes.

Purpose: DATA-02 cannot be game-usable while validated life/threshold values are discarded, and DATA-03 cannot support saved decks or longitudinal comparison while a card's logical ID changes with its source revision.
Output: Required card schema fields, lossless adapter/normalizer wiring, revision-independent official card IDs, updated clean-room fixtures and byte locks, and focused regressions.
Scope note: The JSON fixture edits are a mechanical schema migration; the implementation remains one card-normalization subsystem and adds no new dependency or abstraction.
</objective>

<execution_context>
@C:/Users/Dad/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Dad/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-rules-and-data-authority/01-CONTEXT.md
@.planning/phases/01-rules-and-data-authority/01-RESEARCH.md
@.planning/phases/01-rules-and-data-authority/01-PATTERNS.md
@.planning/phases/01-rules-and-data-authority/01-VERIFICATION.md
@.planning/phases/01-rules-and-data-authority/01-REVIEW.md
@.planning/phases/01-rules-and-data-authority/01-03-SUMMARY.md
@.planning/phases/01-rules-and-data-authority/01-08-SUMMARY.md

<interfaces>
From src/authority/official-card-api-adapter.ts:
- `adaptOfficialCardApiSnapshot(input: unknown): RawCardSnapshot`
- The strict publisher `guardian` object already validates nullable nonnegative `life` and `thresholds: { air, earth, fire, water }` but `adaptCard` currently omits them.

From src/authority/normalize-cards.ts:
- `normalizeCards(rawBytes: Uint8Array, sourceMetadata: SourceMetadata): CanonicalArtifact&lt;NormalizedCardSnapshot&gt;`
- `normalizeCard` currently hashes `{ sourceId, sourceCardId }`, making official card IDs revision-dependent.

From src/authority/schemas.ts:
- `RawCard`, `NormalizedCard`, `rawCardSchema`, and `normalizedCardSchema` share the strict `cardFields` contract.
- `SourceMetadata.sourceId` is revision-qualified provenance and must remain in the artifact `SourceRef`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define required life and threshold contracts</name>
  <read_first>
    - .planning/phases/01-rules-and-data-authority/01-VERIFICATION.md
    - .planning/phases/01-rules-and-data-authority/01-REVIEW.md
    - src/authority/schemas.ts
    - tests/authority/card-snapshot.test.ts
    - tests/authority/fixtures/cards-valid.json
    - tests/authority/fixtures/cards-duplicates.json
    - tests/authority/fixtures/cards-malformed.json
  </read_first>
  <files>src/authority/schemas.ts, tests/authority/card-snapshot.test.ts, tests/authority/fixtures/cards-valid.json, tests/authority/fixtures/cards-duplicates.json, tests/authority/fixtures/cards-malformed.json</files>
  <behavior>
    - RawCard and NormalizedCard require nullable nonnegative safe-integer `life` and a strict thresholds object with exactly air, earth, fire, and water nonnegative safe integers.
    - Missing, negative, fractional, unsafe-integer, or unknown life/threshold fields fail at deterministic JSON Pointer paths.
    - Synthetic fixtures remain independently authored and explicitly provide all required fields; malformed fixtures fail only for their intended defects.
  </behavior>
  <action>Write failing schema assertions first, then extend `RawCard`, `NormalizedCard`, and the shared strict Zod `cardFields` in `schemas.ts` with `life: number | null` and `thresholds: { air: number; earth: number; fire: number; water: number }`. Require all fields rather than defaulting, coercing, or repairing them per D-05/D-07. Update the three clean-room card fixtures with explicit values so duplicate/malformed tests retain their existing intended diagnostics. Keep threshold key names aligned with the already-audited official adapter input; do not introduce an element array, generic stats bag, schema version fork, or dependency.</action>
  <verify>
    <automated>node --test --test-name-pattern="life|threshold|schema" tests/authority/card-snapshot.test.ts &amp;&amp; pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Raw and normalized card types expose required `life` and the exact four threshold keys.
    - Strict validators accept nullable life plus zero/nonzero threshold combinations and reject every malformed case at an exact path.
    - No `.default`, coercion, optional field, unknown-key stripping, or silent compatibility repair is introduced.
  </acceptance_criteria>
  <done>D-05/D-07 card contracts can represent every audited avatar-life and elemental-threshold value without omission or repair.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve gameplay fields through adaptation and normalization</name>
  <read_first>
    - .planning/phases/01-rules-and-data-authority/01-CONTEXT.md
    - src/authority/schemas.ts
    - src/authority/official-card-api-adapter.ts
    - src/authority/normalize-cards.ts
    - tests/authority/card-snapshot.test.ts
    - tests/authority/provenance.test.ts
  </read_first>
  <files>src/authority/official-card-api-adapter.ts, src/authority/normalize-cards.ts, tests/authority/card-snapshot.test.ts, tests/authority/provenance.test.ts</files>
  <behavior>
    - Official API guardian life reaches RawCard.life and NormalizedCard.life unchanged, including null and a nonzero avatar value.
    - All four official threshold integers reach the normalized card unchanged and remain deeply frozen.
    - Reordering object properties changes neither normalized gameplay values nor semantic payload identity.
  </behavior>
  <action>Add failing adapter and end-to-end normalization assertions using a strict synthetic official-array row with nonzero, distinct thresholds and avatar life. Then make `adaptCard` copy `guardian.life` and a defensive four-key threshold object, and make `normalizeCard` copy both into the normalized record. Update the inline raw/normalized schema examples in `provenance.test.ts`. Preserve strict adapter rejection, cardinality, deterministic sorting, deep-freeze behavior, and original-byte SourceRef binding; do not infer thresholds from `elements`, use set metadata instead of guardian metadata, or discard zero/null values.</action>
  <verify>
    <automated>node --test --test-name-pattern="official API|life|threshold|deeply frozen" tests/authority/card-snapshot.test.ts &amp;&amp; node --test --test-name-pattern="schema exports" tests/authority/provenance.test.ts &amp;&amp; pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - A nonzero avatar life and four distinct threshold values survive official adaptation and normalization exactly.
    - Normalized threshold objects are defensively copied and frozen with the enclosing artifact.
    - Existing strict shape, unknown-field, cardinality, ordering, and provenance tests continue to pass.
  </acceptance_criteria>
  <done>The pinned official bytes produce a complete game-usable card record instead of a reproducible but lossy one.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Stabilize official card IDs across authority revisions</name>
  <read_first>
    - .planning/phases/01-rules-and-data-authority/01-CONTEXT.md
    - .planning/phases/01-rules-and-data-authority/01-VERIFICATION.md
    - src/authority/hash.ts
    - src/authority/normalize-cards.ts
    - tests/authority/card-snapshot.test.ts
    - tests/authority/fixtures/bundle-input/cards.raw.json
    - tests/authority/fixtures/bundle-input/sources.json
    - tests/authority/fixtures/bundle-input/input-lock.json
  </read_first>
  <files>src/authority/normalize-cards.ts, tests/authority/card-snapshot.test.ts, tests/authority/fixtures/bundle-input/cards.raw.json, tests/authority/fixtures/bundle-input/sources.json, tests/authority/fixtures/bundle-input/input-lock.json</files>
  <behavior>
    - The same official publisher card ID produces the same `card:*` stable ID under two different revision-qualified source IDs.
    - The two artifacts still retain their distinct source IDs/byte hashes and therefore retain revision-specific provenance and content hashes.
    - Different publisher card IDs remain different, and display/order changes still do not affect logical card identity.
  </behavior>
  <action>Write the cross-revision regression first. For `authorityClass: official`, derive the card stable hash from the fixed revision-independent publisher namespace `sorcerytcg` plus `sourceCardId`; never include the revision-qualified `source.sourceId`. Keep `source.sourceId` and byte hash in the artifact SourceRef exactly as D-04/D-05/D-06 require. Preserve the existing non-official synthetic behavior by using its source identity as the namespace rather than promoting it to official. Add required life/threshold values to the bundle-input card fixture, then recompute its source byte hash, exact input-lock file hash, and canonical input-root hash with the existing project hashing/canonicalization helpers; do not hand-wave or bypass the byte lock.</action>
  <verify>
    <automated>node --test --test-name-pattern="cross-revision|stable ID|input-lock" tests/authority/card-snapshot.test.ts tests/authority/bundle.test.ts &amp;&amp; pnpm verify</automated>
  </verify>
  <acceptance_criteria>
    - Identical official publisher card IDs have identical stable IDs across two source revisions.
    - SourceRefs and artifact content hashes continue to distinguish different authority bytes/revisions.
    - The bundle-input fixture, source record, per-file lock hashes, and canonical input-root hash agree exactly and the complete default suite passes.
  </acceptance_criteria>
  <done>DATA-02 remains reproducible and DATA-03 logical card identity survives authority refreshes without weakening immutable provenance.</done>
</task>

</tasks>

<gap_coverage>
- CR-07: closed by Tasks 1-2 with required lossless life/threshold schemas and end-to-end regressions.
- CR-08: closed by Task 3 with publisher-scoped revision-independent card IDs and revision-specific SourceRefs.
</gap_coverage>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| official API JSON -> RawCard | Untrusted publisher-shaped input can omit, overflow, or mislabel game-critical values. |
| RawCard -> canonical NormalizedCard | A lossy transformation or mutable nested value can corrupt future rule execution. |
| source revision -> logical identity | Revision metadata can accidentally become durable game/deck identity. |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan |
|-----------|----------|----------|-----------|-------------|-----------------|
| T-01-42 | Tampering | high | card schema/adapter boundary | mitigate (blocking) | Required strict life/threshold fields, exact-path negatives, and lossless end-to-end assertions must pass before completion. |
| T-01-43 | Tampering / Repudiation | high | card stable IDs | mitigate (blocking) | Fixed publisher namespace plus publisher card ID; revision remains in SourceRef/content hash; cross-revision regression blocks release. |
| T-01-44 | Denial of service | medium | numeric/card input | mitigate | Retain safe-integer/nonnegative validators and existing record/node bounds. |
| T-01-SC | Tampering | low | package installs | accept | No package is added; use existing Zod and Node/project helpers only. |
</threat_model>

<verification>
Run `pnpm verify`. Confirm the targeted official-array test preserves nonzero life and four distinct thresholds, the cross-revision test keeps one card stable ID while SourceRefs differ, byte-locked fixtures validate, and no network/dependency/UI/database code was added.
</verification>

<success_criteria>
- Official adapter and normalized snapshot retain avatar life and every elemental threshold exactly.
- Required strict schemas reject incomplete or malformed gameplay fields without repair.
- Official card IDs are stable across authority revisions; immutable source/artifact evidence remains revision-specific.
- All default tests, typecheck, and lint pass with updated exact fixture locks.
</success_criteria>

<output>
Create `.planning/phases/01-rules-and-data-authority/01-09-SUMMARY.md` when done.
</output>
