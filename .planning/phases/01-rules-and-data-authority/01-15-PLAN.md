---
phase: 01-rules-and-data-authority
plan: "15"
type: execute
wave: 9
depends_on: ["01-14"]
files_modified:
  - scripts/collect-private-authority.ps1
  - tests/authority/private-authority-collector.test.ts
  - docs/external-reuse-policy.md
  - tests/private-authority/private-source-completeness.test.ts
  - tests/private-authority/repository-boundary.test.ts
autonomous: false
requirements: [DATA-01, DATA-02, DATA-03]
gap_closure: true
must_haves:
  truths:
    - "The project performs no new official-source network acquisition; the user manually saves the fixed seven non-artwork files into one ignored v3 inbox."
    - "Offline intake validates the inbox before copying it to fresh primary and independent backup roots, then writes the ignored lock last."
    - "Both fresh roots pass the reusable content verifier and verifyPrivateSourceSet offline and retain identical before/after path-length-SHA-256 maps."
    - "The deterministic engine remains the sole rules authority; later LLM player and cited-adviser skills cannot mutate state or make an adjudicated game ranked."
    - "No old immutable evidence changes, and Plan 13 remains unreachable until every Plan 15 gate passes."
  artifacts:
    - path: "scripts/collect-private-authority.ps1"
      provides: "Offline-only fixed v3 manual intake and historical-root verification"
    - path: "tests/authority/private-authority-collector.test.ts"
      provides: "Synthetic manual-intake, atomic-publication, no-network, and no-leakage regressions"
    - path: "docs/external-reuse-policy.md"
      provides: "Manual-provision instructions and unchanged private-use boundary"
    - path: ".local/authority/locks/official-2026-08-27-v3/source-set-lock.json"
      provides: "Ignored exact seven-source v3 lock for the manual primary and independent backup"
    - path: "tests/private-authority/private-source-completeness.test.ts"
      provides: "Offline fresh-root content, structure, immutability, no-transport, and sanitized-output proof"
  key_links:
    - from: ".local/authority/manual-inbox/official-2026-08-27-v3"
      to: ".local/authority/locks/official-2026-08-27-v3/source-set-lock.json"
      via: "one offline intake publishes the lock last after validating two fresh seven-source roots"
      pattern: "official-2026-08-27-v3"
    - from: ".local/authority/locks/official-2026-08-27-v3/source-set-lock.json"
      to: "scripts/collect-private-authority.ps1 -VerifyExistingRoots"
      via: "the reusable verifier applies exact content checks without transport or mutation"
      pattern: "VerifyExistingRoots"
---

<objective>
Turn the user's manually saved fixed seven official non-artwork files into one fresh, complete, immutable private authority source set without project or agent network acquisition.

Purpose: The historical roots are byte-stable but fail the strengthened content contract. User-provided local files avoid a new automated collection path while preserving the offline authority and privacy gates required by Plan 13.
Output: Fresh v3 primary/backup/lock evidence, an offline immutable completeness proof, and no broader acquisition or distribution authority.
</objective>

<execution_context>
@C:/Users/Dad/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Dad/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-rules-and-data-authority/01-CONTEXT.md
@.planning/phases/01-rules-and-data-authority/01-VALIDATION.md
@.planning/phases/01-rules-and-data-authority/01-14-SUMMARY.md
@docs/external-reuse-policy.md
@scripts/collect-private-authority.ps1
@src/authority/private-source-set.ts
@tests/authority/private-authority-collector.test.ts

<decision>
The user selected Option 3 on 2026-08-27: manually provide the seven official documents/data files, keep original or placeholder art separate, let an LLM player choose only engine-enumerated actions, and permit an LLM rules adviser only as a cited non-authoritative aid. The player/adviser skills belong to Phase 7 after the Phase 2 action contract and Phase 4 coverage exist; original/placeholder art belongs to Phase 9. This plan implements only the Phase 1 manual authority intake.
</decision>

<interfaces>
- Fixed ignored inbox: .local/authority/manual-inbox/official-2026-08-27-v3.
- Fixed ignored primary: .local/authority/inputs/official-2026-08-27-v3/primary.
- Fixed ignored lock: .local/authority/locks/official-2026-08-27-v3/source-set-lock.json.
- Independent backup: fixed sibling sorcery-tcg-authority-backup-official-2026-08-27-v3 beneath the repository parent.
- Required paths: rulebook/rulebook-current.pdf, formats/constructed-current.html, codex/codex-current.html, codex/faqs-current.html, codex/changelog-current.html, updates/card-updates-2025.html, and cards/cards.raw.json.
- Lock identity: acquisitionMethod user-provided-manual-download with authorizationReference phase-01-20260827-manual-provision-1; the reference is workflow provenance only and grants no transport or legal permission.
- Completed Plan 14 interface: Assert-PrivateAuthorityContentSet plus -VerifyExistingRoots -LockPath <lock>.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace new production transport with fixed offline manual intake</name>
  <files>scripts/collect-private-authority.ps1, tests/authority/private-authority-collector.test.ts, docs/external-reuse-policy.md</files>
  <behavior>
    - The production CLI can either import the one fixed v3 manual inbox or verify an existing lock; it cannot perform HTTP transport.
    - Manual intake requires the explicit private-use acknowledgment, exact fixed inbox/primary/lock identities, and one absent independent backup destination.
    - The inbox is read-only input. Exactly seven ordinary path-contained files must pass the shared content verifier before publication.
    - Primary and backup are staged, structurally verified, moved to final destinations, reverified, and bound by a lock written last with acquisitionMethod user-provided-manual-download and the fixed non-authorizing manual-provision reference.
    - Failure leaves no lock, preserves the inbox, and removes or quarantines only intake-owned staging/publication artifacts; historical evidence stays immutable.
    - Output remains fixed/content-free, and tests contact only synthetic local data.
  </behavior>
  <action>Write RED tests for exact inbox identity, missing acknowledgment, absent/extra/linked/incomplete files, existing destinations, identical independent copies, write-lock-last ordering, failure cleanup, historical verification, sanitized output, and an AST/runtime canary proving the production entry and exports cannot reach network transport. Then reuse the existing path, content, copy, verifier, and write-new helpers to implement the smallest offline intake. Retire the unused consumed-authorization and network-collection surfaces, keep the fixed manual-provision reference as provenance only, and disable new production collection; retain historical locks only for read-only verification. Update the policy with exact manual save paths, private/Git/package boundaries, and no claim of publisher permission or legal clearance.</action>
  <verify>
    <automated>node --test --test-name-pattern="manual inbox|manual intake|offline|no transport|existing roots|sanitized" tests/authority/private-authority-collector.test.ts &amp;&amp; pnpm typecheck</automated>
  </verify>
  <done>The project has one fixed offline manual-intake path and no callable new production transport path.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: User saves the fixed seven files into the ignored manual inbox</name>
  <files>.local/authority/manual-inbox/official-2026-08-27-v3</files>
  <action>After Task 1 is committed and tests pass, pause. The user downloads the seven official non-artwork files in a normal browser and saves them under the exact required relative paths in the fixed ignored inbox. The user does not attach or upload the files to chat. Do not create primary, backup, or lock outputs yet, and do not paste source text, diagnostics, hashes, or private paths into tracked artifacts.</action>
  <verify>
    <human-check>The user replies manual-source-set-ready after all seven files exist locally.</human-check>
  </verify>
  <resume-signal>Type manual-source-set-ready after all seven files are saved locally.</resume-signal>
  <done>The fixed ignored manual inbox contains the user's seven locally saved files.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Import and prove both fresh roots complete, immutable, offline, and non-disclosing</name>
  <files>tests/private-authority/private-source-completeness.test.ts</files>
  <behavior>
    - Offline intake validates the manual inbox and publishes exactly one fresh primary, one independent backup, and one v3 lock.
    - Both roots pass verifyPrivateSourceSet and the shared content verifier with identical unchanged before/after maps.
    - The real verification invocation emits only fixed success text; no network/transport sentinel fires.
    - Missing, partial, stale, linked, aliased, changed, extra, content-incomplete, or diagnostic-leaking evidence fails and leaves Plan 13 unreachable.
  </behavior>
  <action>Confirm the primary, backup, and lock destinations are absent, run the fixed offline manual intake once, and add one opt-in private completeness test using the existing private-test pattern. Require the exact manual method/provision-reference/revision, seven paths, fixed official provenance/media, operating acknowledgment, independent roots, content verification, and identical before/after path-length-SHA-256 maps. Run the repository boundary gate. Do not repair or download source files, mutate the inbox or historical evidence, build/select a revision, or create a broader updater.</action>
  <verify>
    <automated>node --test --test-name-pattern="manual inbox|manual intake|offline" tests/authority/private-authority-collector.test.ts &amp;&amp; node --test --test-name-pattern="fresh v3 primary and backup completeness" tests/private-authority/private-source-completeness.test.ts &amp;&amp; pnpm verify &amp;&amp; node --test tests/private-authority/repository-boundary.test.ts</automated>
  </verify>
  <done>The manually provided v3 evidence is complete, immutable, offline-verifiable, and safe for Plan 13 to consume.</done>
</task>

</tasks>

<verification>
- No production official-source network request occurs.
- gsd-sdk query frontmatter.validate .planning/phases/01-rules-and-data-authority/01-15-PLAN.md --schema plan passes.
- gsd-sdk query verify.plan-structure .planning/phases/01-rules-and-data-authority/01-15-PLAN.md passes.
- Task 1 focused tests and typecheck pass before the human checkpoint.
- Task 3 private completeness, full verification, and boundary gates pass before Plan 13.
</verification>

<success_criteria>
- The project accepts only the exact ignored manual inbox for new v3 input and has no callable new production transport path.
- The user-provided files remain private, local, noncommercial, ignored, unpackaged, and non-redistributed; no artwork is acquired.
- Fresh primary/backup/lock evidence is published only after complete offline validation.
- The LLM player/adviser and original-art ideas are assigned to later phases without speculative implementation now.
</success_criteria>

<output>
After completion, create .planning/phases/01-rules-and-data-authority/01-15-SUMMARY.md.
</output>
