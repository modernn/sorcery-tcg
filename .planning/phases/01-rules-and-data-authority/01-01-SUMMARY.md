---
phase: 01-rules-and-data-authority
plan: "01"
subsystem: tooling-and-testing
tags: [node-24, pnpm, typescript, eslint, node-test, zod]

requires: []
provides:
  - Exact Node 24.19.0, pnpm 11.22.0, and TypeScript 6.0.3 package toolchain
  - Executable node:test todo contracts for DATA-01, DATA-02, and DATA-03
  - Synthetic input-lock contract and clean-room fixture policy
affects: [01-02, 01-03, 01-04, 01-05, authority-data]

tech-stack:
  added: [zod@4.4.3, typescript@6.0.3, "@types/node@24.1.0", eslint@10.8.1, typescript-eslint@8.67.0]
  patterns: [native TypeScript execution, node:test contracts, flat ESLint config, exact pnpm lockfile]

key-files:
  created:
    - package.json
    - pnpm-lock.yaml
    - tsconfig.json
    - eslint.config.js
    - tests/authority/canonical-json.test.ts
    - tests/authority/provenance.test.ts
    - tests/authority/card-snapshot.test.ts
    - tests/authority/bundle.test.ts
    - tests/authority/fixtures/README.md
    - tests/authority/fixtures/bundle-input/input-lock.json
  modified: []

key-decisions:
  - "Use Node's native TypeScript stripping and node:test without a transpiler or test-runner dependency."
  - "Keep all Wave 0 authority cases as named todo contracts for the dependent plans to replace with assertions."
  - "Suppress declaration-file checking because the mandated TypeScript 6.0.3 and @types/node 24.1.0 pins conflict internally; project code remains strictly checked."

patterns-established:
  - "Native TypeScript: NodeNext ESM with erasable syntax, verbatim modules, and no emitted build output."
  - "Authority fixtures: synthetic or minimal facts only, independently authored, deterministic, and clean-room safe."
  - "Input identity: ordered relative paths, exact byte hashes, storage modes, and one canonical input-root hash."

requirements-completed: [DATA-01, DATA-02, DATA-03]

duration: 15min
completed: 2026-08-20
---

# Phase 1 Plan 1: Toolchain and Authority Test Contracts Summary

**Pinned native TypeScript tooling with 57 discoverable authority contracts and a clean-room synthetic input lock**

## Performance

- **Duration:** 15 min
- **Started:** 2026-08-20T14:53:25Z
- **Completed:** 2026-08-20T15:05:16Z
- **Tasks:** 2
- **Files modified:** 11

## Accomplishments

- Pinned the exact Node, pnpm, TypeScript, Zod, ESLint, and Node declaration versions in one private ESM package and generated the lockfile with pnpm 11.22.0.
- Added 57 named `test.todo` contracts covering canonical identity, provenance, card normalization, authority precedence, offline validation, immutable publication, and storage restrictions.
- Added a deterministic synthetic input lock plus a fixture policy that forbids publisher corpora and copied GPL or unlicensed project material.
- Verified the frozen install, typecheck, lint, Node test discovery, input-lock hashes, clean-room policy, and absence of runtime subsystem scaffolding.

## Task Commits

Each task was committed atomically:

1. **Task 1: Pin the native TypeScript toolchain** - `011b7ef` (chore)
2. **Task 2: Establish Phase 1 test contracts and fixture policy** - `4b2944c` (test)

## Files Created/Modified

- `.gitignore` - Keeps generated `node_modules` out of version control.
- `package.json` - Pins the runtime, package manager, dependencies, and verification scripts.
- `pnpm-lock.yaml` - Locks the audited dependency graph generated by pnpm 11.22.0.
- `tsconfig.json` - Enables strict NodeNext native-TypeScript checking.
- `eslint.config.js` - Applies the minimal typescript-eslint recommended flat configuration to source and test TypeScript.
- `tests/authority/canonical-json.test.ts` - DATA-03 canonical serialization and hashing contracts.
- `tests/authority/provenance.test.ts` - DATA-03 provenance, integrity, graph, and path-safety contracts.
- `tests/authority/card-snapshot.test.ts` - DATA-02 deterministic normalization contracts.
- `tests/authority/bundle.test.ts` - DATA-01 authority resolution, offline bundle, and immutable publication contracts.
- `tests/authority/fixtures/README.md` - Synthetic-only clean-room fixture policy.
- `tests/authority/fixtures/bundle-input/input-lock.json` - Exact synthetic paths, hashes, storage modes, and canonical root.

## Decisions Made

- Used only the plan-approved dependencies; no test runner, transpiler, workspace, browser, database, runtime subsystem, or container was added.
- Kept todo contracts descriptive and behavior-focused so later plans can replace them incrementally without changing the acceptance boundary.
- Enabled `skipLibCheck` only for third-party declarations because the exact mandated TypeScript and Node type pins conflict inside `https.AgentOptions`; project TypeScript remains under all required strict flags.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 2 - Missing Critical] Ignored generated dependency installs**
- **Found during:** Task 1 (Pin the native TypeScript toolchain)
- **Issue:** The first install left `node_modules/` untracked, which would make a clean repository return impossible.
- **Fix:** Added the minimal one-line `.gitignore` entry.
- **Files modified:** `.gitignore`
- **Verification:** `git status --short` no longer reports `node_modules/`.
- **Committed in:** `011b7ef`

**2. [Rule 3 - Blocking] Suppressed incompatible pinned declaration internals**
- **Found during:** Task 2 (Establish Phase 1 test contracts and fixture policy)
- **Issue:** TypeScript 6.0.3 reported conflicts inside the mandated `@types/node` 24.1.0 declarations before checking project files.
- **Fix:** Enabled `skipLibCheck` while retaining strict checks for project code.
- **Files modified:** `tsconfig.json`
- **Verification:** `pnpm typecheck` passes.
- **Committed in:** `4b2944c`

**3. [Rule 3 - Blocking] Made lint tolerate the intentionally absent source tree**
- **Found during:** Task 2 (Establish Phase 1 test contracts and fixture policy)
- **Issue:** ESLint 10 rejected the empty `src/**/*.ts` command-line glob in this contract-only plan.
- **Fix:** Changed the lint command to `eslint .`; the flat config still selects only `src/**/*.ts` and `tests/**/*.ts`.
- **Files modified:** `package.json`
- **Verification:** `pnpm lint` and `pnpm verify` pass.
- **Committed in:** `4b2944c`

---

**Total deviations:** 3 auto-fixed (1 missing critical, 2 blocking)
**Impact on plan:** All changes are bootstrap correctness or repository hygiene; no production scope was added.

## Issues Encountered

- NVM initially attempted a 32-bit Node download; retrying the approved version with the existing 64-bit architecture installed and activated Node 24.19.0.
- The Windows `apply_patch` sandbox helper failed on two small follow-up edits after creating the main files. Exact Node-based text replacements were used as the narrow fallback required by the execution prompt.

## Authentication Gates

None.

## Known Stubs

- `tests/authority/canonical-json.test.ts`, `provenance.test.ts`, `card-snapshot.test.ts`, and `bundle.test.ts` contain 57 intentional `test.todo` contracts. Plans 01-02 through 01-05 replace their relevant todos with passing assertions.
- `tests/authority/fixtures/bundle-input/input-lock.json` binds the synthetic file bytes documented in the fixture README; Plan 01-05 materializes those three files when implementing the importer tests.

These stubs are the explicit Wave 0 deliverable and do not prevent this contract-scaffold plan from meeting its goal.

## User Setup Required

None - Node 24.19.0 and pnpm 11.22.0 were installed and activated during execution; no external service or human-only setup remains.

## Verification Results

- `pnpm install --frozen-lockfile` - PASS (pnpm 11.22.0, already up to date)
- `pnpm typecheck` - PASS
- `pnpm lint` - PASS
- `node --test tests/authority/*.test.ts` - PASS (57 discovered todos, 0 failures)
- Synthetic input-lock per-file and canonical-root hash check - PASS
- Fixture clean-room policy keyword check - PASS
- Runtime scope check - PASS (no `src`, `data`, or `docs` subsystem scaffolded)

## Next Phase Readiness

- Ready for Plan 01-02 to replace canonical JSON and provenance todos with passing implementations.
- Official acquisition and publication remain permission-gated for Plans 01-07 and 01-08 as designed.

## Self-Check: PASSED

- All 11 created files exist.
- Task commits `011b7ef` and `4b2944c` exist in git history.
- Full frozen-install and verification suite passed after the final task commit.

---
*Phase: 01-rules-and-data-authority*
*Completed: 2026-08-20*
